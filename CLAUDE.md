# CLAUDE.md — 全自动个人购物 AI 助手（ShoppingAgent）

本地单机对话式购物决策 Agent：Python 标准库后端 + 单文件 `index.html` 前端，LLM 仅做增强、全链路规则引擎兜底，双击 `.bat` 启动。项目全景见 `docs/project/project.md`。

## 文档体系与维护权责（最重要）

`docs/` 目录是开发者与 AI 共同维护的项目文档库，权责划分如下：


| 内容                                  | 维护方            | AI 的角色                                                                                                                                       |
| --------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| docs/project/<模块>/design.md         | 开发者主导        | 可提建议；经开发者审核通过才可修改，不得擅自改写                                                                                                |
| docs/project/<模块>/designs-specs.md  | **AI 生成并维护** | 基于 design.md 严格生成，是开发的直接依据；开发者仅简单审核                                                                                     |
| docs/project/project.md               | 开发者主导        | 可建议；协助完善需审核通过                                                                                                                      |
| docs/log/<YYMMDD>.md         | AI                | 每天一个开发日志；**每个小节标题下必须附创建时间行**（`> 创建于 YYYY-MM-DD HH:MM`，用 date 取实际时间）                                         |
| docs/study/                  | **AI**            | 学习笔记：问答产生的流程讲解/原理剖析/复盘整理，`YYYY-MM-DD-<主题>.md` 命名（详见 `study/study.md`）                                           |
| docs/project/idea/                    | **开发者**        | AI coding 时不需要关注、不得修改（例外：AI 产出的 spec/plan 按机制存放于此，只动自己产出的文件）                                                |
| docs/project/README.md                | **AI**            | spec/plan 存放与归档机制正本                                                                                                                    |
| docs/project/ 内各模块 spec/plan 文档 | **AI**            | brainstorming/写计划产出，命名`YYYY-MM-DD-<主题>-design.md` / `-plan.md`，按模块就地存放；落地或废弃后移入所属目录 archive/（机制见 README.md） |
| docs/project/优化建议区.md            | **开发者**        | AI 据此实施优化；实施后按轮归档进归档区                                                                                                         |
| docs/project/新功能开发区.md          | **开发者**        | AI 据此实施新功能开发（先对齐设计产出 spec/plan）；实施后按轮归档进归档区                                                                       |
| docs/project/问题疑惑区.md            | **开发者**        | **AI 不要主动查阅**，开发者指明时才阅读并答疑                                                                                                   |
| docs/project/演示汇报.md              | **AI**            | 小组课堂汇报演示底稿（开场/演示脚本/分工/答辩预案）；AI 随功能落地实时同步更新，开发者演示前审阅                                                 |
| docs/project/临时草稿（待写入）.md    | **开发者**        | **AI 不需要读取**                                                                                                                               |
| docs/ 各子文件夹的同名说明文档（docs.md、log/log.md 等） | **AI** | 新建子文件夹时同步创建 `<文件夹名>.md` 说明其功能与用法；`project.md` 例外——主体开发者主导，仅顶部说明节由 AI 维护                              |

工作流程：开发者写 design.md → AI 据此生成 designs-specs.md → AI 按 designs-specs.md 开发 → AI 记开发日志 → 落地/废弃的 spec/plan 归档进 archive/。

## 全局硬性规则

- **支付红线**：系统只做下单流程模拟（生成订单号），**绝不代用户支付**；确认前后必须有支付风险提醒。
- **数据真实性**：绝不编造商品数据；每条商品必须带 `data_source` 文字标注（[真实数据] / [演示数据]）；抓取失败如实告知，演示数据必须加声明。
- **无 emoji 红线**（2026-09-12 开发者修订）：全项目任何地方（前端 UI、后端消息模板、LLM 输出）严禁出现 emoji 图标；界面图标一律用 Material Symbols，AI 消息出口经 `ai_client.strip_emoji` 净化。
- **AI 全链路兜底**：LLM 仅做增强（解析/摘要/润色），不做事实决策；任何 AI 调用失败静默回退规则引擎，功能永不因 AI 失效而中断。
- **Key 安全**：API Key 本地脱敏存储展示（前后 4 位），绝不回传前端明文。
- **抓取克制**（2026-09-12 开发者修订，替代旧「只抓粘贴链接/禁止批量搜索」条款）：允许按用户主动发起的购物需求做**限量搜索**——每平台每次仅抓搜索结果页前 ≤10 条卡片，不翻页、不遍历、不做定时轮询；详情页仍支持用户粘贴 1-5 个链接逐个细抓；必须用真实有头浏览器（Playwright 为可选依赖）；遇验证码/登录墙暂停交还人工（用户自行扫码/手动登录，**程序绝不过手账号密码**，回复「继续抓取」续跑）；不绕过平台风控。
- **技术约束**：后端纯 Python 标准库（Playwright/Patchright 为可选依赖，Patchright 缺失自动回退原版 Playwright，二者缺失不影响启动）；前端保持单文件 `index.html`；存储用本地 JSON 文件，无数据库；服务只绑定 `127.0.0.1`。目录约定：业务模块统一放 `core/`（数据 JSON 仍存项目根），根目录只留入口 `web_server.py` / `shopping_agent.py`、`index.html`、启动脚本与文档。
- **已知限制如实告知**：历史价格曲线不支持（仅当前价对比）、图片分析仅为视觉推测——不得夸大能力。
- **启动脚本编码红线**：`.bat` 文件内容必须保持**纯 ASCII**（中文只允许出现在文件名中）。中文写进文件体会在 GBK 控制台下错位解析，导致 `此时不应有 >。` 等致命错误或整脚本碎片化。Python 探测必须「先实测再采用」（本机 `py` 启动器损坏，真机可用的是 `python` = `D:\app\Python\python.exe`）。

## Git 操作纪律（开发者指令）

- **项目所有 git 操作（add / commit / push / pull / checkout / reset / 分支 / 合并等全部命令）一律由用户本人维护完成，AI 一律不得执行。**
- AI 的上限：在用户明确要求时，仅以文字形式给出命令建议，绝不代为执行。
