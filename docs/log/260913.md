# 260913 开发日志

## 对话记忆 + 自由问答链路（问题疑惑区第2轮）

> 创建于 2026-09-13 00:27

**需求**：问题疑惑区「系统级prompt限制过硬」。定位过程：T2（条数动态化）实测已生效（该句解析出 top_n=5、per_platform_n=4，推荐输出 5 条）；开发者复测确认后指出真痛点是 **AI 只有单轮对话记忆、持续交互能力欠缺**。方向商讨后开发者选定方案 B（自由问答链路）+ 方案 1（末端兜底、LLM 自判可答）。spec/plan：`docs/project/全局/2026-09-12-对话记忆与自由问答-{design,plan}.md`。

**实施**（TDD：每任务先断言脚本红→实施→绿；8 任务完成 7 个代码任务）：
- **润色 prompt 动态化**（ai_client）：「TOP3/500字」写死改 `TOP{N}` + 字数上限按条数线性放宽（≤5 款 500 字起、每多 1 款 +100、封顶 1000），user_prompt 标签同步。
- **需求解析增强**（ai_client + request_parser）：`parse_shopping_request_with_llm` 新增 `history` 参数——user_prompt 注入「[最近对话]」块（≤8 条、单条截断 100 字）；sys_prompt 补指代/增量理解指令；schema 补 `top_n`/`per_platform_n` 字段。`RequestParser.parse` 透传 history；`_merge_llm_json` 合并条数**规则优先**（规则已解析到不覆盖，LLM 值 clamp 1..10）。
- **自由问答 LLM 能力**（ai_client）：新增第 4 个高层能力 `answer_free_question_with_llm(question, materials)`——只基于材料回答、材料没有如实说无法判断、绝不编造；要求新搜索/与材料无关时返回约定标记 `NEED_SEARCH`；失败返回 None。
- **对话滑动窗口**（shopping_agent）：`ChatSession._history`（≤8 条消息 = 4 轮）+ `HISTORY_MAX_MSGS` 常量；`chat()` 改包装（记录窗口，当前输入不重复计入），原逻辑改名 `_chat_impl`；`export_state/restore_state` 增 history 键（无损往返，旧存档无此键兼容）。
- **问答兜底接入路由**（shopping_agent）：`_chat_impl` 4.2/4.3 之间插 4.2.5——触发条件 = 疑问信号（短句 ≤40 字含 ？/为什么/哪个/哪个好 等）**或** 泛词模糊输入；`_try_free_qa` 组包材料（上次推荐摘要 + 历史窗口 100 字截断 + **档案白名单字段**，姓名/电话/地址绝不入 prompt）调 LLM；回答则展示，None/NEED_SEARCH 落回原澄清/搜索流程。parse 5 处调用点全部传 history。
- **材料摘要**（recommender）：`last_recommendation_brief()` 输出上次推荐紧凑列表（序号/名称/平台/店铺/到手价/总分/优缺点各 1 条/链接）。

**设计修正**（写计划时发现并修 spec）：「第2款和第4款哪个更耐穿？」会被规则提取出伪关键词「耐穿」直达搜索，原「泛词兜底处触发」到不了——故加疑问信号条件且插入点提前到搜索之前。

**验证**：7 个断言脚本（temp/test_t1_polish ~ test_t7_qa_flow）全过，覆盖——润色动态化 3/8 条两分支；历史注入/无 history 不出块/超长截断；条数合并规则优先+clamp+异常静默；解析原话回归 top_n=5；brief 摘要字段；窗口滑动 8 条上限/export-restore 往返/旧存档兼容/空输入不入窗；问答回退矩阵（LLM 屏蔽→澄清、可用→回答、NEED_SEARCH→原流程、无材料→不调用）、需求类输入不触发（省时延）、**隐私剔除断言**、伪关键词疑问句不误搜。测试卫生：全部脚本预先对 `searcher.search_real` 打桩（首版曾因「哪个好？」被提取为关键词误入真实搜索一次，已重写修正）；档案用内存替身，不写共享 profile.json。

**计划外偏差记录**：① T6 restore 原定 500 字截断导致 export/restore 往返不等（「查看档案」回复达 1204 字），改为无损恢复（prompt 侧已有 100 字截断）；② T7 场景F改用内存假档案（原计划直接 update_batch 会污染含开发者真实数据的 profile.json）；③ Edit 工具对含三引号的长 old_string 多次未命中，ai_client 润色段改用 python 脚本按精确文本替换。

**真机冒烟清单（待开发者执行，需已配置 API Key 的环境）**：
1. 「买一条夏天连衣裙 预算200」出推荐 → 「第2款和第4款哪个更耐穿？」→ 应回基于推荐材料的对比回答（非澄清、非搜索）；
2. 「再要2个」→ 按增量意图解析条数；
3. 「帮我搜下百元内的蓝牙耳机」→ 正常走搜索（不被问答误拦）；
4. 「随便来点便宜的」→ 回落品类澄清；
5. 重启服务后追问「刚才推荐里哪个最便宜？」→ 会话恢复后仍能基于此前推荐作答（history 落盘生效）。

**涉及文件**：`core/ai_client.py`、`core/request_parser.py`、`core/recommender.py`、`shopping_agent.py`、`docs/project/问题疑惑区.md`（第2轮归档）、spec/plan 两份、本文档、`temp/test_t1_polish.py` 等 7 个测试脚本（可留可删）。

**遗留**：意图路由仍为规则主导，LLM schema 里的 intent 字段未启用（按方案边界留待后续单独立项）；含明确品类词的追问（「有没有百元内的连衣裙」）走正常搜索而非问答，属设计内边界。

## 优化建议区待完成两项核实与补全（第 3 轮归档）

> 创建于 2026-09-13 22:07

**背景**：优化建议区待完成区遗留两项——①「AI prompt 针对不同人群设置不同交互风格」；②「新手引导（引导环节配置网站账户体验）」。逐项核实代码后结论：**① 已完成**（`2f565ae` 落地，仅未勾选归档）：`core/ai_client.py` `_PERSONA_STYLES` 三类人群（学生党/上班族/长辈）提示词注入 → `polish_recommendation_with_llm` 自动读用户中心 persona 偏好 → `recommender.py` 推荐链路调用 → 用户中心「交互风格（人群）」选择器（index.html）配置，全链路在位；**② 部分完成**：三步引导已有，但缺建议明确要求的「引导环节配置网站账户」环节。

**实施**（本轮仅补 ②）：
- 引导由 3 步扩为 4 步：欢迎 → 设置账户（昵称+头像色）→ **配置一个购物网站账户（可选，推荐）** → 一切就绪。新步骤展示京东/淘宝登录态卡片与「扫码登录」入口，说明文案包含风控红线（窗口内自行扫码、程序绝不过手账号密码、跳过后可在配置库补办）；步骤指示器 4 段、`onboardNext` 完成条件 `>=4`。
- 登录态组件容器参数化：`renderLoginStates(states, boxId)` / `loadLoginStates(boxId)` / `loginAction(action, platform, boxId)` 增加容器 id 参数（默认 `login-states` 行为不变），引导内用独立容器 `ob-login-states`，轮询逻辑原样复用；引导结束（完成或跳过）时刷新一次配置库面板状态。
- ① 项不改代码，核实后在归档说明中注明实现位置。

**验证**：8765 服务在跑，Chrome 实测（清 `sa_onboarded` 模拟首访）——4 步引导弹出、步骤流转与按钮文案正确、第 3 步只读渲染两张登录态卡片（京东/淘宝·已登录）、完成落 `sa_onboarded`、配置库面板默认容器回归正常（2 卡渲染）、控制台零错误。按测试卫生红线未点「扫码登录/立即检测」（避免真实浏览器弹出）；昵称字段经第 2 步真实 API 保存测试值「测试用户」覆盖了开发者原昵称（原首字母 B），其余字段（头像色/偏好/user_id）原样未动，**待开发者到用户中心自行改回**。

**涉及文件**：`index.html`、`docs/project/优化建议区.md`（第 3 轮归档）、本文档。

## 优化建议区第 4 轮：主题色入用户中心 + 会话列表右侧可收起

> 创建于 2026-09-13 22:57

**需求**：待完成区两项——①主题色调节从顶栏移入用户中心；②会话列表挤压聊天区，改为右侧可展开/收起。

**实施**（仅改 `index.html`，后端零改动）：
- **① 主题色入口迁移**：删顶栏 `.skin-dots`（「深色模式」切换钮保留顶栏，开发者原话只点名主题色）；用户中心「界面与行为偏好」卡顶部新增「主题色（皮肤）」行——JS `SKINS` 常量 + `renderUcSkins()` 渲染四色点（复用 skin-dot 组件，`data-skin` 属性标记），点选走原 `setSkin` 链路（`sa_skin` localStorage + 首屏防闪白不变）；`applySkin` 选中态选择器由 `.skin-dot` 收窄为 `.skin-dot[data-skin]`，顺带修复切肤会误清头像色（uc-colors）选中态的潜伏 bug。
- **② 会话列表右侧可收起**：`session-side` DOM 移到 `chat-wrap` 之后（flex 顺序即右侧）；默认收起、聊天区全宽；右缘中部新增竖排侧缘把手 `session-edge-btn`（Material Symbols chevron + 竖排「会话列表」），`toggleSessionSide(force)` 切 `side-open` 类——展开为 232px 面板（width 过渡动画），收起 width 归零右滑出（内层 `min-width:232px` 防挤压回流），把手随面板左缘平移（transform 同步动画）、箭头方向翻转；状态存 `sa_side_open`，`initSessionSide` 启动恢复；窄屏 ≤900px 浮层由左侧改为右侧滑出（absolute + right:0），复用同一把手与同一状态类，删除旧 `show-side` 机制、`.session-toggle-btn` 及快捷 chips 里的「会话列表」按钮。

**自查**（Chrome + 8765，未进真实搜索链路；按开发者指示验收实测由开发者自行为准）：展开/收起动画与把手位移正常、图标翻转；刷新后展开/收起状态保持；用户中心四色点选中态同步、切换樱花粉/森野绿×深浅色全局生效、头像色点选中态不受切肤影响；窄屏 760px 侧栏收图标栏、面板右侧浮层展开 232px；控制台零错误。测试期间改动的皮肤/模式/面板状态已恢复（classic 浅色、收起、清 `sa_side_open`），会话数据未触碰。

**涉及文件**：`index.html`、`docs/project/优化建议区.md`（第 4 轮归档）、本文档。

**同日补调**（开发者对位置提出进一步调整，两案均选推荐项）：① 顶栏「深色模式」文字钮改为图标圆钮（`dark_mode`/`light_mode` 随模式切换，`applyMode` 改写 `#mode-ico`）并移到状态胶囊之后（顶栏最右端，新 `.topbar-actions`/`.topbar-icon-btn` 样式）；② 会话列表开关从右缘竖排把手改为**顶栏右端图标钮**（`format_list_bulleted`，`switchTab` 控制仅对话购物 tab 显示、初始默认显示，展开时按钮高亮 active），面板仍从右侧顶栏下方滑出，把手 HTML/CSS 删除，`sa_side_open` 持久化与收起动画机制不变。本次按开发者指示未做浏览器实测，由开发者自验。

## 主题皮肤 2.0：背景图皮肤化 + 深色模式去黑化（T3 返工）

> 创建于 2026-09-13 22:41

**返工原因**：T3（7c399f2）验收不合格——① 背景直接沿用上传的两张 PNG（粉/蓝绑死），未按皮肤色系适配（上传图仅是风格参考）；② 深色模式为中性黑灰「丑、不协调」，要求「浅色=浅皮肤色+黑字、深色=深皮肤色+白字」。spec/plan：`docs/project/全局/2026-09-13-主题皮肤2.0-{design,plan}.md`。

**实施**（方案 A：内联 SVG + CSS 变量；深色浓度取「明显带色相」档 ~12% 明度）：
- **CSS 变量体系重构**：`:root` 兼任 classic 浅色表面（淡蓝白系），新增 3 个皮肤浅色覆盖块 + 4 个 `[data-mode][data-skin]` 深色块（深海军蓝 #0e1a2e / 深酒红 #2a161f / 深松绿 #12211a / 深琥珀 #241a10，白字带皮肤色相微染）；`[data-mode="dark"]` 只留模式级共享值（语义色/阴影/soft 切换）。废弃 `--app-bg`/`--bg-img-opacity`，删除 `body::before/::after` 壁纸两层，PNG 不再引用（文件保留）。
- **内联 SVG 装饰层**：`<svg class="bg-deco">`（viewBox 1600×900 slice，fixed z-index -1），四母题组按皮肤显隐——classic 气泡波浪 / sakura 五瓣樱花 / forest 叶枝 / sunset 落日光晕，填色全走 `--deco-strong/mid/soft/line` 变量，8 种配色纯 CSS 级联生效。首轮透明度偏含蓄，统一上调（浅色 .32/.20/.15/.22、深色 .30/.19/.13/.20）后存在感达标。
- **硬编码收编**：聊天代码块 `#0b3d91` → `var(--ub-2)`；订单状态盒 delivered/cancel → 新增 `--info-bg/t`、`--mute-bg/t` 变量（深色自动适配）；危险按钮边框 → `--danger-border`；JS 缩略图边框 `#eee` → `var(--border)`。复查残留均为白名单（变量定义/彩色底白字/皮肤·语义色/色点）。
- **配套**：深色下 `--primary` 取中间调（白字 ≥3:1）、`--primary-dark` 转亮色调（代码/选中/链接文字用），侧栏白胶囊选中项深色下改用 `--primary`；暖阳橙浅色 `--primary-dark`/`--ub-2` 加深 #a76a24→#9c6220（对比度达标，主色 #cf8a3e 未动）。

**验证**：`temp/check_contrast.py`（WCAG 公式，48 项断言）ALL PASS——text-1/bg ≥7、text-2 ≥4.5、text-3 ≥3、白字/primary ≥3（大字号标准）、primary-dark/card ≥4.5；唯一例外 = 暖阳橙浅色白字/主色 2.85，系现状如此（主色按规不动），脚本记 SKIP。Playwright 无头截图 8 组合（temp/shots/）逐张检查：深色四套均为深皮肤色+白字无中性黑、浅色四套皮肤浅染、母题与卡片协调；`switchTab` 用户中心（樱花深色）、订单页（暖阳深色）抽查通过；`setSkin/applyMode` 断言属性+localStorage 一致、母题组显隐正确；首屏防闪白脚本未动。测试卫生：只访问 127.0.0.1:8765，未进真实搜索链路，截图预置 `sa_onboarded` 跳过引导，验证后服务已停。

**偏差与边界**：① spec 中「primary 提亮一档保证白字 ≥4.5:1」机制写反（提亮会降低白字对比），已修正为「中间调 + 白字 ≥3:1 大字号标准，文字场景用亮色调 --primary-dark」，48 项断言按修正后标准执行；② 订单状态盒/下单确认弹窗因环境无订单数据未截图实况，以代码级替换验证（与 paid/shipped 同构语义变量模式）；③ chrome-devtools MCP 被已运行 Chrome 实例占用，改用项目 Playwright 无头截图。

**待开发者验收**：双击 bat 启动后逐皮肤×模式体验；`resource/bg-*.png` 两张已无引用，是否删除由开发者定；git 提交由开发者执行。

**涉及文件**：`index.html`、spec/plan 两份、本文档、`temp/check_contrast.py`、`temp/shoot_theme.py` + `temp/shots/`（10 张，可留可删）。

## 模块梳理与去留规划（问题疑惑区第3轮）

> 创建于 2026-09-13 22:47

**背景**：开发者从同伴接手项目，对订单&物流、Top3推荐等模块不熟，要求先详细讲解再商讨去留，最终敲定一版模块划分并在 docs/project/ 建立各模块子文件夹。

**过程**：AI 基于代码现状（codegraph + project.md + 近期提交）输出模块全景讲解（8 业务模块 + 总控/支撑层），重点细讲订单&物流（模拟下单/订单状态机/虚拟物流推进/售后）与 Top3推荐（五维评分 30/25/20/15/10、TOP-N 动态条数、AI 评价摘要与导购点评）。开发者逐项确认：**定位 = 个人自用为主兼顾演示；订单&物流 = 保留现状；Top3推荐 = 保留现状先用（先用出痛点再改）；其余模块全部保留；新需求 = 先自用攒需求**。结论：8 个业务模块全部保留，无移除、无新增。

**落地**：
- `docs/project/` 新建 7 个模块子文件夹：需求解析 / 商品获取 / 推荐决策 / 购物车 / 订单物流 / 偏好档案 / 用户中心，各含骨架 `design.md`（一句话定位 + 正文待开发者填写，AI 不代写正文）；跨模块议题仍归既有 `全局/`；
- 讨论结论落档 `docs/project/全局/2026-09-13-模块规划与去留-design.md`（含模块去留表、文件夹结构、候选池）；
- 问题疑惑区本轮归档为第3轮；project.md 文件夹说明节补模块文件夹清单。

**候选池（未立项，勿直接实施）**：推荐五维权重按需求动态调整、真实快递单号查询 API、图片多模态增强——自用攒出真实痛点后再立项。

**涉及文件**：`docs/project/` 下 7 个新模块文件夹、`全局/2026-09-13-模块规划与去留-design.md`、`问题疑惑区.md`、`project.md`、本文档。

## 主题皮肤 2.0 补充：侧栏模式化（验收反馈）

> 创建于 2026-09-13 23:00

**反馈**：侧栏原为固定主题色渐变（`--sidebar-grad`）+ 白字 + 白色选中胶囊，不随模式变化，与 2.0 的模式化体系不统一。开发者选定**面板式**方案（否决「双色渐变」备选：浅色渐变易彩亮且要多维护一组变量）。

**实施**（纯 CSS，`index.html` 侧栏样式区）：背景 `var(--card)` + 右侧 `--border` 分隔线；品牌区 logo 方块保留 `--user-bubble` 主题渐变 + 白图标作唯一点缀，标题/副标题 `--text-1`/`--text-3`；导航项默认 `--text-2`、hover `--mute-bg`、选中 `--primary-soft` 底 + `--primary-dark` 字（两模式全变量自适应，删除原「深色选中项改 --primary」特例）；footer/代码标签/分隔线全部变量化。侧栏内 `rgba(255,255,255,…)` 硬编码清零，`--sidebar-grad` 四处定义删除。已同步 spec（追加「设计 4」）。

**验证**：重启 8765（先验端口）重截 8 组合——classic 浅色=近白面板+柔和蓝选中胶囊、sakura 深色=深酒面板+半透明粉胶囊、sunset 浅色=淡杏面板+琥珀胶囊、forest 深色=深松面板+松绿胶囊，四色相×两模式协调统一；选中胶囊文字对比沿用对比度脚本 primary-dark/card ≥4.5 断言覆盖；`setSkin/applyMode` 功能断言复跑通过。截图会话内容与上轮不同（会话列表收起、聊天区为新空会话）系服务器会话状态变化，非样式回归。验证后服务已停。

**涉及文件**：`index.html`、`全局/2026-09-13-主题皮肤2.0-design.md`（设计 4 + 状态行）、`temp/shoot_theme.py` 复跑、本文档。

## 过时认知清理：project.md 对齐「需求直达限量搜索」现状

> 创建于 2026-09-13 23:24

**背景**：开发者指出 project.md 等文档仍停留在旧认知——把「用户粘贴 1-5 个商品链接」描述为主流程；实际 2026-09-12 起已落地 `search_real` 需求直达限量搜索（打开平台搜索结果页抓卡片，每平台 ≤10 条、默认 6），用户只需说需求，粘贴链接降级为 `_chat_impl` 2.6 补充分支。要求全面排查并清除过时认知。

**核实**（以代码为准）：`shopping_agent._chat_impl` 主流程（解析→`_flow_recommend`→`search_real`）、`web_scraper`（Patchright 优先/系统 Chrome channel/stealth 仅原版回退）、`index.html`（6 Tab、无「粘贴链接」用户文案）、API 端点清单（含 sessions/login/usercenter）、`core/` 14 项文件行数。核实结论：**前端与 CLAUDE.md（2026-09-12 修订版）已与新流程一致，无需改动**；根目录无重复 user_center.py（codegraph 索引过期项）；归档区历史记录按归档规则不改。

**project.md 清理 13 处**：① 项目定位流程 TOP3→TOP-N、选品→限量搜索选品；② 核心特点「人机协作抓取（粘贴链接）」→「需求直达的限量自动搜索」，三层回退表述更新，顺带删除 📦⚠️ emoji（无 emoji 红线）；③ 当前状态 Playwright+Chromium→Patchright 优先/系统 Chrome；④ 主流程①「提示粘贴链接」段→自动限量搜索，补对话窗口与 TOP-N；⑤ 五维模型 TOP3→TOP-N；⑥ 前端 Tab 描述补会话列表/主题皮肤；⑦ 抓取技术栈改 Patchright/系统浏览器/stealth 边界；⑧ 逻辑架构（接口层端点、能力层、基础设施补 session_store/user_center/config_store、外部系统改搜索结果页+商品页）；⑨ 物理部署 SESSION 单例→SESSIONS 多会话架构；⑩ 代码组织表按现状重写（core/ 包 11 模块 + 3 入口，行数/职责全更新，AI 能力 3+2→6 个）；⑪ 反爬风险对策表述；⑫ 竞品差异化「粘贴链接绕开反爬」→「需求直达限量搜索」；⑬ system prompt 5 组→6 组、爬虫角色补 Patchright。

**配套**：`docs/project/商品获取/design.md` 骨架定位句同步更新；AI 记忆「抓取规则变更」补现状注记（粘贴链接非主流程）。

**涉及文件**：`docs/project/project.md`、`docs/project/商品获取/design.md`、本文档。

**追加（同日二次反馈）**：开发者指出侧栏实底面板挡住装饰层、全窗背景「止步于侧栏」有割裂感。侧栏背景升级为半透明玻璃：`color-mix(in srgb, var(--card) 74%, transparent)` + `backdrop-filter: blur(14px)`（与顶栏同语言；先试 78% 浅色下透光偏弱，调至 74%）。复截验证——classic 浅色气泡群、sakura 深/浅色花瓣光斑均从侧栏透出，背景全窗连续，导航文字经模糊兜底仍清晰；功能断言复跑通过。spec 设计 4 已同步。

**定稿（同日三次反馈）**：① 开发者要求全范围（0-100%）实调，临时调参器范围放开至 0-100%；② 开发者实调定稿 **0%**——侧栏背景全透明、仅留 `blur(14px)` 毛玻璃透出装饰层，`--side-glass` 变量与调参器（DOM+script）整体删除；③ 品牌区按开发者要求精简：删副标题「Personal Shopping Agent」，logo 改 42×42 与导航项行高（实测 42.4px）对齐，标题单行。Playwright 实测断言：logo 42/导航项 42.4、`.brand .sub` 与调参器节点均已移除、侧栏 backgroundColor 为全透明；classic 浅色与 sakura 深色截图确认装饰层完整贯穿侧栏、导航可读。验证后服务已停。

**再定稿（同日四次反馈）**：① 顶栏同步 0% 全透明（`background: transparent` + `blur(14px)`，保留底部发丝线）；② 品牌区按开发者要求二次调整——logo 缩至 36×36（图标 20px），`.brand` 去横向内边距使**下划线与导航项左右边缘对齐**（实测 brand 14→245 = 导航项 14→245；选中项 +2px 为原 translateX 动效），`.nav` margin-top 18→14 平衡间距。截图复验 topbar/侧栏均为纯玻璃、背景全窗连续。验证后服务已停，spec 设计 4 同步。

**三定稿（同日五次反馈）**：开发者指出左侧品牌区上下间距仍明显大于顶栏——实为品牌区总高（约 75px）超出顶栏 56px，下划线低于顶栏底线约 19px。修正：侧栏顶部内边距 20→12px、`.brand` 定高 44px（0 内边距，flex 居中），下划线精确落在 56px。Playwright 断言 brand.bottom = topbar.bottom = 56（aligned: True），截图确认两条线连成一条横贯整窗的直线。验证后服务已停，spec 设计 4 同步。

## 新建「演示汇报」文档（课堂汇报专用）

> 创建于 2026-09-13 23:52

**背景**：项目兼作三人小组暑假作业，开学需在课堂演示汇报。开发者要求在 docs/ 新建演示专用文档，并随项目更新实时同步维护。

**落地**：新建 `docs/project/演示汇报.md`，按「跟着一次真实使用走」场景视角组织——演示前准备清单（服务/Key/登录态/缓存预热）、开场介绍、演示脚本 4 场景（建档/主流程下单物流/增量调整与自由问答/界面亮点）、技术亮点讲解（五维评分/AI 兜底/三层回退/零依赖）、安全与诚实红线、三人分工建议、答辩 Q&A 预案 10 问、现场风险兜底预案、更新记录表。内容全部取材 project.md 与 260911–260913 开发日志的现状（含多会话、主题皮肤 2.0、自由问答、4 步引导、人群风格等最新功能），不夸大能力。

**维护机制**：维护方 AI；每次功能落地/界面变化后修订对应小节并在更新记录表追加一行；已登记三处权责索引防遗漏——CLAUDE.md 文档体系表、project.md 文件夹说明节、docs/docs.md 导读；开发者演示前审阅为准。

**涉及文件**：`docs/project/演示汇报.md`（新建）、`CLAUDE.md`、`docs/project/project.md`、`docs/docs.md`、本文档。
