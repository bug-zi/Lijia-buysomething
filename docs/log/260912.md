# 260912 开发日志

## 文档库体系补充（开发者指令，文档体系）

> 创建于 2026-09-12 00:12

**需求**：开发者三条补充——① 新增开发者维护的「新功能开发区.md」；② CLAUDE.md 明确项目所有 git 操作由用户本人维护完成；③ 每个子文件夹须有以该文件夹命名的说明文档。

**设计定稿**（无独立 spec，随体系正本落笔）：

- `project/` 目录的说明文档承载于同名 `project.md` 顶部「文件夹说明」节（该名已被项目总介绍占用，不改开发者正本结构）；其余文件夹直接建同名说明文档
- 新功能开发区与优化建议区的分工边界：新功能 = 从 0 到 1，优化建议 = 已有功能改进

**实施**：

- 新建 `docs/project/新功能开发区.md`（待完成 + 按轮归档区，注明开发流程：需求 → 对齐设计出 spec/plan → 实施 → 日志 → 归档）
- 新建 `docs/docs.md`（docs/ 体系导读）、`docs/log/log.md`（日志目录说明）
- `docs/project/project.md` 头部重组：顶部「文件夹说明」节（AI 维护）+ 项目总介绍主体（开发者主导，正文未动）
- `CLAUDE.md`：权责表新增「新功能开发区」「文件夹说明文档」两行（保留开发者的表格对齐格式，顺带修正 `<YYMMDD></yymmdd>.md` 笔误）；Git 纪律改为「项目所有 git 操作一律由用户本人维护完成，AI 一律不得执行，上限仅为文字形式命令建议」
- `docs/project/README.md`（机制正本）新增「文件夹说明文档」节：AI 新建子文件夹时同步创建同名说明文档

**验证**：docs/ 全树复核，三个子文件夹（docs/、log/、project/）说明文档就位；权责表与各说明文档口径一致。

**涉及文件**：`CLAUDE.md`、`docs/docs.md`（新建）、`docs/log/log.md`（新建）、`docs/project/新功能开发区.md`（新建）、`docs/project/project.md`（头部重组）、`docs/project/README.md`（补规则）、本文档。

## 抓取被拦截问题修复（问题疑惑区第1轮，web_scraper）

> 创建于 2026-09-12 00:41

**需求来源**：`docs/project/问题疑惑区.md` 待完成问题——天猫链接抓取 `Page.goto` 30s 超时（截图 `resource/网络抓取异常.png`）；开发者同时询问能否用 MCP/云端浏览器工具（如 Tavily）解决。

**方案商讨**：给出 A（修现有 Playwright 链路）/ B（CDP 接管本机已登录 Chrome）/ C（云端 API，不推荐：无登录态、机房 IP、隐私外发）三案，开发者选定方案 A。

**诊断过程**：
1. modlens 读取截图：纯白页面，`domcontentloaded` 30s 无响应；
2. 代码核查（codegraph + Read）：UA 硬编码 Chrome/124 与捆绑 Chromium 版本不匹配；goto 单次尝试；超时落「抓取异常」而非「交还人工」；
3. 实测：真实 Chrome 打开京东/天猫/淘宝首页全部正常 → 网络与指纹无问题；**原链接为直通车广告追踪链接（`ali_refid/ali_trackid/xxc=ad_ztc`），有时效与用户绑定，过期后即网络层黑洞**——结论：换任何工具都抓不到失效链接，问题主因是链接失效 + 旧链路对真实拦截无兜底。

**实施**（仅改 `web_scraper.py`）：
- 新增 `_find_system_browser()`：探测本机 Chrome/Edge，`_launch_browser` 优先 `channel` 启动真实浏览器（UA 不再覆盖、跟随真实浏览器），channel 启动失败回退捆绑 Chromium；`_check_browser_binaries` 同步接受系统浏览器；
- 新增 `_goto_with_retry()`：`domcontentloaded` 30s → `commit` 45s 两段式重试；
- 新增 `_save_failure_screenshot()`：失败截图存 `temp/`（已在 .gitignore）；goto 超时与验证码同样「交还人工」（block_reason 含「继续抓取」触发续跑、need_human=True 停止后续链接），与 `shopping_agent._flow_grab_and_compare` 的续跑契约对齐。

**验证**：模块导入自测通过；本机检测到真实 Chrome；真实 Chrome 启动 + 京东首页加载冒烟通过；用原始失效链接实测——两段式重试均超时后正确输出「交还人工」提示并留存截图（`temp/scrape_fail_20260912_003912.png`），续跑契约生效。

**遗留**：开发者需用**新鲜**商品链接做最终验收；若新鲜链接仍被拦截，升级方案 B（CDP 接管）。

**涉及文件**：`web_scraper.py`、`docs/project/问题疑惑区.md`（第1轮归档）、本文档。

## 真实搜索：需求直达商品（全局轮，Patchright 基座）

> 创建于 2026-09-12 01:29

**需求**：开发者目标 UX——输入「通勤运动鞋 200-400」直接返回 3-5 条跨平台带图商品再打分 TOP3；解除前任「禁止批量搜索」规则（当前开发者明示）；联盟 API 申请未通过；登录态参照 bilibili-summary-assistant 理念（用户自己扫码，程序绝不过手账号密码）。spec/plan 见 `docs/project/全局/archive/2026-09-12-真实搜索-{design,plan}.md`（开发者中途修订采纳 Patchright，已归档）。

**实施**：
- `web_scraper.py`：① `SEARCH_SITES`（京东/淘宝搜索 URL + 多候选卡片 selector）+ `search_platform()` 限量搜索（每平台 ≤10 条，默认 6；`_detect_search_wall` 搜索页专用拦截检测——替换 `_detect_block`，因其「请登录/slider」等词在搜索页导航/轮播是正常文案会误判）；② `open_login_and_wait()`：登录墙时后台线程弹独立有头登录窗口等扫码，登录成功自动关闭，会话写入持久化 profile；③ `_launch_browser` 导入兜底链 **patchright.sync_api 优先 → 原版 playwright**（T1.5，采纳开发者 GitHub 调研结论），`_has_playwright()` 两者任一即可，真实 Chrome channel + 有头 + 持久化 profile 即 Patchright 官方推荐配置；
- `product_searcher.py`：`search_real()` 逐平台搜索聚合 + `_product_from_card()` 卡片转 Product（价格/销量解析失败留 0、评价字段留空，绝不编造，`data_source="真实"`）；
- `shopping_agent.py`：`_flow_recommend` 改为「真实搜索优先 → 预算初筛 → 打分 TOP3」，搜索失败/不足回退「粘贴链接」老路径；新增 `_pending_search` + 「继续抓取」续跑搜索分支；登录墙回复引导扫码；
- `CLAUDE.md`：「抓取克制」替换为新条款（限量搜索：每平台 ≤10 条、不翻页不遍历、仅用户主动发起）；「技术约束」更新为 Playwright/Patchright 可选依赖；web_scraper 头部 docstring 同步。

**验证**：
- 模块语法/导入自测通过；patchright 1.62.3 安装并生效；
- 京东真实搜索冒烟：未登录 → 6.3s 内正确识别登录墙，弹出登录窗口并交还人工（spec 实施中发现：**京东搜索未登录也是整页登录页**，原平台表「未登录可搜」过时，已在 spec 状态区补记）；
- `python shopping_agent.py demo` 18 案例全量回归无异常；
- patchright 缺失回退回归：模拟导入失败后 `_has_playwright` 仍 True，原版 playwright 正常驱动浏览器加载京东。

**待开发者验收**（需扫码，自动化无法替代）：在助手输入购物需求（如「通勤运动鞋 200-400」）→ 在弹出的登录窗口扫码 → 回复「继续抓取」→ 确认京东 ≥3 条带图真实商品；淘宝同理。京东新版搜索页 DOM 若与现有 selector 不匹配导致 0 卡片，按归档回改通道调整 selector。

**涉及文件**：`web_scraper.py`、`product_searcher.py`、`shopping_agent.py`、`CLAUDE.md`、`docs/project/全局/archive/2026-09-12-真实搜索-{design,plan}.md`、本文档。

## 登录流程修复：同窗口扫码 + Patchright 下停用 stealth 注入

> 创建于 2026-09-12 01:52

**问题（开发者实测反馈）**：① 扫码登录京东一直不成功；② 后续自动启动的浏览器窗口闪现即被关闭，无法扫码；③ 希望登录一次后短期免登录。

**根因分析**：
1. **扫码被拒最大嫌疑**：Patchright 官方明确警告不要叠加自定义 stealth/init 注入脚本（破坏驱动级隐身、暴露自动化特征），而 `_launch_browser` 仍在注入 `_STEALTH_JS`；
2. **窗口闪退**：登录窗口由后台线程另开一个浏览器，与搜索浏览器抢同一个 `.browser_profile` profile 锁，互相挤掉；
3. 「登录一次短期免登」本就是 `.browser_profile` 持久化设计，登录没成功导致该能力无从体现。

**修复（仅 `web_scraper.py`）**：
- Patchright 生效时**停用 `_STEALTH_JS` 注入**（仅回退原版 Playwright 时保留）；新增 `_get_sync_playwright()` 返回当前基座标识；
- 删除后台线程版 `open_login_and_wait`，改为 `_goto_login_and_wait()`：**同一个窗口内**跳登录页原地等扫码（最长 3 分钟，轮询 URL 跳离登录域判定成功），成功后**自动重搜**，无需再回复「继续抓取」；超时/关窗才交还人工；
- `_launch_browser` 启动加重试链（真实 channel → 无 channel → 等 2s 重试），兜底 profile 短暂被占用场景。

**验证**：无残留引用；语法/导入自测通过；模拟登录超时路径交还人工正常；demo 18 案例回归通过。京东未登录页面表现仍不稳定（有时报墙有时空结果），登录态为硬前提。

**待开发者验收**：输入购物需求 → 弹出的窗口跳到登录页 → 扫码（若出现滑块一并完成）→ 等待自动重搜返回结果；登录一次后短期免扫码。

**涉及文件**：`web_scraper.py`、本文档。

## 登录取证 + 京东新版搜索页 selector 适配（真实搜索收尾）

> 创建于 2026-09-12 01:56

**背景**：开发者复测仍失败。取证：temp/ 无新失败截图（排除 goto 超时）；profile 探测为未登录（排除会话持久化问题）→ 定位为**扫码登录环节未成功**。

**登录取证（开发者现场扫码）**：弹登录页逐步截图复现——18s「扫描成功! 请在手机上确认」，25s 跳转 www.jd.com，**登录成功**。证明上一轮修复（Patchright 下停用 stealth 注入）有效，此前失败系旧代码注入脚本暴露自动化特征所致。

**京东新版搜索页适配**：登录态下搜索页可开但 0 卡片——DOM 解剖发现京东已改版为新版搜索页（"购物灵感"+插件化卡片）：
- 卡片：`plugin_goodsCardWrapper`（实测 60 张），带 `data-sku` 属性，**无 `<a href>`**；
- 字段：标题 `[class*='goods_title_container']`、销量 `[class*='goods_volume']`（"已售2万+"格式）、图片 360buyimg CDN、价格拆多个 span（用卡片文本 `¥\d+` 兜底正则提取）；
- `SEARCH_SITES["京东"]` 全部更新（旧版 `#J_goodsList li.gl-item` 保留作回退），新增 `sku_attr`/`url_template` 机制（data-sku → `item.jd.com/{sku}.html`），卡片提取循环同步扩展（sku 拼链接 + 价格兜底）。

**验证（端到端）**：`search_real('运动鞋', ['京东'])` 返回 6 条真实商品——名称/到手价（¥67-259）/销量（万级解析正确）/图片/📦真实标注/商品链接全部就位。demo 回归此前已通过，本轮仅改 web_scraper 搜索层不影响 Mock 链路。

**待办**：① 淘宝扫码登录 + selector 适配（同法）；② 卡片层无评价数据，口碑维度 0 分，精度提示语已带「深挖请发详情链接」；③ 临时诊断脚本 probe_jd.py 已删除。

**涉及文件**：`web_scraper.py`、本文档。

## 启动脚本重写（修复双击无响应 / cmd 报「此时不应有 >。」，全栈启动层）

> 创建于 2026-09-12 00:47

**需求来源**：开发者反馈双击 `.bat` 无响应，`cmd /c` 调用报 `此时不应有 >。`；后要求放弃修补、直接换更简单的启动方式。

**诊断过程**（多次实测复现）：

1. 原症状复现：原版网页版 bat 一执行即报 `此时不应有 >。` 并终止（解析期致命错误，pause 都执行不到，故双击表现为闪退）；
2. 纯 ASCII 最小复现不报错 → 排除 bat 逻辑本身，锁定编码问题：**bat 文件为 UTF-8，而中文 cmd 默认按 GBK 解析**，中文注释的 UTF-8 字节流在 GBK 下错位成对（lead+trail 吞掉后续 ASCII），行边界丢失、语法碎片化；
3. 试验「首行提前 chcp 65001」无效；改存 GBK 后经 `call` 传递仍碎裂（与测试管道叠加干扰）；
4. 另发现两个叠加 bug：`for /f ... in ('...')` 命令串内嵌 `sep='.'` 单引号破坏 for 定界；本机 `py` 启动器损坏（PEP-514 无注册，报 No installed Python found!），真机可用的是 `python` = `D:\app\Python\python.exe`。

**设计定稿**：放弃修补原脚本，按开发者指示重写为**纯 ASCII 极简启动脚本**——中文只允许出现在文件名，文件体禁用一切非 ASCII；Python 探测改为「先实测（`-c "import sys"`）再采用」的 py→python→python3 链。

**实施**（仅重写两个 bat，业务代码零改动）：

- `启动购物助手-网页版.bat`：探测链 → 启动 `web_server.py`（默认端口 8765）→ 失败时 pause + 指引日志位置
- `启动购物助手.bat`：同结构 → 启动 `shopping_agent.py`（透传 `%*`，`demo` 参数可用）
- `CLAUDE.md` 全局硬性规则新增「启动脚本编码红线」条目

**验证**：网页版经 GBK 中转真实调起——横幅、探测（自动落到 python）、服务启动全部正常，`GET /api/health` 返回 `{"ok": true}`，测毕已停服务；CLI 版与网页版同构，未做交互端到端（避免挂起），`demo` 回归待开发者双击自测。测试残留（临时 relay/副本/测试服务器）已全部清理。

**涉及文件**：`启动购物助手.bat`、`启动购物助手-网页版.bat`、`CLAUDE.md`、本文档。
