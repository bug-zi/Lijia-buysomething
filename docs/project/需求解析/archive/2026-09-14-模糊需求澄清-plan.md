# 模糊需求先澄清再检索 — 实施计划

> 创建于 2026-09-14。设计依据：同目录 `2026-09-14-模糊需求澄清-design.md`。

## T1 request_parser.py — 维度缺失判定

- 新增模块级 `_CATEGORY_ATTRS` 表（品类→问句+chips，含未知品类中性兜底）与归一化命中函数。
- `_parse_rules` 第10步重写：预算缺失判定解除「仅4品类」限制（有关键词/品类即判）；新增用途缺失、品类属性缺失问句，全部并入 `needs_clarify`。
- 新增 `RequestParser.missing_dims(req) -> List[dict]`（或等价方法）：返回缺失维度结构 `{dim, question, chips}`，供 shopping_agent 生成追问消息。规则独立可用，无 LLM 依赖。
- 自测：`python core/request_parser.py` 冒烟保持通过，补维度断言。

## T2 shopping_agent.py — 一轮式追问

- 4.3 节闸 B 替换：`missing_dims` ≥2 → `_start_dims_clarify(req)`（stage 3），模板按设计文档生成，`_last_chips` = 预算档+属性chips+「直接搜」。
- `_flow_clarify_answer` 增加 stage 3 分支：skip → 原需求直搜；否则 `merge(base, answer)` 重解析 → 直搜（一轮即止）。
- 缺1维（仅预算）维持「直搜+提示」；闸A品类问答不变。
- 自测：伪关键词不误搜等既有断言保持通过。

## T3 recommender.py + index.html — 评价区如实化

- `format_top`（~443-498 行）：标题改「买家好评/买家差评」；去掉 while 凑数；`_core_highlights` 独立「商品卖点」行；无评价数据/有评价无差评 如实话术；统计行仅 `review_count>0` 输出。
- index.html:2446 兜底文案「暂无明显差评」→ 如实表述。
- 自测：构造三态商品（有评价/无评价/有差评）断言渲染文案；无 emoji 断言。

## T4 验证与归档

- 断言脚本（tests 风格仿现有，全临时数据）：维度判定矩阵 / 一轮澄清流 / 门槛豁免 / 渲染三态。
- 真机冒烟清单写入 `docs/log/260914.md`（含创建时间行）。
- 问题疑惑区两条目归档进「已完成」区顶部（第4轮）。

## 不做（YAGNI）

- 不做 LLM 生成问法；不自动补抓详情页评价；不做逐轮硬追问；不动 stage 1/2 旧流程。
