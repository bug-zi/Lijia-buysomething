# 配置库（API Key + 网站登录态）实施计划（plan）

> 创建于 2026-09-12 02:35
> **Spec:** [2026-09-12-配置库-design.md](./2026-09-12-配置库-design.md)
> - 已归档：260912 随 配置库 落地（自测/demo 回归/接口冒烟/京东真实三连冒烟通过；扫码 E2E 由开发者验收，待办见 docs/log/260912.md 对应小节）
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **Git 纪律（覆盖技能默认）**：本项目所有 git 操作由开发者本人执行，AI 只在任务末尾以文字形式给出建议命令，绝不代跑。

**Goal:** 把 Tab 5「API Key 设置」升级为「配置库」：AI Key 区原样保留，新增京东/淘宝的登录态查看、手动扫码登录、按域清除，并把登录有效期解答写入界面。

**Architecture:** 新建 `config_store.py` 专管登录态元数据缓存（`login_state.json`，绝不存 Cookie 内容）与浏览器动作（复用 `web_scraper._launch_browser`/`_goto_login_and_wait`）；`web_server.py` 挂 `GET/POST /api/login`；`web_scraper.py` 在既有登录墙分支插两行 `record_state` 顺带保鲜；`index.html` Tab 5 下半区渲染状态卡。

**Tech Stack:** Python 标准库 + 可选依赖 Patchright/Playwright（经 web_scraper 兜底链）；前端单文件 index.html（原生 JS）。

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `config_store.py` | 新建 | 登录态缓存读写、检测、后台扫码登录、按域清除、并发锁 |
| `web_scraper.py` | 修改（2 处插入 + 1 行导入） | 抓取时顺带回写登录态（不改抓取主流程） |
| `web_server.py` | 修改（路由 + 1 个方法 + 导入 + docstring） | `/api/login` 接口 |
| `index.html` | 修改（Tab 5 区域 + JS） | 配置库界面：状态卡、按钮流转、有效期说明 |
| `.gitignore` | 修改 | 追加 `login_state.json` |
| `docs/log/260912.md` | 修改（AI 日志） | 本轮开发日志小节 |

## 循环依赖规避（重要）

`config_store` **不得**在模块顶层导入 `web_scraper`（`web_scraper` 要在顶层 `import config_store`）；前者只在 `check_login` / `_manual_login_worker` / `clear_login` 函数体内惰性导入。方向固定为：`web_scraper ──顶层──▶ config_store ──函数内惰性──▶ web_scraper`。

---

### T1 `config_store.py` — 配置库核心模块（新建）

**Files:**
- Create: `config_store.py`（项目根）

- [ ] **Step 1.1：写入完整模块代码**

```python
# -*- coding: utf-8 -*-
"""
配置库 · 网站登录态管理（状态缓存 / 手动检测 / 扫码登录 / 按域清除）

- 登录凭据（Cookie）始终由 web_scraper 的持久化配置目录 .browser_profile 管理；
  本模块只记录「状态元数据」（login_state.json），绝不存储任何 Cookie 内容。
- 检测/登录均用真实有头浏览器（复用 _launch_browser）；遇验证码/登录墙交还用户，
  不绕过任何平台风控；程序绝不过手账号密码（扫码登录只打开登录页等用户手动完成）。
- 本模块顶层不得导入 web_scraper（会被 web_scraper 顶层导入，构成循环）；
  浏览器相关函数体内惰性导入。
"""

import json
import os
import threading
import time
from typing import Any, Dict, List, Optional

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
LOGIN_STATE_FILE = os.path.join(BASE_DIR, "login_state.json")

PLATFORMS: List[str] = ["京东", "淘宝/天猫"]

# 检测页与清理域（键名须与 web_scraper.LOGIN_URLS 平台名一致）
CHECK_URLS: Dict[str, str] = {
    "京东": "https://order.jd.com/center/list.action",
    "淘宝/天猫": "https://www.taobao.com/",
}
CLEAR_DOMAINS: Dict[str, List[str]] = {
    "京东": ["jd.com"],
    "淘宝/天猫": ["taobao.com", "tmall.com"],
}

_lock = threading.Lock()
# 内存任务表：platform -> {"state": "检测中|登录中|清理中", "started_at": epoch}
_jobs: Dict[str, Dict[str, Any]] = {}


def _now_str(ts: Optional[float] = None) -> str:
    return time.strftime("%Y-%m-%d %H:%M:%S", time.localtime(ts if ts is not None else time.time()))


def _read_states() -> Dict[str, Dict[str, Any]]:
    """读取状态缓存文件（调用方需已持有 _lock，或接受自测场景的瞬时读）"""
    try:
        with open(LOGIN_STATE_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        return data if isinstance(data, dict) else {}
    except Exception:
        return {}


def record_state(platform: str, status: Optional[str], source: str, note: str = "") -> None:
    """写入状态；status=None 表示保留原状态、仅更新来源与备注"""
    with _lock:
        data = _read_states()
        old = data.get(platform) or {}
        data[platform] = {
            "status": status if status is not None else old.get("status", "未检测"),
            "checked_at": _now_str(),
            "source": source,
            "note": note,
        }
        try:
            with open(LOGIN_STATE_FILE, "w", encoding="utf-8") as f:
                json.dump(data, f, ensure_ascii=False, indent=2)
        except OSError:
            pass


def get_states() -> List[Dict[str, Any]]:
    """给前端展示：缓存状态 + 合并进行中任务（任务态优先）"""
    with _lock:
        cached = _read_states()
        states = []
        for p in PLATFORMS:
            job = _jobs.get(p)
            if job:
                states.append({
                    "platform": p, "status": job["state"],
                    "checked_at": _now_str(job.get("started_at")),
                    "source": "任务进行中", "note": "",
                })
            else:
                st = cached.get(p) or {}
                states.append({
                    "platform": p,
                    "status": st.get("status", "未检测"),
                    "checked_at": st.get("checked_at", ""),
                    "source": st.get("source", ""),
                    "note": st.get("note", ""),
                })
        return states


def _set_job(platform: str, state: str) -> None:
    with _lock:
        _jobs[platform] = {"state": state, "started_at": time.time()}


def _clear_job(platform: str) -> None:
    with _lock:
        _jobs.pop(platform, None)


def _close_browser(pw, context) -> None:
    try:
        if context is not None:
            context.close()
    except Exception:
        pass
    try:
        if pw is not None:
            pw.stop()
    except Exception:
        pass


def _detect_logged_in(platform: str, page) -> bool:
    """平台登录判定：与 _check_block 同思路（URL 域名/特征词），不绕风控"""
    low = (page.url or "").lower()
    if platform == "京东":
        return "passport" not in low and "login" not in low
    # 淘宝/天猫：unb cookie（用户编号）存在 且 页面无「亲，请登录」→ 已登录
    has_unb = False
    try:
        cookies = page.context.cookies("https://www.taobao.com")
        has_unb = any(c.get("name") == "unb" for c in cookies)
    except Exception:
        pass
    need_login_text = False
    try:
        body = page.inner_text("body")[:2000]
        need_login_text = "亲，请登录" in body
    except Exception:
        pass
    return has_unb and not need_login_text


def check_login(platform: str) -> Dict[str, Any]:
    """同步检测登录态：真实有头浏览器短暂打开检测页，判定后写缓存并关闭（≤25s）"""
    if platform not in CHECK_URLS:
        return {"ok": False, "message": f"暂不支持{platform}的登录态检测（如实告知）"}
    with _lock:
        if platform in _jobs:
            return {"ok": False, "message": "该平台的检测/登录任务正在进行中，请稍后再试"}
        _jobs[platform] = {"state": "检测中", "started_at": time.time()}
    try:
        try:
            from web_scraper import _launch_browser, _random_delay  # 惰性导入，避免循环依赖
        except Exception as e:
            record_state(platform, None, "检测", note=f"Playwright 未安装，无法检测：{e}")
            return {"ok": False, "message": "Playwright 未安装，无法检测（如实告知，不影响其他功能）"}
        pw = context = None
        try:
            pw, context, page = _launch_browser()   # 真实有头浏览器（红线）
            loaded = True
            try:
                page.goto(CHECK_URLS[platform], wait_until="domcontentloaded", timeout=20000)
            except Exception:
                loaded = False   # 加载失败多为风控静默拦截：不判定，如实告知
            _random_delay(1.0, 2.0)
            if not loaded:
                record_state(platform, None, "检测", note="检测页加载失败（疑似风控拦截），本次不判定")
                return {"ok": False, "message": f"{platform} 检测页加载失败，本次不判定（请稍后重试）"}
            status = "已登录" if _detect_logged_in(platform, page) else "未登录"
            record_state(platform, status, "手动检测")
            return {"ok": True, "status": status, "message": f"{platform}：{status}"}
        except Exception as e:
            record_state(platform, None, "检测", note=f"检测失败：{e}")
            return {"ok": False, "message": f"检测失败：{e}"}
        finally:
            _close_browser(pw, context)
    finally:
        _clear_job(platform)


def start_manual_login(platform: str) -> Dict[str, Any]:
    """弹出有头浏览器跳登录页等用户人工扫码（程序绝不过手凭据）；立即返回，前端轮询"""
    if platform not in CHECK_URLS:   # 平台范围与 LOGIN_URLS 一致
        return {"ok": False, "message": f"暂不支持{platform}的网页端登录（如实告知）"}
    with _lock:
        if platform in _jobs:
            return {"ok": False, "message": "该平台的检测/登录任务正在进行中，请稍后再试"}
        _jobs[platform] = {"state": "登录中", "started_at": time.time()}
    threading.Thread(target=_manual_login_worker, args=(platform,), daemon=True).start()
    return {"ok": True, "running": True,
            "message": f"已弹出 {platform} 登录窗口，请在浏览器里扫码/手动登录（最长等待 3 分钟）"}


def _manual_login_worker(platform: str) -> None:
    pw = context = None
    try:
        from web_scraper import _launch_browser, _goto_login_and_wait  # 惰性导入
        pw, context, page = _launch_browser()
        ok = _goto_login_and_wait(page, platform, max_wait_s=180)
        if ok:
            record_state(platform, "已登录", "扫码登录")
        else:
            record_state(platform, None, "扫码登录", note="本次登录未完成（超时或窗口被关闭），可重试")
    except Exception as e:
        record_state(platform, None, "扫码登录", note=f"登录任务异常：{e}")
    finally:
        _clear_job(platform)
        _close_browser(pw, context)


def clear_login(platform: str) -> Dict[str, Any]:
    """按平台域清除本项目 profile 内的 Cookie（不影响其他平台）；状态置回「未检测」"""
    if platform not in CLEAR_DOMAINS:
        return {"ok": False, "message": f"暂不支持{platform}的登录态清理（如实告知）"}
    with _lock:
        if platform in _jobs:
            return {"ok": False, "message": "该平台的检测/登录任务正在进行中，请稍后再试"}
        _jobs[platform] = {"state": "清理中", "started_at": time.time()}
    try:
        try:
            from web_scraper import _launch_browser  # 惰性导入
        except Exception as e:
            return {"ok": False, "message": f"Playwright 未安装，无法清理：{e}"}
        pw = context = None
        try:
            pw, context, page = _launch_browser()
            cookies = context.cookies()
            domains = CLEAR_DOMAINS[platform]
            victims = [c for c in cookies
                       if any((c.get("domain") or "").lstrip(".").endswith(d) for d in domains)]
            degraded = False
            for c in victims:
                try:
                    context.clear_cookies(name=c.get("name"), domain=c.get("domain"),
                                          path=c.get("path") or "/")
                except TypeError:
                    # 旧版 Playwright 不支持按 name/domain/path 过滤：如实降级为全清
                    context.clear_cookies()
                    degraded = True
                    break
            if degraded:
                record_state(platform, "未检测", "清除登录态",
                             note="旧版 Playwright 不支持按域过滤，已清除全部站点登录态（如实告知）")
                return {"ok": True, "message": "已清除全部站点登录态（当前 Playwright 版本不支持按域过滤，如实告知）"}
            record_state(platform, "未检测", "清除登录态",
                         note=f"已清除 {len(victims)} 条 Cookie（仅 {platform} 相关域）")
            return {"ok": True, "message": f"已清除 {platform} 登录态（{len(victims)} 条 Cookie），重新检测将显示未登录"}
        except Exception as e:
            record_state(platform, None, "清除登录态", note=f"清理失败：{e}")
            return {"ok": False, "message": f"清理失败：{e}"}
        finally:
            _close_browser(pw, context)
    finally:
        _clear_job(platform)


if __name__ == "__main__":
    # 函数级自测（无需浏览器）：缓存读写 / 状态合并 / 任务互斥 / 无敏感字段
    import tempfile
    LOGIN_STATE_FILE = os.path.join(tempfile.gettempdir(), "_login_state_selftest.json")
    try:
        os.remove(LOGIN_STATE_FILE)
    except OSError:
        pass

    # 1) 初始状态：全部「未检测」
    sts = {s["platform"]: s["status"] for s in get_states()}
    assert sts == {"京东": "未检测", "淘宝/天猫": "未检测"}, sts

    # 2) 写入与保留原状态
    record_state("京东", "已登录", "自测")
    record_state("淘宝/天猫", None, "自测", note="仅更新备注")
    sts = {s["platform"]: (s["status"], s["source"], s["note"]) for s in get_states()}
    assert sts["京东"] == ("已登录", "自测", ""), sts["京东"]
    assert sts["淘宝/天猫"][0] == "未检测" and sts["淘宝/天猫"][2] == "仅更新备注", sts["淘宝/天猫"]

    # 3) 进行中任务合并与互斥
    _set_job("淘宝/天猫", "登录中")
    busy = [s["status"] for s in get_states()]
    assert busy == ["已登录", "登录中"], busy
    r = start_manual_login("淘宝/天猫")
    assert r["ok"] is False, r
    r = start_manual_login("拼多多")
    assert r["ok"] is False, r
    _clear_job("淘宝/天猫")

    # 4) 缓存文件不得含任何敏感字段
    with open(LOGIN_STATE_FILE, "r", encoding="utf-8") as f:
        raw = f.read().lower()
    for bad in ("cookie", "token", "password", "pt_key", "unb"):
        assert bad not in raw, f"敏感字段泄漏：{bad}"

    os.remove(LOGIN_STATE_FILE)
    print("✅ config_store 自测通过（缓存读写 / 状态合并 / 任务互斥 / 无敏感字段）")
```

- [ ] **Step 1.2：运行自测，确认通过**

Run: `python config_store.py`
Expected: 输出 `✅ config_store 自测通过（缓存读写 / 状态合并 / 任务互斥 / 无敏感字段）`，无 AssertionError。

- [ ] **Step 1.3：提交检查点（开发者本人执行，AI 仅建议命令）**

```bash
git add config_store.py
git commit -m "配置库：新增 config_store 登录态管理模块（缓存/检测/扫码登录/按域清除）"
```

---

### T2 `web_scraper.py` — 抓取时顺带回写登录态（最小侵入）

**Files:**
- Modify: `web_scraper.py:23`（导入区）、`web_scraper.py:813-823`（登录墙分支）

- [ ] **Step 2.1：顶层导入 config_store**

在 `from product_searcher import Product, Review`（第 23 行）之后新增一行：

```python
import config_store  # noqa: E402  配置库：抓取时顺带回写登录态
```

（config_store 顶层仅用标准库，无循环依赖；方向见计划头部「循环依赖规避」。）

- [ ] **Step 2.2：登录成功分支插入回写**

`search_platform` 内，原代码（约 816-818 行）：

```python
                ok = _goto_login_and_wait(page, platform)
                if ok:
                    continue
```

改为：

```python
                ok = _goto_login_and_wait(page, platform)
                if ok:
                    config_store.record_state(platform, "已登录", "抓取扫码")
                    continue
```

- [ ] **Step 2.3：登录墙未通过分支插入回写**

原代码（约 824-827 行）：

```python
            if is_login:
                result["block_reason"] = (
                    f"🛑 {platform} 搜索仍需登录（本次扫码未成功或已超时）。"
                    "回复「继续抓取」可重试登录；登录一次后短期内无需重复扫码。")
```

改为（在赋值前插一行）：

```python
            if is_login:
                config_store.record_state(platform, "未登录", "抓取时")
                result["block_reason"] = (
                    f"🛑 {platform} 搜索仍需登录（本次扫码未成功或已超时）。"
                    "回复「继续抓取」可重试登录；登录一次后短期内无需重复扫码。")
```

- [ ] **Step 2.4：语法与导入自检**

Run: `python -c "import web_scraper; print('import ok')"`
Expected: `import ok`（无 ImportError / 循环导入报错）。

- [ ] **Step 2.5：提交检查点（开发者本人执行）**

```bash
git add web_scraper.py
git commit -m "抓取登录墙分支顺带回写登录态到配置库"
```

---

### T3 `web_server.py` — `/api/login` 接口

**Files:**
- Modify: `web_server.py:6-16`（docstring）、`web_server.py:33`（导入区）、`web_server.py:113-115`（GET 路由）、`web_server.py:146-148`（POST 路由）、`_api_apikey_post` 之后（新方法）

- [ ] **Step 3.1：docstring API 清单追加**

在第 14 行 `POST /api/cancel ...` 之后插入两行：

```
    GET  /api/login            网站登录态列表（配置库）
    POST /api/login            body: {"action":"check|login|clear","platform":"京东|淘宝/天猫"}
```

- [ ] **Step 3.2：导入 config_store**

在第 33 行 `from product_searcher import save_scrape_cache` 之后新增：

```python
import config_store  # noqa: E402  配置库：网站登录态管理
```

- [ ] **Step 3.3：GET 路由**

在 `do_GET` 的 `/api/apikey` 块（113-115 行）之后插入：

```python
        if path == "/api/login":
            json_response(self, 200, {"ok": True, "states": config_store.get_states()})
            return
```

- [ ] **Step 3.4：POST 路由**

在 `do_POST` 的 `/api/apikey` 块（146-148 行）之后插入：

```python
        if path == "/api/login":
            self._api_login_post(body)
            return
```

- [ ] **Step 3.5：新增 `_api_login_post` 方法**

在 `_api_apikey_post` 方法结束后新增：

```python
    def _api_login_post(self, body: dict):
        """配置库 · 网站登录态：check=同步检测(≤25s)；login=后台扫码(前端轮询)；clear=按域清理"""
        action = str(body.get("action") or "").strip().lower()
        platform = str(body.get("platform") or "").strip()
        if not platform:
            json_response(self, 400, {"ok": False, "message": "platform 不能为空",
                                      "states": config_store.get_states()})
            return
        if action == "check":
            r = config_store.check_login(platform)
        elif action == "login":
            r = config_store.start_manual_login(platform)
        elif action == "clear":
            r = config_store.clear_login(platform)
        else:
            json_response(self, 400, {"ok": False, "message": "未知 action，仅支持 check/login/clear"})
            return
        json_response(self, 200, dict(r, states=config_store.get_states()))
```

- [ ] **Step 3.6：接口冒烟（不需要浏览器）**

启动服务：`python web_server.py`（后台），另开终端：

Run: `curl -s http://127.0.0.1:8765/api/login`
Expected: `{"ok": true, "states": [{"platform": "京东", "status": "未检测", ...}, {"platform": "淘宝/天猫", ...}]}`

Run: `curl -s -X POST http://127.0.0.1:8765/api/login -d "{\"action\":\"bad\",\"platform\":\"京东\"}"`
Expected: 400 `未知 action`。

- [ ] **Step 3.7：提交检查点（开发者本人执行）**

```bash
git add web_server.py
git commit -m "web_server 新增 /api/login 登录态接口（check/login/clear）"
```

---

### T4 `index.html` — Tab 5 升级为「配置库」

**Files:**
- Modify: `index.html`（`</head>` 前、侧栏 593-596、Tab 5 区 741-812、`switchTab` 848-859、JS 1228 行后）

- [ ] **Step 4.1：`</head>` 前引入 Material Symbols（无 emoji 图标红线）**

```html
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=block">
<style>.material-symbols-outlined{font-size:20px;line-height:1;vertical-align:middle;}</style>
```

- [ ] **Step 4.2：侧栏入口改名换图标**

原（593-596 行）：

```html
      <div class="nav-item" data-tab="tab-api"     onclick="switchTab('tab-api')">
        <span class="ico">🔑</span><span class="label">API Key 设置</span>
        <span class="badge" id="llm-badge" style="display:none"></span>
      </div>
```

改为：

```html
      <div class="nav-item" data-tab="tab-api"     onclick="switchTab('tab-api')">
        <span class="ico"><span class="material-symbols-outlined">settings</span></span><span class="label">配置库</span>
        <span class="badge" id="llm-badge" style="display:none"></span>
      </div>
```

- [ ] **Step 4.3：Tab 5 标题改「配置库」**

原（741-745 行）：

```html
    <!-- Tab 5: API Key 设置 -->
    <section class="tab-panel" id="tab-api">
      <div class="card">
        <div class="card-title">
          <span>🔑 AI 能力 · API Key 设置</span>
```

改为（sub 行保持原文不动）：

```html
    <!-- Tab 5: 配置库（AI 能力 + 网站登录态） -->
    <section class="tab-panel" id="tab-api">
      <div class="card">
        <div class="card-title">
          <span>配置库 · AI 能力与网站登录态</span>
```

- [ ] **Step 4.4：Tab 5 底部插入「网站登录态」区**

在「安全说明」`</ul>` 之后、`</div></section>`（811-812 行）之前插入：

```html
        <hr style="border:none;border-top:1px dashed var(--border);margin:18px 0;">
        <div class="card-title">
          <span>网站登录态 · 常用购物平台</span>
          <span class="sub">登录/检测由你在弹出的真实浏览器里人工完成，程序绝不过手账号密码；会话保存在本项目 .browser_profile，重启不丢。</span>
        </div>
        <div id="login-states" style="display:grid;gap:12px;"></div>
        <div class="help" style="margin-top:14px;">
          <b>登录有效期说明：</b>有效期各平台不一且不承诺固定值——一般几天到几个月（京东 PC 会话常见约 30 天，淘宝定期使用可维持更久）；
          改密码、异地登录、平台风控都可能随时提前失效。<b>没有真正永不过期的方法</b>：本地持久化 + 低频正常使用就是最接近的做法，
          失效后在这里点「扫码登录」重新授权即可。本系统不支持从别处导入 Cookie：复制会话凭据极易触发风控，且程序绝不过手账号凭据。
        </div>
```

- [ ] **Step 4.5：`switchTab` 标题与挂载点**

原（853、859 行）：

```js
    "tab-api":     "🔑 AI API Key 设置 · 自动探测服务商 + 安全保存",
```

改为：

```js
    "tab-api":     "配置库 · AI 能力 + 网站登录态",
```

原：

```js
  if (id === "tab-api")     loadApiKeyStatus();
```

改为：

```js
  if (id === "tab-api")     { loadApiKeyStatus(); loadLoginStates(); }
```

- [ ] **Step 4.6：追加登录态 JS**

在 `apiKeyAction` 函数结束（1228 行）之后、「Tab 2: 档案」注释之前插入：

```js
/* =============== 配置库 · 网站登录态 =============== */
const LOGIN_BUSY = new Set(["检测中", "登录中", "清理中"]);
let _loginPollTimer = null;

function loginStatusPill(status){
  const cls = status === "已登录" ? "green" : "warn";
  return `<span class="tag-pill ${cls}"><span class="dot"></span>${status}</span>`;
}

function renderLoginStates(states){
  const box = $("#login-states");
  if(!box) return;
  box.innerHTML = states.map(s => {
    const busy = LOGIN_BUSY.has(s.status);
    const btns = busy
      ? `<button class="btn" disabled>${s.status}，请稍候…</button>`
      : `<button class="btn" onclick="loginAction('check','${s.platform}')">立即检测</button>
         <button class="btn" onclick="loginAction('login','${s.platform}')">扫码登录</button>` +
        (s.status === "已登录"
          ? `<button class="btn danger" onclick="loginAction('clear','${s.platform}')">清除登录态</button>` : "");
    const meta = [s.checked_at, s.source].filter(Boolean).join(" · ") || "从未检测";
    return `
      <div style="background:var(--bg);border:1px dashed var(--border);border-radius:12px;padding:12px 14px;display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;">
        <div>
          <b>${s.platform}</b> ${loginStatusPill(s.status)}
          <div class="help" style="margin-top:4px;">${meta}${s.note ? " · " + s.note : ""}</div>
        </div>
        <div style="display:flex;gap:8px;flex-wrap:wrap;">${btns}</div>
      </div>`;
  }).join("");
}

async function loadLoginStates(){
  const r = await api("/api/login","GET");
  if(r && r.ok) renderLoginStates(r.states || []);
  const busy = ((r && r.states) || []).some(s => LOGIN_BUSY.has(s.status));
  if(busy && !_loginPollTimer){
    _loginPollTimer = setInterval(async () => {
      const rr = await api("/api/login","GET");
      if(!(rr && rr.ok)) return;
      renderLoginStates(rr.states || []);
      if(!((rr.states || []).some(s => LOGIN_BUSY.has(s.status)))){
        clearInterval(_loginPollTimer); _loginPollTimer = null;
        toast("登录态任务已结束","ok");
      }
    }, 3000);
  }
}

async function loginAction(action, platform){
  if(action === "clear" && !confirm(`确认清除 ${platform} 的登录态吗？清除后需要重新扫码登录。`)) return;
  toast(action === "check" ? `正在打开浏览器检测 ${platform} 登录态（约 5-25 秒）…`
      : action === "login" ? "正在弹出浏览器，请在窗口内扫码/手动登录（最长等 3 分钟）…"
      : `正在清除 ${platform} 登录态…`);
  const r = await api("/api/login","POST",{action, platform});
  if(!r){ toast("请求失败","err"); return; }
  if(!r.ok){ toast(r.message || "操作失败","warn"); }
  else if(action === "check"){ toast(r.message || `${platform}：${r.status}`,"ok"); }
  renderLoginStates(r.states || []);
  loadLoginStates();   // 若进入 登录中/清理中，自动启动轮询
}
```

- [ ] **Step 4.7：提交检查点（开发者本人执行）**

```bash
git add index.html
git commit -m "前端 Tab 5 升级为配置库：网站登录态管理区 + 有效期说明"
```

---

### T5 `.gitignore` — 追加登录态缓存

**Files:**
- Modify: `.gitignore`

- [ ] **Step 5.1：文件末尾追加一行**

```
login_state.json
```

- [ ] **Step 5.2：提交检查点（开发者本人执行）**

```bash
git add .gitignore
git commit -m "gitignore 追加 login_state.json"
```

---

### T6 验证回归 + 日志 + 归档

**Files:**
- Modify: `docs/log/260912.md`、`docs/project/优化建议区.md`、本 spec/plan 移入 `archive/`

- [ ] **Step 6.1：模块自测**

Run: `python config_store.py` → Expected: 自测通过输出（同 T1）。

- [ ] **Step 6.2：demo 全量回归（Mock 链路不受影响红线）**

Run: `python shopping_agent.py demo`
Expected: 全部通过，无因 config_store 引入的报错。

- [ ] **Step 6.3：真实冒烟（有浏览器环境）**

1. 启动 `python web_server.py`，浏览器打开 `http://127.0.0.1:8765/`，进「配置库」：AI Key 区功能回归（显示状态、测试连通性）；登录态两卡显示「未检测」。
2. 对未登录平台点「立即检测」→ 约 5-25 秒返回「未登录」（若平台已登录则显示「已登录」，如实记录）。
3. 点「扫码登录」→ 有头浏览器弹出到登录页；不扫码等超时 → 卡片恢复且 note 提示「本次登录未完成」；再点并人工扫码 → 轮询结束变「已登录」。
4. 点「清除登录态」→ 再「立即检测」显示「未登录」；另一平台状态不受影响（按域清理验证）。
5. 对话购物发起一次真实搜索触发登录墙→登录→「继续抓取」后，配置库自动变「已登录」（抓取顺带回写验证）。
6. 检查项目根 `login_state.json` 内容：只有 platform/status/checked_at/source/note，无任何 Cookie。

- [ ] **Step 6.4：Playwright 缺失场景（可选依赖红线）**

临时改配置_store 无法模拟时，最小验证：`python -c "import config_store; print(config_store.check_login('京东'))"` 在 Playwright 正常机器应进入浏览器流程（人工确认弹窗），而 `start_manual_login('拼多多')` 应返回不支持（已在自测覆盖）。若需完整缺失场景：临时重命名 playwright 目录后启动服务，确认 `/api/login` check 返回「Playwright 未安装」如实文案且服务不崩。

- [ ] **Step 6.5：开发日志（AI 维护）**

`docs/log/260912.md` 新增小节（**小节标题下必须附创建时间行**，用 `date "+%Y-%m-%d %H:%M"` 取实际时间），内容含：配置库上线（方案 A 选型）、config_store 职责、检测/登录/清除三动作、抓取顺带回写、登录有效期解答正本、验证结果。

- [ ] **Step 6.6：归档（AI 执行文件移动）**

1. `2026-09-12-配置库-design.md` 与本 plan 移入 `docs/project/全局/archive/`（按 README.md 机制，落地后归档）；
2. `docs/project/优化建议区.md`：把「待完成」条目移入归档区**顶部**，新增「### 第 1 轮（260912）：配置库」小节，保留原始描述并附完成说明（配置库已上线：Tab 5 升级，登录态可查看/扫码/清除）；
3. git 提交由开发者执行（建议：`git add docs/ config_store.py web_scraper.py web_server.py index.html .gitignore` 后提交）。

---

## 风险与对策

| 风险 | 对策 |
|---|---|
| 循环导入（config_store ↔ web_scraper） | 固定方向：web_scraper 顶层导入 config_store；config_store 仅函数体内惰性导入（T1/T2 已编码约束） |
| 检测页被风控静默拦截（永不加载完成） | goto 超时即「不判定」如实返回，绝不把空白页误判为已登录 |
| 淘宝 `unb` 特征随改版失效 | 双特征（cookie + 「亲，请登录」文案）；失效表现为「未登录」误报，可重扫码，不损坏数据 |
| 旧版 Playwright 无按域 clear_cookies | TypeError 捕获 → 如实降级全清并显著告知 |
| 配置库操作与抓取并发抢 profile | `_jobs` 任务互斥 + `_launch_browser` 既有三段重试 + 「请稍后再试」文案 |
| Material Symbols 需联网加载 | 仅影响侧栏一个图标字体（离线显示图标名文本），功能不受影响 |
| demo/Mock 链路回归破坏 | 新流程独立模块与接口，不改 Mock 路径；T6.2 全量回归验证 |

## 验收清单（对应 spec 验收标准 7 条）

- [ ] 配置库 Tab 渲染正常，AI Key 区 set/test/clear 回归全通（验收 1）
- [ ] 立即检测：未登录/已登录/超时三态正确（验收 2）
- [ ] 扫码登录：弹窗→扫码→自动写「已登录」；失败/关窗保持原状态且轮询正常结束（验收 3）
- [ ] 清除登录态：按域清理后检测为「未登录」，另一平台不受影响（验收 4）
- [ ] 抓取顺带更新：登录墙→登录→继续抓取后自动「已登录」（验收 5）
- [ ] `login_state.json` 无任何 Cookie/token 内容（验收 6，自测断言 + 人工复核）
- [ ] Playwright 缺失场景如实报错不崩溃，AI Key 功能不受影响（验收 7）
