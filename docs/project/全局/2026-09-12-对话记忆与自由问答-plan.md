# 对话记忆 + 自由问答链路 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **本项目纪律（覆盖默认流程）：**
> - 所有 git 操作由开发者本人执行；每个任务末尾的「建议提交点」仅以文字给出命令，AI 绝不执行 git。
> - 测试用一次性断言脚本放 `temp/`，用 `python`（不是 `py`，本机 py 启动器损坏）运行；**绝不触发真实搜索链路**（不调 search_real / 不弹浏览器）。
> - 后端纯标准库；LLM 失败必须静默回退，功能永不中断。
> - spec：`docs/project/全局/2026-09-12-对话记忆与自由问答-design.md`（实施前先读）。

**Goal:** LLM 需求解析注入最近对话窗口（口语化增量可解析）+ 新增基于会话材料的自由问答兜底链路 + 条数 schema 补全与润色 prompt 动态化。

**Architecture:** ChatSession 内存维护 8 条滑动窗口 `_history` 随会话态落盘；解析增强走 ai_client 既有函数加 history 参数；自由问答在 chat() 路由 4.2 与 4.3 之间插入单次 LLM 兜底调用（可答则答，NEED_SEARCH/失败静默落回原流程）。

**Tech Stack:** Python 标准库（urllib/json/re/dataclasses），无 pytest——用 `python temp/test_*.py` 断言脚本 + monkeypatch 模块全局函数。

---

## 约定（所有任务通用）

- 工作目录 = 项目根 `D:\Code\举一反三\李佳Agent\buy something`。
- 测试脚本头部统一：
```python
# -*- coding: utf-8 -*-
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))  # 项目根（shopping_agent 需要）
sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "core"))
try: sys.stdout.reconfigure(encoding="utf-8")
except Exception: pass
```
- 运行方式：`python temp/<脚本名>.py`，期望输出以 `ALL PASS` 结尾。
- LLM 屏蔽手法（纯规则测试用）：`import request_parser as rp; rp.parse_shopping_request_with_llm = None`；配置屏蔽（问答兜底测试用）：`import ai_client; ai_client._CFG.api_key = ai_client._CFG.base_url = ai_client._CFG.model = ""`。

---

### Task 1: 润色 prompt 动态化（TOP3/500字 → 按实际条数）

**Files:**
- Modify: `core/ai_client.py:454-485`（`polish_recommendation_with_llm`）
- Test: `temp/test_t1_polish.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t1_polish.py —— 头部按「约定」补齐 sys.path 与 stdout 设置
import ai_client

captured = {}
def fake_chat(messages, **kw):
    captured["sys"] = messages[0]["content"]
    captured["user"] = messages[1]["content"]
    return "好的，点评如下"

ai_client.chat_completion = fake_chat

# 3 条 → 应写 TOP3、500 字
ai_client.polish_recommendation_with_llm(
    [{"rank": i, "name": f"商品{i}"} for i in (1, 2, 3)], {}, persona="default")
assert "TOP3" in captured["sys"], captured["sys"]
assert "500" in captured["sys"], captured["sys"]
assert "[TOP3 推荐列表]" in captured["user"], captured["user"]

# 8 条 → 应写 TOP8、字数 500+(8-5)*100=800
ai_client.polish_recommendation_with_llm(
    [{"rank": i, "name": f"商品{i}"} for i in range(1, 9)], {}, persona="default")
assert "TOP8" in captured["sys"], captured["sys"]
assert "800" in captured["sys"], captured["sys"]
assert "TOP3" not in captured["sys"], captured["sys"]  # 不再写死

print("ALL PASS")
```

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t1_polish.py`
Expected: AssertionError（当前 sys_prompt 写死 "TOP3"，8 条时仍含 TOP3、且无 800）

- [ ] **Step 3: 实施**

`core/ai_client.py` 的 `polish_recommendation_with_llm` 中，将：

```python
    style = _PERSONA_STYLES.get(persona or "", "")
    sys_prompt = """你是贴心但客观的购物顾问，语气简洁，不吹捧商品。
输入为 TOP3 推荐对象列表和用户个人档案。
请输出一段中文 Markdown 报告（不超过500字）："""
```

改为：

```python
    style = _PERSONA_STYLES.get(persona or "", "")
    n_items = len(items)
    word_cap = min(1000, 500 + max(0, n_items - 5) * 100)  # ≤5款500字起，每多1款+100字，封顶1000
    sys_prompt = f"""你是贴心但客观的购物顾问，语气简洁，不吹捧商品。
输入为 TOP{n_items} 推荐对象列表和用户个人档案。
请输出一段中文 Markdown 报告（不超过{word_cap}字）："""
```

同函数尾部 user_prompt 中：

```python
        f"[TOP3 推荐列表]\n{json.dumps(items, ensure_ascii=False, indent=2)}\n"
```

改为：

```python
        f"[TOP{n_items} 推荐列表]\n{json.dumps(items, ensure_ascii=False, indent=2)}\n"
```

（docstring 里「推荐TOP3对象」一并改为「推荐TOP-N对象」。）

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t1_polish.py`
Expected: `ALL PASS`

- [ ] **Step 5: 建议提交点（开发者执行）**

```
git add core/ai_client.py temp/test_t1_polish.py
git commit -m "润色prompt动态化：TOP-N与字数上限按推荐条数生成，替换写死TOP3/500字"
```

---

### Task 2: 需求解析 LLM 增强——history 参数 + 条数字段入 schema

**Files:**
- Modify: `core/ai_client.py:387-419`（`parse_shopping_request_with_llm`）
- Test: `temp/test_t2_parse_llm.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t2_parse_llm.py —— 头部按「约定」补齐
import json
import ai_client

captured = {}
def fake_chat(messages, **kw):
    captured["sys"] = messages[0]["content"]
    captured["user"] = messages[1]["content"]
    return json.dumps({"query": "连衣裙", "top_n": 5, "per_platform_n": 4}, ensure_ascii=False)

ai_client.chat_completion = fake_chat

# 1) history 注入：最近对话块出现、当前输入不重复出现在历史块
r = ai_client.parse_shopping_request_with_llm(
    "再要2个", {}, None,
    history=[{"role": "user", "text": "上淘宝和京东各筛前4名连衣裙，最终排名前5的商品"},
             {"role": "ai", "text": "为你选出TOP5……"}])
assert r["top_n"] == 5 and r["per_platform_n"] == 4
assert "[最近对话]" in captured["user"], captured["user"]
assert "用户：上淘宝和京东各筛前4名连衣裙" in captured["user"]
assert "助手：为你选出TOP5" in captured["user"]
assert "[用户当前输入]\n再要2个" in captured["user"]

# 2) schema 含条数字段说明
assert "top_n" in captured["sys"] and "per_platform_n" in captured["sys"]
# 3) 指代/增量提示语存在
assert "指代" in captured["sys"]
# 4) history=None 时不输出最近对话块
ai_client.parse_shopping_request_with_llm("买连衣裙", {}, None, history=None)
assert "[最近对话]" not in captured["user"]
# 5) history 超长截断：单条 >100 字被截到 100+省略号
long_text = "长" * 300
ai_client.parse_shopping_request_with_llm("再要2个", {}, None,
                                           history=[{"role": "user", "text": long_text}])
line = [l for l in captured["user"].splitlines() if l.startswith("用户：长")][0]
assert len(line) <= len("用户：") + 101, line

print("ALL PASS")
```

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t2_parse_llm.py`
Expected: TypeError（`parse_shopping_request_with_llm() got an unexpected keyword argument 'history'`）

- [ ] **Step 3: 实施**

`core/ai_client.py` 的 `parse_shopping_request_with_llm`：

3.1 签名（第 387 行）改为：

```python
def parse_shopping_request_with_llm(user_text: str, profile: Dict[str, Any],
                                    prev: Optional[Dict[str, Any]],
                                    history: Optional[List[Dict[str, str]]] = None) -> Optional[Dict[str, Any]]:
```

3.2 sys_prompt 中，`target_rank` 行之后、`intent` 行之前插入两行：

```
  top_n: number|null       用户指定的最终输出条数（如"排名前5"填5；没提则null）
  per_platform_n: number|null 用户指定的每平台候选条数（如"各筛前4名"填4；没提则null）
```

sys_prompt 末段（「所有 string 必须为中文简洁表述」之前）插入一行：

```
结合[最近对话]理解指代与增量表达（如"再要2个""换成京东的""第二种呢"）；条数要求填入 top_n/per_platform_n。
```

3.3 user_prompt 构造（第 409-413 行）改为：

```python
    hist_block = ""
    if history:
        lines = []
        for m in history[-8:]:
            role = "用户" if m.get("role") == "user" else "助手"
            t = str(m.get("text") or "")[:100]
            lines.append(f"{role}：{t}")
        if lines:
            hist_block = "[最近对话]\n" + "\n".join(lines) + "\n\n"
    user_prompt = (
        f"[已保存的用户个人档案]\n{json.dumps(profile, ensure_ascii=False)}\n\n"
        f"[上一轮的需求参考（若is_adjustment为true则增量叠加，否则忽略）]\n{json.dumps(prev or {}, ensure_ascii=False)}\n\n"
        f"{hist_block}"
        f"[用户当前输入]\n{user_text}\n\n请输出JSON："
    )
```

（调用 `chat_completion(...)` 一行不动。）

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t2_parse_llm.py`
Expected: `ALL PASS`

- [ ] **Step 5: 建议提交点（开发者执行）**

```
git add core/ai_client.py temp/test_t2_parse_llm.py
git commit -m "需求解析LLM增强：注入最近对话窗口+schema补top_n/per_platform_n字段"
```

---

### Task 3: 自由问答 LLM 能力 answer_free_question_with_llm

**Files:**
- Modify: `core/ai_client.py`（`parse_shopping_request_with_llm` 之后、`summarize_reviews_with_llm` 之前新增函数与常量）
- Test: `temp/test_t3_qa.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t3_qa.py —— 头部按「约定」补齐
import ai_client

# 1) 正常回答：透传材料与问题
def fake_ok(messages, **kw):
    assert "只基于" in messages[0]["content"] or "材料" in messages[0]["content"]
    assert "第2款" in messages[1]["content"]
    return "第2款综合分更高，更值得考虑。"
ai_client.chat_completion = fake_ok
r = ai_client.answer_free_question_with_llm(
    "第2款和第4款哪个更耐穿？",
    {"last_recommendation": [{"rank": 2, "name": "A"}], "recent_chat": [], "profile": {}})
assert r == "第2款综合分更高，更值得考虑。", r

# 2) NEED_SEARCH 透传
ai_client.chat_completion = lambda m, **kw: "NEED_SEARCH"
r = ai_client.answer_free_question_with_llm("随便搜点便宜的", {"recent_chat": []})
assert r == "NEED_SEARCH", r

# 3) LLM 失败 → None
def fake_fail(messages, **kw):
    return None
ai_client.chat_completion = fake_fail
r = ai_client.answer_free_question_with_llm("哪个好？", {"recent_chat": []})
assert r is None

# 4) 空问题 → None（不发起调用）
def fake_boom(messages, **kw):
    raise AssertionError("不应发起调用")
ai_client.chat_completion = fake_boom
assert ai_client.answer_free_question_with_llm("", {"recent_chat": []}) is None

print("ALL PASS")
```

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t3_qa.py`
Expected: `AttributeError: module 'ai_client' has no attribute 'answer_free_question_with_llm'`

- [ ] **Step 3: 实施**

`core/ai_client.py` 在 `summarize_reviews_with_llm` 之前新增：

```python
def answer_free_question_with_llm(question: str, materials: Dict[str, Any]) -> Optional[str]:
    """自由问答：基于会话材料（上次推荐/最近对话/档案摘要）回答用户追问。
    返回回答文本；需新搜索或与材料无关时返回 "NEED_SEARCH"；失败返回 None（上层回退规则流程）。"""
    if not question or not question.strip() or not isinstance(materials, dict):
        return None
    sys_prompt = (
        "你是购物助手的对话答疑模块。用户在当前会话中追问，你只能依据【材料】回答。\n"
        "红线：\n"
        "1) 材料里没有的事实（历史价格/库存/真伪/未出现过的商品）如实回答「材料中没有，无法判断」，"
        "绝不编造商品、价格、评价；\n"
        "2) 「买哪个好」类问题：基于评分材料给建议并说明理由，结尾提醒一句下单前核对尺码/价格；\n"
        "3) 回答用简洁中文 Markdown，不超过300字。\n"
        "输出约定：若用户是在要求搜索/推荐新商品，或问题与本会话材料完全无关，只输出一行：NEED_SEARCH"
    )
    user_prompt = (
        f"[材料]\n{json.dumps(materials, ensure_ascii=False, indent=2)}\n\n"
        f"[用户问题]\n{question.strip()}\n\n请回答："
    )
    return chat_completion([
        {"role": "system", "content": sys_prompt},
        {"role": "user", "content": user_prompt},
    ], temperature=0.4, max_tokens=500, timeout=10)
```

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t3_qa.py`
Expected: `ALL PASS`

- [ ] **Step 5: 建议提交点（开发者执行）**

```
git add core/ai_client.py temp/test_t3_qa.py
git commit -m "新增自由问答LLM能力：基于会话材料答疑，NEED_SEARCH约定+失败返回None"
```

---

### Task 4: request_parser——history 透传 + 条数合并（规则优先）

**Files:**
- Modify: `core/request_parser.py:76-95`（`parse` 签名与 LLM 调用）、`core/request_parser.py:540-545`（`_merge_llm_json` 增量合并区）
- Test: `temp/test_t4_parser.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t4_parser.py —— 头部按「约定」补齐
import request_parser as rp
from request_parser import RequestParser

# 1) 纯规则回归：开发者原话（屏蔽 LLM）
rp.parse_shopping_request_with_llm = None
r = RequestParser().parse("上淘宝和京东各筛选出最合适的前4名商品，然后在放在一块综合比较，最终给我排名前5的商品")
assert r.top_n == 5 and r.per_platform_n == 4, (r.top_n, r.per_platform_n)

# 2) 规则优先：规则已解析到 top_n=5，LLM 给 9 不覆盖
rp.parse_shopping_request_with_llm = lambda text, profile, prev, history=None: {"top_n": 9, "per_platform_n": 8}
r = RequestParser().parse("各筛前4名，最终排名前5")
assert r.top_n == 5 and r.per_platform_n == 4, (r.top_n, r.per_platform_n)

# 3) 规则缺失时采纳 LLM 值并 clamp 1..10
rp.parse_shopping_request_with_llm = lambda text, profile, prev, history=None: {"top_n": 99, "per_platform_n": 4}
r = RequestParser().parse("来点连衣裙")   # 规则解析不到条数
assert r.top_n == 10, r.top_n            # 99 → clamp 10
assert r.per_platform_n == 4

# 4) LLM 无条数字段 → 保持 None
rp.parse_shopping_request_with_llm = lambda text, profile, prev, history=None: {"query": "连衣裙"}
r = RequestParser().parse("来点连衣裙")
assert r.top_n is None and r.per_platform_n is None

# 5) history 透传到 LLM 函数
seen = {}
def spy(text, profile, prev, history=None):
    seen["history"] = history
    return None
rp.parse_shopping_request_with_llm = spy
hist = [{"role": "user", "text": "买连衣裙"}]
RequestParser().parse("再要2个", history=hist)
assert seen["history"] is hist

# 6) LLM 抛异常 → 静默回退（不 crash、top_n 保持规则值）
def boom(text, profile, prev, history=None):
    raise RuntimeError("llm down")
rp.parse_shopping_request_with_llm = boom
r = RequestParser().parse("各筛前4名，最终排名前5")
assert r.top_n == 5 and r.per_platform_n == 4

print("ALL PASS")
```

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t4_parser.py`
Expected: 用例 5 报 `TypeError: parse() got an unexpected keyword argument 'history'`

- [ ] **Step 3: 实施**

3.1 `parse` 签名（第 76-77 行）加 `history`：

```python
    def parse(self, text: str, *, profile: Optional[Dict[str, Any]] = None,
              previous: Optional["ShoppingRequest"] = None,
              history: Optional[List[Dict[str, str]]] = None) -> ShoppingRequest:
```

3.2 LLM 调用处（第 86 行）改为：

```python
                    j = parse_shopping_request_with_llm(text, prof_d, prev_d, history=history)
```

3.3 `_merge_llm_json` 中，在「增量 intent / target_rank / is_adjustment」注释块（第 540 行）之前插入：

```python
        # 条数：规则优先，仅当规则未解析到时采纳 LLM（clamp 1..10，与抓取克制一致）
        for src, dst in (("top_n", "top_n"), ("per_platform_n", "per_platform_n")):
            if getattr(req, dst) is None:
                v = j.get(src)
                if isinstance(v, (int, float)) and v >= 1:
                    setattr(req, dst, int(max(1, min(10, v))))
```

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t4_parser.py`
Expected: `ALL PASS`

- [ ] **Step 5: 建议提交点（开发者执行）**

```
git add core/request_parser.py temp/test_t4_parser.py
git commit -m "需求解析透传对话历史+LLM条数合并（规则优先、clamp 1..10）"
```

---

### Task 5: recommender——last_recommendation_brief() 材料摘要

**Files:**
- Modify: `core/recommender.py:649-657` 附近（`get_last_product` 之后新增方法）
- Test: `temp/test_t5_brief.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t5_brief.py —— 头部按「约定」补齐
from profile_module import ProfileManager
from product_searcher import ProductSearcher
from recommender import Recommender

pm = ProfileManager()
rec = Recommender(pm, ProductSearcher())
prods = rec.searcher.mock_search(keyword="夏天连衣裙")   # 演示数据，离线，不进真实搜索
assert prods, "mock_search 应有数据"
rec._last_products = prods[:3]
rec._last_scores = {p.pid: rec.score_product(p) for p in prods[:3]}

brief = rec.last_recommendation_brief()
assert len(brief) == 3
first = brief[0]
for key in ("rank", "name", "platform", "seller", "final_price", "total_score", "good", "bad", "url"):
    assert key in first, key
assert first["rank"] == 1
assert isinstance(first["total_score"], int)

# 空缓存 → 空列表
rec2 = Recommender(ProfileManager(), ProductSearcher())
assert rec2.last_recommendation_brief() == []

print("ALL PASS")
```

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t5_brief.py`
Expected: `AttributeError: 'Recommender' object has no attribute 'last_recommendation_brief'`

- [ ] **Step 3: 实施**

`core/recommender.py` 在 `get_last_product` 之后新增：

```python
    def last_recommendation_brief(self) -> List[Dict[str, Any]]:
        """上次推荐的紧凑摘要（自由问答的材料源；不含评价全文，控制 token）"""
        brief: List[Dict[str, Any]] = []
        for i, p in enumerate(self._last_products, 1):
            s = self._last_scores.get(p.pid)
            brief.append({
                "rank": i,
                "name": p.name,
                "platform": p.platform,
                "seller": p.seller,
                "final_price": round(p.final_price, 1),
                "total_score": int(s.total) if s else None,
                "good": (list(p.review.good_points) or ["—"])[0],
                "bad": (list(p.review.bad_points) or ["—"])[0],
                "url": p.source_url or "",
            })
        return brief
```

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t5_brief.py`
Expected: `ALL PASS`

- [ ] **Step 5: 建议提交点（开发者执行）**

```
git add core/recommender.py temp/test_t5_brief.py
git commit -m "recommender新增last_recommendation_brief：自由问答材料摘要"
```

---

### Task 6: ChatSession——_history 滑动窗口 + chat 包装 + 持久化

**Files:**
- Modify: `shopping_agent.py:51-67`（`__init__` 会话态字段）、`shopping_agent.py:70`（chat 改包装）、`shopping_agent.py:777-789`（export_state）、`shopping_agent.py:802-817`（restore_state）
- Test: `temp/test_t6_history.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t6_history.py —— 头部按「约定」补齐
import ai_client
import request_parser as rp
ai_client._CFG.api_key = ai_client._CFG.base_url = ai_client._CFG.model = ""  # LLM 屏蔽
rp.parse_shopping_request_with_llm = None

from shopping_agent import ChatSession, HISTORY_MAX_MSGS

cs = ChatSession()
assert cs._history == [] and HISTORY_MAX_MSGS == 8

# 「查看档案」是档案命令，立即返回，不进搜索（测试卫生）
r1 = cs.chat("查看档案")
assert r1 and len(cs._history) == 2
assert cs._history[0]["role"] == "user" and cs._history[0]["text"] == "查看档案"
assert cs._history[1]["role"] == "ai" and cs._history[1]["text"] == r1

# 窗口滑动：灌 10 轮（20 条）→ 只剩 8 条，最旧被丢
for i in range(10):
    cs.chat("查看档案")
assert len(cs._history) == 8
assert cs._history[-1]["role"] == "ai"

# export/restore 往返
state = cs.export_state()
assert isinstance(state.get("history"), list) and len(state["history"]) == 8
cs2 = ChatSession()
cs2.restore_state(state)
assert cs2._history == cs._history

# 旧存档兼容：无 history 键 → 空列表，不报错
cs3 = ChatSession()
cs3.restore_state({"collecting_profile": False, "cancelled": False})
assert cs3._history == []

# 空输入不进历史
cs.chat("")
assert len(cs._history) == 8

print("ALL PASS")
```

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t6_history.py`
Expected: `ImportError: cannot import name 'HISTORY_MAX_MSGS' from 'shopping_agent'`

- [ ] **Step 3: 实施**

3.1 模块级常量（`sys.path.insert` 之后、import 区之后，约第 34 行处）：

```python
HISTORY_MAX_MSGS = 8   # LLM 可见的对话窗口条数（4 轮）
```

3.2 `__init__`（第 67 行 `_last_chips` 之后）：

```python
        self._history: List[Dict[str, str]] = []              # 最近对话窗口（随会话态持久化，LLM 材料源）
```

3.3 `chat()` 改包装（第 70 行）：原 `def chat(self, user_input: str) -> str:` 整体改名 `def _chat_impl(self, user_input: str) -> str:`（函数体不动），其上方新增：

```python
    def chat(self, user_input: str) -> str:
        """对外主入口：滑动窗口记录本轮对话（当前输入单独传给解析，不重复计入窗口）"""
        text = (user_input or "").strip()
        reply = self._chat_impl(user_input)
        if text:
            self._history.append({"role": "user", "text": text})
            self._history.append({"role": "ai", "text": reply})
            if len(self._history) > HISTORY_MAX_MSGS:
                del self._history[:len(self._history) - HISTORY_MAX_MSGS]
        return reply
```

3.4 `export_state`（第 780-789 行）返回 dict 末尾追加一键：

```python
            "history": [dict(m) for m in (self._history or [])],
```

（docstring「7 个会话态字段」改为「会话态字段」。）

3.5 `restore_state` 在 `pending_clarify` 恢复（第 814 行）之后插入：

```python
            h = state.get("history")
            self._history = []
            if isinstance(h, list):
                for m in h:
                    if isinstance(m, dict) and m.get("role") in ("user", "ai") and m.get("text"):
                        self._history.append({"role": str(m["role"]), "text": str(m["text"])[:500]})
```

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t6_history.py`
Expected: `ALL PASS`

- [ ] **Step 5: 建议提交点（开发者执行）**

```
git add shopping_agent.py temp/test_t6_history.py
git commit -m "ChatSession对话滑动窗口：chat包装记录+export/restore持久化（旧存档兼容）"
```

---

### Task 7: 自由问答兜底链路接入 chat() 路由 + parse 各调用点传历史

**Files:**
- Modify: `shopping_agent.py:143-145`（主 parse 调用）、`shopping_agent.py:208,212,227,235`（澄清 parse 调用）、`shopping_agent.py:158-176`（4.2/4.3 之间插 4.2.5）、`shopping_agent.py`（ChatSession 类内新增 `_looks_like_question` / `_try_free_qa` / `_PROFILE_QA_KEYS`）
- Test: `temp/test_t7_qa_flow.py`

- [ ] **Step 1: 写失败测试**

```python
# -*- coding: utf-8 -*-
# temp/test_t7_qa_flow.py —— 头部按「约定」补齐
import ai_client
import request_parser as rp
ai_client._CFG.api_key = ai_client._CFG.base_url = ai_client._CFG.model = ""  # 默认屏蔽 LLM
rp.parse_shopping_request_with_llm = None

import shopping_agent
from shopping_agent import ChatSession

# ---- 场景A：LLM 屏蔽 + 有材料 + 疑问句 → 回退澄清，绝不触发搜索 ----
cs = ChatSession()
cs._history.append({"role": "user", "text": "买连衣裙"})
r = cs.chat("哪个好？")
assert "我还没听清你想买什么" in r, r          # 落回 _start_clarify
assert len(cs._history) == 4                   # 问答对也入窗口

# ---- 场景B：问答可用 → 返回 LLM 回答 ----
ai_client.get_config = lambda: {"enabled": True}
ai_client.answer_free_question_with_llm = lambda q, m: f"伪回答·{q}"
cs2 = ChatSession()
cs2.recommender._last_products = []            # 材料仅历史窗口
cs2._history.append({"role": "user", "text": "上淘宝和京东各筛前4名连衣裙"})
r = cs2.chat("第2款和第4款哪个更耐穿？")
assert r.startswith("伪回答·"), r
# 材料包含上次推荐摘要与截断历史（校验组包）
# （answer 已被替换无法捕获 materials，改用 spy）
seen = {}
def spy_qa(q, m):
    seen["q"], seen["m"] = q, m
    return "伪回答"
ai_client.answer_free_question_with_llm = spy_qa
cs3 = ChatSession()
cs3._history = [{"role": "user", "text": "长" * 300}]
r = cs3.chat("哪个好？")
assert r == "伪回答"
assert seen["m"]["recent_chat"][0]["text"] == "长" * 100      # 100 字截断
assert "profile" in seen["m"] and "last_recommendation" in seen["m"]

# ---- 场景C：NEED_SEARCH → 回退澄清 ----
ai_client.answer_free_question_with_llm = lambda q, m: "NEED_SEARCH"
cs4 = ChatSession()
cs4._history.append({"role": "user", "text": "买连衣裙"})
r = cs4.chat("随便来点便宜的")
assert "我还没听清你想买什么" in r, r

# ---- 场景D：无材料 → 不进问答（spy 不被调用）→ 澄清 ----
called = {"n": 0}
def spy_cnt(q, m):
    called["n"] += 1
    return "伪回答"
ai_client.answer_free_question_with_llm = spy_cnt
cs5 = ChatSession()                            # 无 _history 无 _last_products
r = cs5.chat("哪个好？")
assert called["n"] == 0
assert "我还没听清你想买什么" in r, r

# ---- 场景E：有明确品类的需求 → 不触发问答（省一次 LLM 时延）----
ai_client.answer_free_question_with_llm = spy_cnt
cs6 = ChatSession()
cs6._history.append({"role": "user", "text": "买连衣裙"})
# 「来点雪纺连衣裙」有品类词 → 正常走向搜索流程；此处仅验证 spy 未被调用即视为通过
# （不等待搜索完成：先用 target_rank 短路一个无害输入验证路由顺序）
r_probe = cs6.chat("来点雪纺连衣裙")
assert called["n"] == 0, "有品类词的需求不应触发问答"

# ---- 场景F：隐私剔除——档案里的姓名/电话/地址不得进材料 ----
ai_client.answer_free_question_with_llm = spy_qa
cs7 = ChatSession()
cs7.profile.update_batch({"height": "165", "receiver": "张三", "phone": "13800000000",
                          "address": "北京市朝阳区建国路88号", "color_like": "白色"})
cs7._history.append({"role": "user", "text": "买连衣裙"})
cs7.chat("哪个好？")
mat_str = str(seen["m"]["profile"])
assert "张三" not in mat_str and "13800000000" not in mat_str and "朝阳区" not in mat_str
assert "白色" in mat_str

print("ALL PASS")
```

> 场景E注意：「来点雪纺连衣裙」在 LLM 屏蔽下会走 `_flow_recommend` → `search_real`。**为守住测试卫生红线，执行场景E前必须把 `cs6.searcher.search_real` 替换为桩**：
> ```python
> cs6.searcher.search_real = lambda *a, **k: ([], "桩：不进真实搜索", False)
> ```
> （桩返回空候选 → 流程落到「粘贴链接」回退文案，快速无害。）

- [ ] **Step 2: 运行确认失败**

Run: `python temp/test_t7_qa_flow.py`
Expected: `AttributeError: 'ChatSession' object has no attribute '_looks_like_question'`（或场景A断言失败——「哪个好？」被当需求走澄清以外的分支）

- [ ] **Step 3: 实施**

3.1 ChatSession 类属性区（`_CLARIFY_SKIP` 附近，约第 188 行）新增：

```python
    # 自由问答：疑问信号（短句+疑问标记）与档案材料白名单
    _QA_QUESTION_RE = re.compile(
        r"[??]|为什么|哪个|哪些|怎么|怎么样|好不好|值不值|划算|值得吗|多少|有没有|能不能|可不可以|区别|差别|对比|理由|合适吗|好吗|行吗")
    _PROFILE_QA_KEYS = ["height", "weight", "budget_max", "color_like", "color_dislike",
                        "style_like", "style_dislike", "material_like", "material_dislike",
                        "fit_like", "fit_dislike", "size_habit", "brands_like", "brands_dislike",
                        "accept_no_name", "dislike_elements", "ship_region"]
```

3.2 类内新增两个方法（放在 `_start_clarify` 之前）：

```python
    def _looks_like_question(self, text: str) -> bool:
        """短句 + 疑问信号 → 疑似基于上下文的追问（长句视为正常需求，避免误拦）"""
        t = (text or "").strip()
        return bool(t) and len(t) <= 40 and bool(self._QA_QUESTION_RE.search(t))

    def _try_free_qa(self, question: str) -> Optional[str]:
        """自由问答兜底：基于会话材料（上次推荐+历史窗口+档案摘要）LLM 答疑。
        返回回答文本；材料缺失/LLM不可用/调用失败/NEED_SEARCH → None（上层落回原流程）。"""
        try:
            last_brief = self.recommender.last_recommendation_brief()
        except Exception:
            last_brief = []
        if not last_brief and not self._history:
            return None
        try:
            import ai_client
            if not ai_client.get_config().get("enabled"):
                return None
            materials = {
                "last_recommendation": last_brief,
                "recent_chat": [{"role": m["role"], "text": m["text"][:100]} for m in self._history],
                # 档案只取购物相关白名单字段：姓名/电话/地址等隐私绝不进 prompt
                "profile": {k: self.profile.get(k) for k in self._PROFILE_QA_KEYS if self.profile.get(k)},
            }
            ans = ai_client.answer_free_question_with_llm(question, materials)
        except Exception:
            return None
        if not ans or not ans.strip():
            return None
        if ans.strip().upper().startswith("NEED_SEARCH"):
            return None
        return ans.strip()
```

3.3 chat() 路由插入 4.2.5：将第 158-165 行的：

```python
        # 4.3 信息不全，主动追问（不盲目搜索）；品类/关键词全缺或只有泛词 → 澄清式多轮问答
        #     泛词判定独立于 needs_clarify（LLM 脑补出「其他/东西」等关键词时 needs_clarify 可能为空）
        clarify_hint = ""
        junk_kw = (not str(req.keyword or "").strip()) or \
                  req.keyword.strip() in ("其他", "东西", "商品", "物品")
        if not req.target_rank and not req.category and junk_kw \
                and not req.require_tags and not req.purpose:
            return self._start_clarify(text)
```

改为：

```python
        # 4.2.5 自由问答兜底：疑似上下文追问（疑问信号）或解析不出明确需求（泛词），
        #       且会话有材料、LLM 可用 → 尝试基于材料答疑；None → 落回下方原流程
        junk_kw = (not str(req.keyword or "").strip()) or \
                  req.keyword.strip() in ("其他", "东西", "商品", "物品")
        req_vague = junk_kw and not req.category and not req.require_tags and not req.purpose
        if (self._looks_like_question(text) or req_vague):
            qa = self._try_free_qa(text)
            if qa is not None:
                return qa

        # 4.3 信息不全，主动追问（不盲目搜索）；品类/关键词全缺或只有泛词 → 澄清式多轮问答
        #     泛词判定独立于 needs_clarify（LLM 脑补出「其他/东西」等关键词时 needs_clarify 可能为空）
        clarify_hint = ""
        if not req.target_rank and not req.category and junk_kw \
                and not req.require_tags and not req.purpose:
            return self._start_clarify(text)
```

3.4 parse 各调用点传历史——共 5 处，均在调用实参尾部追加 `history=self._history`：

- 第 145 行：`req = self.parser.parse(text, profile=profile_dict, previous=self._last_request, history=self._history)`
- 第 208 行：`req = self.parser.parse(base_text, profile=self.profile.get_all(), previous=self._last_request, history=self._history)`
- 第 212 行：`req = self.parser.parse(merged, profile=self.profile.get_all(), previous=self._last_request, history=self._history)`
- 第 227 行：`req2 = self.parser.parse(base_text, profile=self.profile.get_all(), previous=self._last_request, history=self._history)`
- 第 235 行：`req = self.parser.parse(merged, profile=self.profile.get_all(), previous=self._last_request, history=self._history)`

- [ ] **Step 4: 运行确认通过**

Run: `python temp/test_t7_qa_flow.py`
Expected: `ALL PASS`

- [ ] **Step 5: 全量回归（此前任务脚本一起跑）**

```
python temp/test_t1_polish.py
python temp/test_t2_parse_llm.py
python temp/test_t3_qa.py
python temp/test_t4_parser.py
python temp/test_t5_brief.py
python temp/test_t6_history.py
python temp/test_t7_qa_flow.py
```
Expected: 7 个脚本全部 `ALL PASS`（Task 6 测试在 LLM 真实配置了 key 的机器上也能过，因其已屏蔽 `_CFG`；若开发者 api_key.json 存在，T6/T7 的 monkeypatch 顺序必须先于 `ChatSession()` 实例化——脚本已按此顺序书写）

- [ ] **Step 6: 建议提交点（开发者执行）**

```
git add shopping_agent.py temp/test_t7_qa_flow.py
git commit -m "自由问答兜底接入chat路由：疑问信号+泛词双触发、材料组包隐私白名单、parse传历史"
```

---

### Task 8: 真机冒烟 + 收尾（问题疑惑区归档、开发日志）

**Files:**
- Modify: `docs/project/问题疑惑区.md`（勾选 + 归档第2轮）
- Create: `docs/log/260912.md` 增补小节（当天日志已存在则追加）
- 无代码改动

- [ ] **Step 1: 真机冒烟（需开发者环境有可用 API Key）**

重启服务（`.bat` 或 `python web_server.py`），验证 8765 端口可访问后，在页面里按序验证：
1. 「买一条夏天连衣裙 预算200」出推荐 → 「第2款和第4款哪个更耐穿？」→ 应返回基于推荐材料的对比回答（非澄清、非搜索）；
2. 「再要2个」→ 解析出增量条数（回复中出现 TOP2 或条数变化）；
3. 「帮我搜下百元内的蓝牙耳机」→ 正常走搜索（不被问答误拦）；
4. 「随便来点便宜的」→ 回落品类澄清；
5. 重启服务后追问「刚才推荐里哪个最便宜？」→ 会话恢复后仍能基于此前推荐作答（history 落盘生效）。
冒烟中任一步异常 → 停下排查，不带病提交。

- [ ] **Step 2: 问题疑惑区收尾（AI 按归档规则执行）**

`docs/project/问题疑惑区.md`：将「待完成·问题区」的条目勾选为 `[X]`，附修复说明（要点：T2 条数解析已生效并复测通过；「仍只有三个」实为候选池缩水+本轮补齐对话能力；对话记忆与自由问答由本轮 spec/plan 落地），然后按归档规则把该条目作为「### 第2轮（260912）：系统提示词过硬→对话记忆与自由问答」插入已完成区顶部。

- [ ] **Step 3: 开发日志**

`docs/log/260912.md` 追加小节（标题下附 `> 创建于 YYYY-MM-DD HH:MM`，用 `date` 取实际时间），记录：背景定位（T2 后复测 top_n=5 正常，真痛点=单轮记忆）、四项改动（润色动态化/解析历史注入+条数字段/问答链路/会话窗口持久化）、回退矩阵、测试结果（7 个脚本 + 真机冒烟）。

- [ ] **Step 4: 建议提交点（开发者执行）**

```
git add docs/project/问题疑惑区.md docs/log/260912.md
git commit -m "对话记忆+自由问答落地：疑惑区第2轮归档+开发日志"
```

---

## 自审记录（写完计划后核对）

- **spec 覆盖**：4.1 窗口→Task6；4.2 解析增强→Task2+4；4.3 问答链路→Task3+5+7；4.4 润色动态化→Task1；测试点 1-7→Task4(1,3)/T7(A-C)/T6(6)/T7(F)，测试点2的「再要2个」LLM 真机部分→Task8 冒烟2。无遗漏。
- **占位符扫描**：无 TBD/TODO；所有代码步骤给出完整代码。
- **类型一致性**：`answer_free_question_with_llm(question, materials)` 三处引用一致；`last_recommendation_brief()` 返回 List[Dict]；`HISTORY_MAX_MSGS` 命名统一；`_history` 元素结构 `{"role","text"}` 统一。
- **已知偏差**：T7 场景E需 searcher 桩（脚本内已写明），防止有品类词需求在 LLM 屏蔽下误入真实搜索。
