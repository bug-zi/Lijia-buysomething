# 注销账号 + 顶栏用户名头像 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 注销账号（验密码后永久删除账户条目与 `accounts/<用户名>/` 数据目录）+ 顶栏改为「用户名在左、头像在右」（上传头像 → 性别默认图 → 首字母色块）。

**Architecture:** 后端 `AccountStore.delete_account()`（复用 `_lock`/`_hash_password`/原子写）+ 新端点 `POST /api/auth/delete_account`（token 鉴权后路由）；前端单文件 index.html：账户胶囊结构对调 + 取头像函数 + 刷新钩子，账户菜单加「注销账号」+ `askText` 密码确认弹窗。

**Tech Stack:** 纯 Python 标准库（hashlib/shutil/os）；前端 index.html 原生 JS。

**红线遵守：**
- 本计划**不含任何 git 步骤**——项目 Git 纪律：全部 git 操作由开发者本人完成。
- 全程无 emoji；界面图标用 Material Symbols；警示色用 `var(--danger)` 主题变量。
- 测试不进真实搜索链路（不触发抓取/LLM 探测）；脚本级测试只走临时目录（不碰真实 `accounts.json` / `accounts/`）；浏览器实测用「注册即注销」的一次性测试账户，测完自清理。
- 重启服务后必须验 8765 端口存活。
- spec：`docs/project/功能模块/多账户/2026-09-16-注销账号与顶栏头像-design.md`。

**验证命令约定：** 在项目根执行；`python` 即 `D:\app\Python\python.exe`；服务地址 `http://127.0.0.1:8765`。

---

### Task 1: 后端 `AccountStore.delete_account`（TDD）

**Files:**
- Modify: `core/account_manager.py`（import 区 ~L13-20；`logout` 之后 ~L147；`__main__` 自测块末尾 ~L296-298）

- [ ] **Step 1: 在 `__main__` 自测块追加第 10) 节测试（先写测试）**

在 `core/account_manager.py` 第 296 行（第 9) 节 `assert got == ...` 之后）、第 298 行 `print(...)` 之前插入，并同步更新末尾 print 文案：

```python
    # 10) 注销：密码错误不删；密码正确条目+目录都消失、旧 token 失效；目录缺失仅删条目
    rd = store.register("注销用户", "del@x.com", "pass1234")
    assert rd["ok"], rd
    udir = os.path.join(DATA_ROOT, "注销用户")
    os.makedirs(udir, exist_ok=True)
    open(os.path.join(udir, "user_center.json"), "w", encoding="utf-8").write("{}")
    assert not store.delete_account("注销用户", "wrong")["ok"], "密码错误应拒绝注销"
    assert "注销用户" in store._read()["accounts"], "密码错误不应移除条目"
    assert os.path.isfile(os.path.join(udir, "user_center.json")), "密码错误不应删目录"
    assert store.delete_account("注销用户", "pass1234")["ok"], "正确密码应注销成功"
    assert "注销用户" not in store._read()["accounts"], "注销后条目应移除"
    assert not os.path.exists(udir), "注销后数据目录应删除"
    assert store.resolve(rd["token"]) is None, "注销后旧 token 应失效"
    rn = store.register("无目录用户", "nodir@x.com", "1234")
    assert rn["ok"] and store.delete_account("无目录用户", "1234")["ok"], "数据目录缺失时也应注销成功"

    print("account_manager 冒烟通过：校验/注册/登录/登出/token持久化/上下文路径/迁移/线程传播/注销")
```

（原 print 行整行替换为上面的新 print 行。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python core/account_manager.py`
Expected: `AttributeError: 'AccountStore' object has no attribute 'delete_account'`

- [ ] **Step 3: 实现 `delete_account`**

① 文件头 import 区（`import re` 之后，保持字母序）加一行：

```python
import shutil
```

② `logout` 方法（L147 `return False` 之后）、`resolve` 方法之前插入：

```python
    def delete_account(self, username: str, password: str) -> Dict[str, Any]:
        """注销：验密后移除账户条目并删除其数据目录（不可恢复）"""
        username = (username or "").strip()
        with _lock:
            data = self._read()
            a = data["accounts"].get(username)
            if not a:
                return {"ok": False, "error": "账户不存在。"}
            if a.get("password_hash") != _hash_password(password or "", a.get("salt", "")):
                return {"ok": False, "error": "密码不正确。"}
            del data["accounts"][username]
            self._write(data)
            # 目录名净化与 data_dir() 同规则；realpath 收敛在 DATA_ROOT 下才删（防路径意外）
            safe = "".join(c for c in username if c not in '\\/:*?"<>|')
            user_dir = os.path.realpath(os.path.join(DATA_ROOT, safe))
            root = os.path.realpath(DATA_ROOT)
            if user_dir != root and user_dir.startswith(root + os.sep) and os.path.isdir(user_dir):
                shutil.rmtree(user_dir, ignore_errors=True)
        return {"ok": True}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python core/account_manager.py`
Expected: `account_manager 冒烟通过：校验/注册/登录/登出/token持久化/上下文路径/迁移/线程传播/注销`

---

### Task 2: web_server 端点 `POST /api/auth/delete_account`

**Files:**
- Modify: `web_server.py`（do_POST，L294 `account_manager.set_current(user["username"])` 之后、L296 `if path == "/api/chat":` 之前）

- [ ] **Step 1: 插入路由**

在鉴权区之后（必须持有效 token 才可达）插入：

```python
        if path == "/api/auth/delete_account":
            r = account_manager.ACCOUNTS.delete_account(user["username"], (body or {}).get("password", ""))
            if r.get("ok"):
                json_response(self, 200, {"ok": True})
            else:
                json_response(self, 400, r)
            return
```

- [ ] **Step 2: 语法检查**

Run: `python -m py_compile web_server.py`
Expected: 无输出（编译通过）

- [ ] **Step 3: 重启服务并验活**

重启方式：关掉旧服务窗口，双击 `启动购物助手-网页版.bat`（或等价命令行启动）。
Run: `curl -s http://127.0.0.1:8765/api/health`
Expected: 返回 JSON 且 `ok` 为真（服务存活）。
再验未登录拦截：`curl -s -X POST http://127.0.0.1:8765/api/auth/delete_account`
Expected: `{"ok": false, "error": "未登录"}`（401）

---

### Task 3: 前端顶栏「用户名 + 头像」

**Files:**
- Modify: `index.html`（chip HTML ~L1200-1202；acct CSS ~L1054 后；JS 头像函数 ~L1928 后；刷新钩子 ~L1713-1729、~L2309、~L2336、~L2352、~L2361、~L2369、~L3473）

- [ ] **Step 1: 改 chip 结构（用户名左、头像右）**

L1200-1202 的按钮内，把图标 span 换成头像 span 并移到用户名之后：

```html
          <button class="acct-chip" id="acct-chip" onclick="toggleAcctMenu(event)" title="账户">
            <span id="acct-name">未登录</span><span class="acct-avatar" id="acct-avatar"></span>
          </button>
```

- [ ] **Step 2: 加 CSS**

在 `.acct-menu button .material-symbols-outlined { ... }`（L1054）之后追加：

```css
.acct-chip .acct-avatar { width: 24px; height: 24px; border-radius: 50%; overflow: hidden; display: inline-flex;
  align-items: center; justify-content: center; flex: none; }
.acct-chip .acct-avatar img { width: 100%; height: 100%; object-fit: cover; display: block; }
.acct-chip .acct-avatar .uc-avatar { width: 24px; height: 24px; font-size: 12px; }
```

（复用用户中心 `.uc-avatar` 的色系 class 作首字母色块，仅覆盖尺寸。）

- [ ] **Step 3: 加顶栏头像取值与刷新函数**

在 `refreshChatAvatars()`（L1924-1928）之后追加：

```js
/* 顶栏账户胶囊头像：优先自定义/性别默认头像图，否则首字母+主题色色块（不用图标） */
function topbarAvatarHtml(name){
  const src = ucAvatarSrc();
  if (src) return '<img src="' + escapeHtml(src) + '" alt="">';
  const color = (_uc && _uc.avatar_color) || "classic";
  const letter = ((name || "用").charAt(0) || "用").toUpperCase();
  return '<span class="uc-avatar ' + color + '">' + escapeHtml(letter) + '</span>';
}
function refreshAcctChip(){
  const box = $("#acct-avatar");
  if (box) box.innerHTML = topbarAvatarHtml(saUsername());
}
```

- [ ] **Step 4: 接刷新钩子（7 处，均为追加一次调用）**

① `showAuthGate()`（L1713-1718）内 `$("#acct-name").textContent = "未登录";` 之后加：

```js
  const _av = $("#acct-avatar"); if (_av) _av.innerHTML = "";
```

② `onAuthed()`（L1719-1729）内 `$("#acct-info").textContent = "当前账户：" + name;` 之后加：

```js
  refreshAcctChip();
```

③ `loadUserCenter()` 成功分支（L2309 `renderUcAvatar();`）后追加一行 `refreshAcctChip();`

④ `setUcGender()` L2336 的 `renderUcGender(); renderUcAvatar(); refreshChatAvatars();` 改为：

```js
    _uc = r.user; renderUcGender(); renderUcAvatar(); refreshChatAvatars(); refreshAcctChip();
```

⑤ `onUcAvatarSelect()` L2352 同法追加 `refreshAcctChip();`

⑥ `resetUcAvatar()` L2361 同法追加 `refreshAcctChip();`

⑦ `saveUserCenter()` L2369 的 `renderUcAvatar();` 后追加 `refreshAcctChip();`；引导向导 `obNext()` L3473 的 `renderUcAvatar(); refreshChatAvatars();` 后追加 `refreshAcctChip();`

- [ ] **Step 5: 浏览器快验**

刷新 `http://127.0.0.1:8765`，登录既有账户：顶栏胶囊应为「用户名 + 24px 圆形头像」（头像为该账户当前头像状态：上传过→自定义图；有性别→默认图；否则首字母色块）；用户中心改性别/上传头像后顶栏即时变化。控制台无报错。

---

### Task 4: 前端「注销账号」入口 + 密码确认

**Files:**
- Modify: `index.html`（acct-menu ~L1205；doLogout 之后 ~L1770）

- [ ] **Step 1: 菜单加按钮（退出登录下方）**

```html
            <button class="danger" onclick="doDeleteAccount()"><span class="material-symbols-outlined">delete_forever</span>注销账号</button>
```

- [ ] **Step 2: 加 danger 配色（主题变量，非彩亮色）**

Task 3 Step 2 追加的 CSS 之后再补：

```css
.acct-menu button.danger { color: var(--danger); }
.acct-menu button.danger .material-symbols-outlined { color: var(--danger); }
```

- [ ] **Step 3: 加 `doDeleteAccount()`**

在 `doLogout()`（L1763-1770）之后追加（密码错误时弹窗重开并就地带出错误文案，可重输）：

```js
async function doDeleteAccount(){
  const name = saUsername();
  const ask = (msg) => askText({
    title: "注销账号", icon: "delete_forever", danger: true, okText: "确认注销",
    message: msg || "账号及其全部本地数据（档案/订单/清单/会话等）将被永久删除，不可恢复。请输入登录密码确认。",
    placeholder: "登录密码", value: ""
  });
  let pwd = await ask();
  while (pwd !== null && pwd){
    const r = await api("/api/auth/delete_account", "POST", { password: pwd });
    if (r && r.ok){
      try{ localStorage.removeItem("sa_active_session"); localStorage.removeItem("sa_uc"); localStorage.removeItem("sa_onboarded"); }catch(e){}
      setSaToken("", "");
      CURRENT_SESSION_ID = "";
      location.reload();
      return;
    }
    toast((r && r.error) || "注销失败", "err");
    pwd = await ask((r && r.error) || "注销失败，请重试。");
  }
}
```

- [ ] **Step 4: 快验**

刷新页面，点开账户菜单：应见「退出登录」与暗红色「注销账号」两项；点「注销账号」弹出带密码输入框的确认弹窗，取消可关。

---

### Task 5: 浏览器实测走查（一次性测试账户，测完自清理）

- [ ] **Step 1: 三种头像形态**

注册/登录一次性账户 `测试注销01`（密码随意 ≥4 位）：
1. 走完引导（性别不填）→ 顶栏应为「测试注销01 + classic 蓝色首字母色块“测”」；
2. 用户中心性别改「男生」→ 顶栏变 avatar_male.png；改「女生」→ avatar_female.png；
3. 用户中心上传一张本地图片 → 顶栏变自定义图；点「恢复默认头像」→ 回性别默认图。
全程顶栏即时刷新，无 person 图标出现。

- [ ] **Step 2: 注销全流程**

1. 账户菜单 → 注销账号 → 输错密码 → 弹窗带出「密码不正确。」可重输，账户未被删；
2. 输对密码 → 回登录门；旧 token 验 401：注销前先从 localStorage 复制 `sa_token`，注销后执行
   `curl -s -H "X-Auth-Token: <旧token>" http://127.0.0.1:8765/api/auth/me`
   Expected: `{"ok": false, "error": "未登录"}`；
3. 检查 `accounts.json` 无 `测试注销01`、`accounts/测试注销01/` 目录已消失；
4. 重新注册同名 `测试注销01` → 全新空数据（档案/购物车/清单为空）→ 走注销删掉，完成自清理。

- [ ] **Step 3: 回归抽查**

深色模式下顶栏胶囊与弹窗外观协调；既有账户「退出登录」仍正常；对话页正常收发（用演示数据，不进真实搜索链路）。

---

### Task 6: 文档落地

**Files:**
- Create/Modify: `docs/log/260916.md`（已存在则追加小节）
- Modify: `docs/project/新功能开发区.md`（两项待办移入归档区）
- Move: `docs/project/功能模块/多账户/2026-09-16-注销账号与顶栏头像-design.md`、`...-plan.md` → `docs/project/功能模块/多账户/archive/`
- Modify: `docs/project/演示汇报.md`

- [ ] **Step 1: 开发日志**

`docs/log/260916.md` 不存在则新建；每个小节标题下加 `> 创建于 YYYY-MM-DD HH:MM`（用 `date` 取实际时间）。小节：①注销账号（后端 delete_account + 端点 + 前端密码确认）；②顶栏用户名+头像（取值优先级与刷新钩子）；③测试与验证（自测块新增第 10 节、浏览器走查、一次性账户自清理）。

- [ ] **Step 2: 新功能开发区归档（第3轮）**

两项待办从「待完成」移除；归档区顶部插入（保持既有格式，含完成说明）：

```markdown
### 第3轮（260916）：注销账号 + 顶栏用户名头像

- [X] 补充“注销当前账号”的功能
  - **完成说明**：已落地。`AccountStore.delete_account`（验密码 → accounts.json 原子移除条目 → 删 `accounts/<用户名>/` 数据目录）+ 端点 `POST /api/auth/delete_account`（token 鉴权后路由）；前端账户菜单「注销账号」暗红入口 + askText 密码确认弹窗（错密就地重输），删除后清本地缓存回登录门。设计/实施：`docs/project/功能模块/多账户/archive/`；详见 260916 开发日志。
- [X] 右上角显示用户名+头像，用户名在左，头像在右；头像优先使用用户上传的，没有就用系统默认的，不要用当前的这个图标
  - **完成说明**：已落地。账户胶囊改「用户名左 + 24px 圆形头像右」；头像取值 上传自定义 → 性别默认图（男/女）→ 首字母+主题色色块（复用 uc-avatar 色系），不再使用 person 图标；登录/用户中心保存/上传/恢复默认/引导完成 7 处即时刷新。设计/实施：`docs/project/功能模块/多账户/archive/`；详见 260916 开发日志。
```

- [ ] **Step 3: spec/plan 移入 archive**

把本轮 design.md 与 plan.md 两个文件移动到 `docs/project/功能模块/多账户/archive/`。

- [ ] **Step 4: 演示汇报.md 同步**

在多账户相关演示点处补充两句：「注销账号：密码确认后账户与全部数据永久删除，可现场演示删除-重注册的全新隔离」「顶栏用户名+头像（上传/系统默认/首字母色块三级取值）」；执行时先读该文档定位插入点。

---

## Self-Review 记录

- **Spec 覆盖**：§3.1→Task1（含目录缺失分支测试）；§3.2→Task2；§4.1→Task3（7 处刷新钩子逐条列出）；§4.2→Task4；§5 边界→Task1 测试 + Task2 未登录拦截 + Task4 错密重输；§6→Task1/5；§7→Task6。无缺口。
- **占位符**：无 TBD/“适当处理”类描述；所有代码步骤给出完整代码。
- **命名一致性**：函数名与 spec 一致（`delete_account` / `topbarAvatarHtml` / `refreshAcctChip` / `doDeleteAccount`）；路径净化规则与 `data_dir()` 同源。
