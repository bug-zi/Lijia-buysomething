# 多账户机制 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 本地多账户：注册（用户名+邮箱+密码即成功）/登录/退出，各账户数据完全隔离（含 API Key），首个注册账户继承存量数据。

**Architecture:** 新增 `core/account_manager.py`（账户表 + 当前账户上下文 + 存量迁移）；各数据模块路径改为「按当前账户上下文解析」；web_server 统一鉴权（X-Auth-Token），会话缓存与共享管理器宿主按账户分键；前端 index.html 加登录/注册门与账户菜单。

**Tech Stack:** 纯 Python 标准库（hashlib/uuid/threading）；前端单文件 index.html 原生 JS。

**红线遵守：**
- 本计划**不含任何 git 步骤**——项目 Git 纪律：全部 git 操作由开发者本人完成。
- 全程无 emoji；界面图标用 Material Symbols；配色用主题变量。
- 测试不进真实搜索链路（不触发抓取/LLM 连通探测），不动真实 `accounts.json`、`accounts/`、`8765` 端口——集成测试用临时数据根 + 8901 端口。
- spec：`docs/project/功能模块/多账户/2026-09-15-多账户机制-design.md`。

**验证命令约定：** 在项目根执行；`python` 即 `D:\app\Python\python.exe`。

---

### Task 1: 新模块 core/account_manager.py（账户表 + 上下文 + 迁移）

**Files:**
- Create: `core/account_manager.py`

- [ ] **Step 1: 写模块（含文件尾自测，先写测试后写实现的主体一次成文）**

```python
# -*- coding: utf-8 -*-
"""
账户管理模块（本地多账户）

- accounts.json（项目根，全局一份）：用户名/邮箱/加盐哈希密码/token；
- 每账户数据目录 DATA_ROOT/<用户名>/，业务模块经 data_file() 按当前账户取路径；
- 当前账户上下文：threading.local，web_server 每请求鉴权后 set_current()；
  跨线程（线程池）用 bound() 显式传播；
- migrate_legacy_into：注册第一个账户时把项目根旧数据文件移入其账户目录（首个注册继承存量数据）。
只用标准库；无账户上下文时路径回落 LEGACY_ROOT（项目根，兼容存量与各模块自测）。
"""

import hashlib
import json
import os
import re
import threading
import time
import uuid
from typing import Any, Callable, Dict, List, Optional

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))  # core/ 的上级 = 项目根
ACCOUNTS_FILE = os.path.join(BASE_DIR, "accounts.json")
DATA_ROOT = os.path.join(BASE_DIR, "accounts")   # 每账户数据目录的父目录
LEGACY_ROOT = BASE_DIR                            # 无账户上下文时的回落目录

# 首个注册账户可继承的存量数据文件白名单（与 spec 第 4 节 8 文件一致）
LEGACY_DATA_FILES = (
    "user_profile.json", "orders.json", "virtual_cart.json", "shopping_list.json",
    "chat_sessions.json", "user_center.json", "api_key.json", "search_history.json",
)

_lock = threading.RLock()
_local = threading.local()

_USERNAME_RE = re.compile(r"^[\u4e00-\u9fa5A-Za-z0-9_]{2,20}$")
_EMAIL_RE = re.compile(r"^[^@\s]+@[^@\s]+\.[^@\s]+$")


def _now() -> str:
    return time.strftime("%Y-%m-%d %H:%M:%S")


def _hash_password(password: str, salt_hex: str) -> str:
    return hashlib.sha256(bytes.fromhex(salt_hex) + password.encode("utf-8")).hexdigest()


def _read_store(path: str) -> Dict[str, Any]:
    """读账户表；缺失/损坏（备份 .broken）按空表处理"""
    if not os.path.exists(path):
        return {"accounts": {}}
    try:
        with open(path, "r", encoding="utf-8") as f:
            data = json.load(f)
        if not isinstance(data, dict) or not isinstance(data.get("accounts"), dict):
            raise ValueError("bad accounts.json")
        return data
    except Exception:
        try:
            os.replace(path, path + ".broken")
        except OSError:
            pass
        return {"accounts": {}}


def _write_store(path: str, data: Dict[str, Any]) -> None:
    """原子写：临时文件 + os.replace"""
    tmp = path + ".tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
    os.replace(tmp, path)


def validate_register(username: str, email: str, password: str) -> str:
    """注册校验；返回错误文案，空串=通过"""
    if not isinstance(username, str) or not _USERNAME_RE.match(username.strip()):
        return "用户名需 2-20 位，仅限中英文、数字、下划线，不含空格。"
    if not isinstance(email, str) or not _EMAIL_RE.match(email.strip()):
        return "邮箱格式不正确。"
    if not isinstance(password, str) or len(password) < 4:
        return "密码至少 4 位。"
    return ""


class AccountStore:
    """账户表：注册/登录/登出/token 解析（文件路径可注入，便于自测）"""

    def __init__(self, accounts_file: str = ACCOUNTS_FILE):
        self.accounts_file = accounts_file

    def _read(self) -> Dict[str, Any]:
        return _read_store(self.accounts_file)

    def _write(self, data: Dict[str, Any]) -> None:
        _write_store(self.accounts_file, data)

    def is_empty(self) -> bool:
        with _lock:
            return not self._read()["accounts"]

    def register(self, username: str, email: str, password: str) -> Dict[str, Any]:
        username = (username or "").strip()
        email = (email or "").strip()
        err = validate_register(username, email, password)
        if err:
            return {"ok": False, "error": err}
        with _lock:
            data = self._read()
            accounts = data["accounts"]
            if username in accounts:
                return {"ok": False, "error": "该用户名已被注册。"}
            if any(a.get("email") == email for a in accounts.values()):
                return {"ok": False, "error": "该邮箱已被注册。"}
            salt = os.urandom(16).hex()
            token = uuid.uuid4().hex
            accounts[username] = {
                "username": username, "email": email,
                "salt": salt, "password_hash": _hash_password(password, salt),
                "created_at": _now(), "token": token, "token_time": _now(),
            }
            self._write(data)
            return {"ok": True, "token": token, "username": username}

    def login(self, username: str, password: str) -> Dict[str, Any]:
        username = (username or "").strip()
        with _lock:
            data = self._read()
            a = data["accounts"].get(username)
            if not a or a.get("password_hash") != _hash_password(password or "", a.get("salt", "")):
                return {"ok": False, "error": "用户名或密码错误。"}
            token = uuid.uuid4().hex
            a["token"] = token
            a["token_time"] = _now()
            self._write(data)
            return {"ok": True, "token": token, "username": username}

    def logout(self, token: str) -> bool:
        if not token:
            return False
        with _lock:
            data = self._read()
            for a in data["accounts"].values():
                if a.get("token") == token:
                    a["token"] = ""
                    self._write(data)
                    return True
        return False

    def resolve(self, token: str) -> Optional[Dict[str, Any]]:
        """token → 账户公开信息；无效返回 None"""
        if not token:
            return None
        with _lock:
            for a in self._read()["accounts"].values():
                if a.get("token") and a["token"] == token:
                    return {"username": a["username"], "email": a["email"],
                            "created_at": a.get("created_at", "")}
        return None


ACCOUNTS = AccountStore()   # web_server 使用的全局账户表（测试可整体替换）


# ---------- 当前账户上下文 ----------

def set_current(username: Optional[str]) -> None:
    _local.username = username


def current() -> Optional[str]:
    return getattr(_local, "username", None)


def data_dir(username: Optional[str] = None) -> str:
    """账户数据目录；无账户（未传且无上下文）回落 LEGACY_ROOT"""
    u = username if username is not None else current()
    if not u:
        return LEGACY_ROOT
    safe = "".join(c for c in u if c not in '\\/:*?"<>|')
    return os.path.join(DATA_ROOT, safe)


def data_file(filename: str) -> str:
    """业务模块统一入口：按当前账户解析数据文件路径"""
    return os.path.join(data_dir(), filename)


def bound(fn: Callable) -> Callable:
    """把当前账户上下文绑进将在其他线程执行的 callable（线程池传播）；无上下文时原样返回"""
    acct = current()
    if not acct:
        return fn

    def runner(*args, **kwargs):
        set_current(acct)
        try:
            return fn(*args, **kwargs)
        finally:
            set_current(None)
    return runner


# ---------- 存量数据迁移（首个注册账户继承） ----------

def migrate_legacy_into(username: str, legacy_dir: str = "",
                        dest_dir: str = "") -> List[str]:
    """把项目根旧数据文件原子移动进账户目录；返回实际移动的文件名列表"""
    src_dir = legacy_dir or LEGACY_ROOT
    dst = dest_dir or data_dir(username)
    os.makedirs(dst, exist_ok=True)
    moved: List[str] = []
    for name in LEGACY_DATA_FILES:
        src = os.path.join(src_dir, name)
        if not os.path.isfile(src):
            continue
        try:
            os.replace(src, os.path.join(dst, name))
            moved.append(name)
        except OSError:
            pass
    return moved


if __name__ == "__main__":
    # 冒烟：全部走临时目录，不碰真实 accounts.json / accounts/
    import tempfile
    tmpdir = tempfile.mkdtemp(prefix="_acct_selftest_")
    global DATA_ROOT, LEGACY_ROOT   # noqa: F824  自测内重定向数据根
    DATA_ROOT = os.path.join(tmpdir, "accounts")
    LEGACY_ROOT = os.path.join(tmpdir, "legacy")
    os.makedirs(LEGACY_ROOT, exist_ok=True)
    store = AccountStore(accounts_file=os.path.join(tmpdir, "accounts.json"))

    # 1) 校验
    assert validate_register("a b", "x@y.com", "1234"), "用户名含空格应拒绝"
    assert validate_register("小明", "bad-mail", "1234"), "邮箱格式错应拒绝"
    assert validate_register("小明", "x@y.com", "123"), "短密码应拒绝"
    assert validate_register("小明", "x@y.com", "1234") == "", "合法注册应通过"

    # 2) 注册 / 重复
    r1 = store.register("小明", "a@x.com", "1234")
    assert r1["ok"] and r1["token"], r1
    assert not store.register("小明", "b@x.com", "1234")["ok"], "重名应拒绝"
    assert not store.register("小红", "a@x.com", "1234")["ok"], "重邮箱应拒绝"

    # 3) token 解析 / 明文不落盘
    assert store.resolve(r1["token"])["username"] == "小明"
    assert store.resolve("bad-token") is None
    raw = open(store.accounts_file, encoding="utf-8").read()
    assert "1234" not in raw, "密码明文泄漏"

    # 4) 登录：错误统一文案；成功换发新 token，旧 token 失效
    assert store.login("小明", "wrong")["error"] == "用户名或密码错误。"
    r2 = store.login("小明", "1234")
    assert r2["ok"] and r2["token"] != r1["token"], "登录应换发 token"
    assert store.resolve(r1["token"]) is None, "旧 token 应失效"

    # 5) 登出
    assert store.logout(r2["token"])
    assert store.resolve(r2["token"]) is None

    # 6) 重启存活：同文件新实例仍能解析 token
    r3 = store.register("小红", "b@x.com", "5678")
    store2 = AccountStore(accounts_file=store.accounts_file)
    assert store2.resolve(r3["token"])["username"] == "小红", "token 应持久存活"

    # 7) 上下文路径
    set_current("小明")
    assert data_file("orders.json") == os.path.join(DATA_ROOT, "小明", "orders.json")
    set_current(None)
    assert data_file("orders.json") == os.path.join(LEGACY_ROOT, "orders.json")

    # 8) 迁移：白名单内移动、缺失跳过
    open(os.path.join(LEGACY_ROOT, "user_profile.json"), "w", encoding="utf-8").write("{}")
    open(os.path.join(LEGACY_ROOT, "api_key.json"), "w", encoding="utf-8").write("{}")
    open(os.path.join(LEGACY_ROOT, "not_in_list.json"), "w", encoding="utf-8").write("{}")
    moved = migrate_legacy_into("小红")
    assert sorted(moved) == ["api_key.json", "user_profile.json"], moved
    assert os.path.isfile(os.path.join(DATA_ROOT, "小红", "user_profile.json"))
    assert not os.path.exists(os.path.join(LEGACY_ROOT, "user_profile.json"))
    assert os.path.isfile(os.path.join(LEGACY_ROOT, "not_in_list.json")), "白名单外不应移动"

    # 9) 线程传播 bound
    set_current("小明")
    wrapped = bound(lambda: data_file("x.json"))
    set_current(None)
    import concurrent.futures as _cf
    with _cf.ThreadPoolExecutor(max_workers=1) as ex:
        got = ex.submit(wrapped).result()
    assert got == os.path.join(DATA_ROOT, "小明", "x.json"), "bound 应在线程内恢复上下文"

    print("account_manager 冒烟通过：校验/注册/登录/登出/token持久化/上下文路径/迁移/线程传播")
```

- [ ] **Step 2: 运行自测**

Run: `python core/account_manager.py`
Expected: `account_manager 冒烟通过：校验/注册/登录/登出/token持久化/上下文路径/迁移/线程传播`

---

### Task 2: 数据模块路径上下文化（session_store / user_center / shopping_list / product_searcher）

模式统一：模块保留原常量作回落；新增 `_file()` —— 有当前账户上下文时返回 `accounts/<用户名>/<文件名>`，否则返回原常量（各模块 `__main__` 自测重绑定常量的老用法不受影响）。

**Files:**
- Modify: `core/session_store.py`
- Modify: `core/user_center.py`
- Modify: `core/shopping_list.py`
- Modify: `core/product_searcher.py:881-893`

- [ ] **Step 1: session_store.py —— `_read`/`_write` 改走 `_file()`**

在 `SESSIONS_FILE = ...`（第 19 行）之后加：

```python
def _file() -> str:
    """数据文件路径：有当前账户上下文时用 accounts/<用户名>/chat_sessions.json，否则项目根"""
    from account_manager import current, data_dir
    u = current()
    return os.path.join(data_dir(), "chat_sessions.json") if u else SESSIONS_FILE
```

`_read()` 内 3 处、`_write()` 内 2 处 `SESSIONS_FILE` 全部替换为 `_file()`（含 `SESSIONS_FILE + ".broken"` → `_file() + ".broken"`、`tmp = SESSIONS_FILE + ".tmp"` → `tmp = _file() + ".tmp"`）。文件头注释「文件名固定」改为「路径随当前账户上下文（无上下文=项目根）」。

- [ ] **Step 2: user_center.py —— `_load_raw`/`_save_raw` 改走 `_file()`**

在 `UC_FILE = ...`（第 18 行）之后加：

```python
def _file() -> str:
    """数据文件路径：有当前账户上下文时用 accounts/<用户名>/user_center.json，否则项目根"""
    from account_manager import current, data_dir
    u = current()
    return os.path.join(data_dir(), "user_center.json") if u else UC_FILE
```

`_load_raw` 的 `open(UC_FILE, ...)` → `open(_file(), ...)`；`_save_raw` 的 `open(UC_FILE, ...)` → `open(_file(), ...)`。`__main__` 自测不动（重绑定 UC_FILE 仍生效）。

- [ ] **Step 3: shopping_list.py —— 单例支持「路径变化即重载」**

`ShoppingList` 类顶部（`__init__` 之后）加两个方法：

```python
    def _file(self) -> str:
        """数据文件路径：随当前账户上下文（无上下文=项目根）"""
        from account_manager import current, data_dir
        u = current()
        return os.path.join(data_dir(), "shopping_list.json") if u else self.file_path

    def _ensure(self) -> None:
        """账户上下文切换后惰性重载（单例跨账户复用同一实例）"""
        f = self._file()
        if f != self.file_path:
            self.file_path = f
            self.items = self._load()
```

在 8 个公开方法首行各加 `self._ensure()`：`add`、`remove`、`toggle_done`、`clear_done`、`clear`、`pending_count`、`list_text`、`to_list`。

- [ ] **Step 4: product_searcher.py —— save_search_history 按账户落盘**

`save_search_history`（第 882 行）中

```python
    cache_file = os.path.join(BASE_DIR, "search_history.json")
```

改为

```python
    from account_manager import current, data_dir
    u = current()
    cache_file = os.path.join(data_dir(), "search_history.json") if u else os.path.join(BASE_DIR, "search_history.json")
```

- [ ] **Step 5: 回归各模块自测（无上下文路径必须与旧行为完全一致）**

Run:
```
python core/shopping_list.py
python core/user_center.py
```
Expected: 两条 `...自测通过/冒烟通过` 原样输出。

Run: `python -c "import sys; sys.path.insert(0,'core'); import session_store, account_manager, product_searcher; print('imports ok')"`
Expected: `imports ok`

---

### Task 3: core/ai_client.py —— API Key 按账户隔离

**Files:**
- Modify: `core/ai_client.py`（`_load_from_sources` 118-153、`get_config` 159-172、`_write_and_apply` 227-235、`clear_api_key` 238-254、`chat_completion` 310-368、vision 627-669）

- [ ] **Step 1: `_load_from_sources` 参数化 + 新增 `_key_file`/`_cur`**

把 `_load_from_sources`（118-153 行）改为两个函数（保留原函数名作全局兼容）：

```python
def _load_from_sources_into(cfg: LLMConfig, key_file: str) -> None:
    """按优先级加载进指定配置对象：环境变量 > .env 文件 > key_file"""
    api_key = os.getenv(ENV_API_KEY, "").strip()
    base_url = os.getenv(ENV_BASE_URL, "").strip()
    model = os.getenv(ENV_MODEL, "").strip()

    # .env 文件（兼容，只是读取不写入）
    env_path = os.path.join(BASE_DIR, ".env")
    if not api_key and os.path.exists(env_path):
        try:
            with open(env_path, "r", encoding="utf-8") as f:
                for line in f:
                    line = line.strip()
                    if not line or line.startswith("#") or "=" not in line: continue
                    k, v = line.split("=", 1)
                    k, v = k.strip(), v.strip().strip('"').strip("'")
                    if not api_key and k == ENV_API_KEY:   api_key = v
                    if not base_url and k == ENV_BASE_URL: base_url = v
                    if not model and k == ENV_MODEL:       model = v
        except OSError:
            pass

    # 本地 json（用户给的 key 落这里；多账户时为 accounts/<用户名>/api_key.json）
    stored = _safe_read_json(key_file)
    if not api_key:  api_key  = str(stored.get("api_key", "") or "").strip()
    if not base_url: base_url = str(stored.get("base_url", "") or "").strip()
    if not model:    model    = str(stored.get("model", "") or "").strip()
    provider = str(stored.get("provider", "") or "").strip()

    cfg.api_key = api_key
    cfg.base_url = base_url
    cfg.model = model
    cfg.provider = provider


def _load_from_sources() -> None:
    with _cfg_lock:
        _load_from_sources_into(_CFG, KEY_FILE)


_ACCT_CFG: Dict[str, LLMConfig] = {}   # 按账户的配置缓存（含各自成功/失败统计）


def _key_file() -> str:
    from account_manager import current, data_dir
    u = current()
    return os.path.join(data_dir(), "api_key.json") if u else KEY_FILE


def _cur() -> LLMConfig:
    """当前生效配置对象：有账户上下文用该账户的（惰性加载），否则全局 _CFG"""
    from account_manager import current, data_dir
    u = current()
    if not u:
        return _CFG
    with _cfg_lock:
        c = _ACCT_CFG.get(u)
        if c is None:
            c = LLMConfig()
            _load_from_sources_into(c, os.path.join(data_dir(), "api_key.json"))
            _ACCT_CFG[u] = c
        return c
```

（`from typing import Dict` 已存在则不重复导入。）

- [ ] **Step 2: 全部 `_CFG` 读写点替换为 `_cur()`**

- `get_config`（162 行）：`c = _CFG` → `c = _cur()`
- `_write_and_apply`（227-235 行）：锁内 4 行赋值对象改为 `c = _cur()` 再 `c.api_key = ...` 等；落盘 `_safe_write_json(KEY_FILE, data)` → `_safe_write_json(_key_file(), data)`
- `clear_api_key`（238-254 行）：`os.remove(KEY_FILE)` → `os.remove(_key_file())`（外套 try 已有）；`_CFG.success = 0` 等 3 行 → `c = _cur()` 后操作 `c`；`_load_from_sources()` → `_load_from_sources_into(c, _key_file())`；末尾守卫 `if not _CFG.api_key and not os.getenv(ENV_API_KEY):` → 用 `c.api_key`
- `chat_completion`（310-368 行）：310 `if not _CFG.enabled:` → `c = _cur()` + `if not c.enabled:`；312-313 的 `_CFG.api_key, _CFG.base_url`、`_CFG.model` → `c.`；345/352 `_CFG.success += 1` → `c.success += 1`；358-359/367-368 `_CFG.fails += 1`、`_CFG.last_error = f"{_CFG.masked_key()} | ..."` → `c.`
- vision 段（627-669 行）：同样 `c = _cur()` 起手，627/629 及 662/668-669 全部 `c.`

- [ ] **Step 3: 语法与行为自查**

Run: `python -c "import sys; sys.path.insert(0,'core'); import ai_client; print(ai_client.get_config()['enabled'])"`
Expected: `False`（无 Key 环境）或当前本机真实状态（True/配置名），关键是**无异常**。

Run: `python -c "import sys; sys.path.insert(0,'core'); import account_manager, ai_client, os, tempfile; account_manager.DATA_ROOT = tempfile.mkdtemp(); account_manager.set_current('t'); print(ai_client._key_file()); account_manager.set_current(None); print(ai_client._key_file())"`
Expected: 第一行在临时 `DATA_ROOT/t/api_key.json`，第二行是项目根 `api_key.json` 绝对路径。

---

### Task 4: shopping_agent.py —— ChatSession 按账户目录实例化管理器

**Files:**
- Modify: `shopping_agent.py:19-24`（导入区）、`shopping_agent.py:54-61`（`ChatSession.__init__`）

- [ ] **Step 1: 导入区加一行（与其他扁平导入并列）**

```python
from account_manager import data_dir
```

- [ ] **Step 2: `__init__` 数据文件按当前账户解析（无上下文=项目根，行为不变）**

```python
    def __init__(self):
        ddir = data_dir()
        self.profile = ProfileManager(os.path.join(ddir, "user_profile.json"))
        self.searcher = ProductSearcher()
        self.recommender = Recommender(self.profile, self.searcher)
        self.orders = OrderManager(self.profile, orders_file=os.path.join(ddir, "orders.json"))
        self.cart = VirtualCart(file_path=os.path.join(ddir, "virtual_cart.json"))
        self.shopping_list = shopping_list   # 购物清单单例（跨会话共享的需求池；内部按上下文切换文件）
```

（保持其余字段不变；`import os` 已在文件头。）

- [ ] **Step 3: 语法自查**

Run: `python -c "import sys; sys.path.insert(0,'core'); sys.path.insert(0,'.'); import shopping_agent; print('chat session import ok')"`
Expected: `chat session import ok`

---

### Task 5: core/recommender.py —— 线程池上下文传播（全项目仅有的两处后台线程）

**Files:**
- Modify: `core/recommender.py:410-426`（AI 增强 1）、`core/recommender.py:517-543`（AI 增强 2）

- [ ] **Step 1: 两处 `ThreadPoolExecutor` 提交的 callable 用 `account_manager.bound()` 包裹**

site 1（约 410 行 try 块内、`tasks` 判定后）：

```python
                if tasks:
                    import account_manager as _am

                    def _worker(tup):
```

并把 `futs = [ex.submit(_worker, t) for t in tasks]` 改为

```python
                        futs = [ex.submit(_am.bound(_worker), t) for t in tasks]
```

site 2（约 517 行）：

```python
                import concurrent.futures as _cf
                import account_manager as _am
```

并把 `fut = ex.submit(polish_recommendation_with_llm, items_dict, (profile_snapshot or {}))` 改为

```python
                    fut = ex.submit(_am.bound(polish_recommendation_with_llm),
                                    items_dict, (profile_snapshot or {}))
```

- [ ] **Step 2: 语法自查**

Run: `python -c "import sys; sys.path.insert(0,'core'); import recommender; print('recommender ok')"`
Expected: `recommender ok`

---

### Task 6: web_server.py —— 鉴权层 + 每账户会话与共享宿主

**Files:**
- Modify: `web_server.py`（导入区 48-55、会话注册表 61-127、do_OPTIONS、do_GET 183-245、do_POST 247-300、会话管理 389-451、health 191-201、新增 auth 处理器、do_DELETE 305-311）

- [ ] **Step 1: 导入与会话注册表改造**

导入区加：

```python
import account_manager  # noqa: E402  本地多账户：注册/登录/token 与数据目录上下文
```

61-74 行注册表改为（`_SHARED = ChatSession()` 一句删除，`ACTIVE_SESSION_ID` 全局删除）：

```python
# ---------- 多会话注册表（按账户隔离） ----------
# SESSIONS/_SESSION_LOCKS 键 = "用户名:会话id"；每账户激活会话由其自己的 chat_sessions.json(active_id) 承载；
# _SHARED 为每账户的共享管理器宿主（profile/orders/cart 三模块 JSON 持久化按账户目录隔离）。
SESSION_LOCK = threading.Lock()          # 元锁：只保护 SESSIONS/_SHARED 与锁表（毫秒级持有，绝不裹真实抓取）
SESSIONS: Dict[str, ChatSession] = {}    # 内存中的会话实例（键 "用户名:会话id"）
_SESSION_LOCKS: Dict[str, threading.Lock] = {}
_BROWSER_LOCK = threading.Lock()
_CANCEL_FLAGS: Dict[str, bool] = {}
_CANCEL_LOCK = threading.Lock()
_SHARED: Dict[str, ChatSession] = {}     # 每账户的共享管理器宿主（惰性创建）
```

- [ ] **Step 2: 会话装配函数重写（77-127 行整体替换）**

```python
def _new_chat_session(user: str) -> ChatSession:
    """会话工厂：挂上该账户共享的档案/订单/购物车管理器，并重建推荐器绑定"""
    shared = _shared_for(user)
    cs = ChatSession()
    cs.profile = shared.profile
    cs.orders = shared.orders
    cs.cart = shared.cart
    cs.recommender = Recommender(cs.profile, cs.searcher)
    return cs


def _shared_for(user: str) -> ChatSession:
    """每账户的共享管理器宿主（须持有 SESSION_LOCK 调用；构造期间临时切到该账户上下文）"""
    cs = _SHARED.get(user)
    if cs is None:
        account_manager.set_current(user)
        try:
            cs = ChatSession()
        finally:
            account_manager.set_current(None)
        _SHARED[user] = cs
    return cs


def _ensure_default_session() -> str:
    """保证当前账户存在激活会话（须持有 SESSION_LOCK 调用；账户上下文已由请求分发设置）"""
    lst = session_store.list_sessions()
    if lst:
        aid = session_store.get_active_id()
        if aid not in {s["id"] for s in lst}:
            aid = lst[0]["id"]
            session_store.set_active_id(aid)
        return aid
    s = session_store.create_session("默认会话")
    return s["id"]


def _chat(session_id: str = "") -> Tuple[str, ChatSession]:
    """取当前账户的指定/激活会话（须持有 SESSION_LOCK 调用）"""
    user = account_manager.current() or ""
    prefix = user + ":"
    sid = (session_id or "").strip() or session_store.get_active_id() or ""
    if sid and session_store.get_session(sid) is None:
        sid = ""
    if not sid:
        sid = _ensure_default_session()
    key = prefix + sid
    cs = SESSIONS.get(key)
    if cs is None:
        cs = _new_chat_session(user)
        meta = session_store.get_session(sid, with_state=True) or {}
        if meta.get("state"):
            cs.restore_state(meta["state"])
        SESSIONS[key] = cs
    return sid, cs


def _lock_for(sid: str) -> threading.Lock:
    """会话专属锁：按「账户:会话」隔离（须先用元锁注册）"""
    user = account_manager.current() or ""
    key = user + ":" + sid
    with SESSION_LOCK:
        lk = _SESSION_LOCKS.get(key)
        if lk is None:
            lk = threading.Lock()
            _SESSION_LOCKS[key] = lk
        return lk
```

- [ ] **Step 3: 鉴权辅助 + OPTIONS 放行头**

`ShoppingHandler` 类内（`_read_body` 之后）加：

```python
    # ---------- 多账户鉴权 ----------
    def _auth_token(self) -> str:
        return (self.headers.get("X-Auth-Token") or "").strip()

    def _authenticate(self) -> Optional[dict]:
        tok = self._auth_token()
        return account_manager.ACCOUNTS.resolve(tok) if tok else None
```

`do_OPTIONS` 与 `json_response` 中的 `Access-Control-Allow-Headers: "Content-Type"` 改为 `"Content-Type, X-Auth-Token"`（两处）。

- [ ] **Step 4: do_GET 分发改造（183-201 行区域）**

`do_GET` 在 `path` 计算之后、原 health 分支之前插入鉴权；原 health 内联块替换为处理器调用：

```python
        user = self._authenticate()
        if path == "/api/health":
            self._api_health(user)
            return
        if path == "/api/auth/me":
            if user is None:
                json_response(self, 401, {"ok": False, "error": "未登录"})
                return
            json_response(self, 200, {"ok": True, "user": user})
            return
        is_public = path in ("", "/") or path == "/index.html" or path.startswith("/resource/")
        if user is None and not is_public:
            json_response(self, 401, {"ok": False, "error": "未登录"})
            return
        account_manager.set_current(user["username"] if user else None)
```

（其后原有路由顺序保持不变；`/`、`/index.html`、`/resource/*` 放行。）

新增 health 处理器（未登录只给基础信息，不带任何账户数据）：

```python
    def _api_health(self, user: Optional[dict]):
        payload = {"ok": True, "name": "ShoppingAgent Web", "version": "1.0"}
        if user:
            account_manager.set_current(user["username"])
            try:
                with SESSION_LOCK:
                    _sid, cs = _chat()
                    snap = cs.snapshot()
                payload.update({
                    "cart_count": snap.get("cart_count", 0),
                    "list_pending": shopping_list.pending_count(),
                    "data_source_blocked": snap.get("data_source_blocked", False),
                })
            finally:
                account_manager.set_current(None)
        json_response(self, 200, payload)
```

- [ ] **Step 5: do_POST 分发改造（247-300 行区域）**

`do_POST` 在 `body = self._read_body()` 之后、原 `/api/chat` 之前插入：

```python
        if path == "/api/auth/register":
            self._api_auth_register(body)
            return
        if path == "/api/auth/login":
            self._api_auth_login(body)
            return
        if path == "/api/auth/logout":
            user = self._authenticate()
            account_manager.ACCOUNTS.logout(self._auth_token())
            json_response(self, 200, {"ok": True})
            return
        user = self._authenticate()
        if user is None:
            json_response(self, 401, {"ok": False, "error": "未登录"})
            return
        account_manager.set_current(user["username"])
```

新增注册/登录处理器（注册成功且此前账户表为空 → 迁移存量数据）：

```python
    def _api_auth_register(self, body: dict):
        first = account_manager.ACCOUNTS.is_empty()
        r = account_manager.ACCOUNTS.register(str(body.get("username") or ""),
                                              str(body.get("email") or ""),
                                              str(body.get("password") or ""))
        if not r.get("ok"):
            json_response(self, 200, {"ok": False, "error": r.get("error", "注册失败")})
            return
        if first:
            moved = account_manager.migrate_legacy_into(r["username"])
            print(f"[accounts] 首个账户 {r['username']} 注册，继承存量数据 {len(moved)} 个文件")
        json_response(self, 200, {"ok": True, "token": r["token"], "username": r["username"]})

    def _api_auth_login(self, body: dict):
        r = account_manager.ACCOUNTS.login(str(body.get("username") or ""),
                                           str(body.get("password") or ""))
        if not r.get("ok"):
            json_response(self, 200, {"ok": False, "error": r.get("error", "登录失败")})
            return
        json_response(self, 200, {"ok": True, "token": r["token"], "username": r["username"]})
```

- [ ] **Step 6: 会话管理处理器去掉 ACTIVE_SESSION_ID、缓存键加账户前缀（389-451 行）**

- `_api_sessions_get`（392 行）：`active = ACTIVE_SESSION_ID` → `active = session_store.get_active_id()`
- `_api_sessions_post`（397-404 行）：删 `global ACTIVE_SESSION_ID`；`ACTIVE_SESSION_ID = s["id"]` 删除（create_session 已写 active_id）；`SESSIONS[s["id"]] = _new_chat_session()` → `SESSIONS[account_manager.current() + ":" + s["id"]] = _new_chat_session(account_manager.current())`
- `_api_session_detail`（412 行）：`meta["active_id"] = ACTIVE_SESSION_ID` → `meta["active_id"] = session_store.get_active_id()`
- `_api_session_switch`（426-434 行）：删 `global`；`ACTIVE_SESSION_ID = sid` 删除（set_active_id 已写）；`_chat(sid)` 保持
- `_api_session_delete`（437-451 行）：删 `global`；`SESSIONS.pop(sid, None)` → `SESSIONS.pop(account_manager.current() + ":" + sid, None)`；`_SESSION_LOCKS.pop(sid, None)` → `account_manager.current() + ":" + sid`；445-449 行改为：

```python
            aid = session_store.get_active_id()
            if not aid:
                aid = _ensure_default_session()
            else:
                _chat(aid)   # 预加载新激活会话
```

  且响应 `"active_id": ACTIVE_SESSION_ID` → `"active_id": aid`

- [ ] **Step 7: do_DELETE 购物车分支核对**

`do_DELETE` 内 `_chat()` 调用不变（上下文由分发层设置；DELETE 无鉴权入口——在 `do_DELETE` 的 `path` 计算后补与 do_GET 相同的鉴权三行：

```python
        user = self._authenticate()
        if user is None:
            json_response(self, 404, {"ok": False, "error": "Not Found"})
            return
        account_manager.set_current(user["username"])
```

- [ ] **Step 8: 语法与启动自查（不实际 serve）**

Run: `python -c "import sys; sys.path.insert(0,'.'); sys.path.insert(0,'core'); import web_server; print('web_server import ok')"`
Expected: `web_server import ok`（`_SHARED` 已是空 dict，导入不再构造 ChatSession）

---

### Task 7: index.html —— 登录/注册门 + api 包装 + 账户菜单 + 启动流程

**Files:**
- Modify: `index.html`（CSS 追加于 917 行「用户中心」样式区之后；HTML 加在 `<main class="main">` 之前与 `topbar-actions` 内；JS 加在 `api()`（1523 行）附近与 DOMContentLoaded（3116 行））

- [ ] **Step 1: CSS（追加到「/* 用户中心：头像 / 分段选择 / 字号字体偏好 */」区之后）**

```css
/* ===== 多账户：登录/注册门 + 顶栏账户菜单 ===== */
#auth-gate { position: fixed; inset: 0; z-index: 999; display: none;
  align-items: center; justify-content: center; background: var(--bg); }
#auth-gate.show { display: flex; }
.auth-card { width: 360px; max-width: calc(100vw - 40px); background: var(--card);
  border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow-lg);
  padding: 28px 26px; }
.auth-brand { display: flex; align-items: center; gap: 10px; margin-bottom: 18px; }
.auth-brand .material-symbols-outlined { font-size: 28px; color: var(--primary); }
.auth-brand b { font-size: 18px; color: var(--text-1); }
.auth-tabs { display: flex; gap: 6px; background: var(--mute-bg); border-radius: 10px; padding: 4px; margin-bottom: 16px; }
.auth-tab { flex: 1; border: 0; background: transparent; padding: 8px 0; border-radius: 8px;
  cursor: pointer; font-size: 14px; color: var(--text-3); }
.auth-tab.active { background: var(--card); color: var(--primary); font-weight: 600; box-shadow: var(--shadow-sm); }
.auth-field { margin-bottom: 12px; }
.auth-field label { display: block; font-size: 12.5px; color: var(--text-3); margin-bottom: 4px; }
.auth-field input { width: 100%; box-sizing: border-box; padding: 9px 11px; border: 1px solid var(--border);
  border-radius: 8px; font-size: 14px; background: var(--card); color: var(--text-1); }
.auth-field input:focus { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-focus); }
.auth-submit { width: 100%; border: 0; background: var(--primary); color: #fff; padding: 10px 0;
  border-radius: 8px; font-size: 14.5px; font-weight: 600; cursor: pointer; margin-top: 4px; }
.auth-submit:hover { background: var(--primary-dark); }
.auth-submit:disabled { opacity: .6; cursor: default; }
.auth-err { min-height: 18px; font-size: 12.5px; color: var(--danger-t); margin-top: 10px; }
.auth-tip { font-size: 12px; color: var(--text-3); margin-top: 8px; line-height: 1.6; }
.acct-chip { display: inline-flex; align-items: center; gap: 6px; height: 34px; padding: 0 12px;
  border: 1px solid var(--border); border-radius: 17px; background: var(--card);
  color: var(--text-1); font-size: 13px; cursor: pointer; position: relative; }
.acct-chip:hover { border-color: var(--primary); color: var(--primary); }
.acct-chip .material-symbols-outlined { font-size: 18px; }
.acct-menu { position: absolute; right: 0; top: 40px; min-width: 170px; background: var(--card);
  border: 1px solid var(--border); border-radius: var(--radius-sm); box-shadow: var(--shadow);
  padding: 6px; display: none; z-index: 60; }
.acct-menu.show { display: block; }
.acct-menu .acct-info { padding: 8px 10px; font-size: 12.5px; color: var(--text-3);
  border-bottom: 1px solid var(--border); margin-bottom: 6px; word-break: break-all; }
.acct-menu button { width: 100%; border: 0; background: transparent; text-align: left;
  padding: 8px 10px; border-radius: 8px; font-size: 13.5px; color: var(--text-1); cursor: pointer;
  display: flex; align-items: center; gap: 6px; }
.acct-menu button:hover { background: var(--mute-bg); }
.acct-menu button .material-symbols-outlined { font-size: 17px; color: var(--text-3); }
```

- [ ] **Step 2: HTML —— 登录/注册门（插在 `<!-- ============= 主内容 ============= -->` 之前）**

```html
  <!-- ============= 登录/注册门（未登录时全屏） ============= -->
  <div id="auth-gate">
    <div class="auth-card">
      <div class="auth-brand">
        <span class="material-symbols-outlined">shopping_bag</span>
        <b>购物助手 · 本地多账户</b>
      </div>
      <div class="auth-tabs">
        <button class="auth-tab active" id="auth-tab-login" onclick="authSwitch('login')">登录</button>
        <button class="auth-tab" id="auth-tab-reg" onclick="authSwitch('reg')">注册</button>
      </div>
      <div class="auth-field">
        <label>用户名</label>
        <input id="auth-user" maxlength="20" placeholder="2-20 位中英文/数字/下划线">
      </div>
      <div class="auth-field" id="auth-f-email" style="display:none">
        <label>邮箱</label>
        <input id="auth-email" placeholder="仅用于标识账户">
      </div>
      <div class="auth-field">
        <label>密码</label>
        <input id="auth-pass" type="password" placeholder="至少 4 位">
      </div>
      <button class="auth-submit" id="auth-submit" onclick="authSubmit()">登录</button>
      <div class="auth-err" id="auth-err"></div>
      <div class="auth-tip">本地单机使用：注册填写用户名+邮箱+密码即创建成功；各账户数据相互隔离。</div>
    </div>
  </div>
```

- [ ] **Step 3: HTML —— 顶栏账户菜单（`topbar-actions` 内、`mode-btn` 之后）**

```html
        <div style="position:relative">
          <button class="acct-chip" id="acct-chip" onclick="toggleAcctMenu(event)" title="账户">
            <span class="material-symbols-outlined">person</span><span id="acct-name">未登录</span>
          </button>
          <div class="acct-menu" id="acct-menu">
            <div class="acct-info" id="acct-info"></div>
            <button onclick="doLogout()"><span class="material-symbols-outlined">logout</span>退出登录</button>
          </div>
        </div>
```

- [ ] **Step 4: JS —— token 封装、门逻辑、账户菜单（加在 `api()` 函数之后）**

```js
/* =============== 多账户：token / 登录注册门 =============== */
function saToken(){ try{ return localStorage.getItem("sa_token") || ""; }catch(e){ return ""; } }
function saUsername(){ try{ return localStorage.getItem("sa_username") || ""; }catch(e){ return ""; } }
function setSaToken(t, name){
  try{
    if (t) { localStorage.setItem("sa_token", t); localStorage.setItem("sa_username", name || ""); }
    else { localStorage.removeItem("sa_token"); localStorage.removeItem("sa_username"); }
  }catch(e){}
}
let AUTH_MODE = "login";
function authSwitch(mode){
  AUTH_MODE = mode;
  $("#auth-tab-login").classList.toggle("active", mode === "login");
  $("#auth-tab-reg").classList.toggle("active", mode === "reg");
  $("#auth-f-email").style.display = mode === "reg" ? "" : "none";
  $("#auth-submit").textContent = mode === "reg" ? "注册并进入" : "登录";
  $("#auth-err").textContent = "";
}
function showAuthGate(){
  setSaToken("", "");
  $("#acct-name").textContent = "未登录";
  $("#auth-gate").classList.add("show");
  authSwitch("login");
}
function onAuthed(token, name){
  if (saUsername() && saUsername() !== name){
    // 换账户：清掉上个账户的本地缓存（会话选择 / 用户中心偏好 / 引导标记）
    try{ localStorage.removeItem("sa_active_session"); localStorage.removeItem("sa_uc"); localStorage.removeItem("sa_onboarded"); }catch(e){}
    CURRENT_SESSION_ID = "";
  }
  setSaToken(token, name);
  $("#auth-gate").classList.remove("show");
  $("#acct-name").textContent = name;
  $("#acct-info").textContent = "当前账户：" + name;
}
async function authSubmit(){
  const username = $("#auth-user").value.trim();
  const email = $("#auth-email").value.trim();
  const password = $("#auth-pass").value;
  const errEl = $("#auth-err");
  errEl.textContent = "";
  if (!username || !password) { errEl.textContent = "请填写用户名和密码。"; return; }
  if (AUTH_MODE === "reg" && !email) { errEl.textContent = "请填写邮箱。"; return; }
  const url = AUTH_MODE === "reg" ? "/api/auth/register" : "/api/auth/login";
  const body = AUTH_MODE === "reg" ? {username, email, password} : {username, password};
  $("#auth-submit").disabled = true;
  try{
    const r = await fetch(url, {method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify(body)});
    const data = await r.json();
    if (!data.ok) { errEl.textContent = data.error || "操作失败"; return; }
    onAuthed(data.token, data.username);
    bootAfterAuth();
  }catch(e){ errEl.textContent = "服务不可用，请确认后端已启动。"; }
  finally{ $("#auth-submit").disabled = false; }
}
async function ensureAuth(){
  if (!saToken()){ showAuthGate(); return false; }
  try{
    const r = await fetch("/api/auth/me", {headers: {"X-Auth-Token": saToken()}});
    if (r.status === 401){ showAuthGate(); return false; }
    const d = await r.json();
    if (d && d.ok){ onAuthed(saToken(), d.user.username); return true; }
  }catch(e){}
  showAuthGate();
  return false;
}
function toggleAcctMenu(ev){ ev.stopPropagation(); $("#acct-menu").classList.toggle("show"); }
document.addEventListener("click", () => { const m = $("#acct-menu"); if (m) m.classList.remove("show"); });
async function doLogout(){
  try{ await api("/api/auth/logout", "POST", {}); }catch(e){}
  CURRENT_SESSION_ID = "";
  location.reload();
}
let BOOTED = false;
function bootAfterAuth(){
  if (BOOTED) return;
  BOOTED = true;
  loadProfile();
  loadOrders();
  initSessions();
  loadUserCenter();
  maybeOnboard();
  updateBadges({ profile: {}, orders: [] });
}
```

- [ ] **Step 5: JS —— `api()` 注入 token 与 401 拦截（替换 1523-1530 行函数体）**

```js
async function api(url, method="GET", body=null, extra=null) {
  const opts = { method, headers: {"Content-Type":"application/json"} };
  const tk = saToken();
  if (tk) opts.headers["X-Auth-Token"] = tk;
  if (body) opts.body = JSON.stringify(body);
  if (extra) Object.assign(opts, extra);   // 如 { signal }（取消发送）
  const r = await fetch(url, opts);
  if (r.status === 401) { showAuthGate(); return {ok:false, error:"未登录"}; }
  const data = await r.json();
  return data;
}
```

- [ ] **Step 6: JS —— 消灭绕过 api() 的裸 fetch**

搜索 `fetch(` 全部调用点：除 `authSubmit`/`ensureAuth`（门内本身不需 token）外，约 2538 行

```js
  const snap = await (await fetch("/api/health").catch(()=>({json:async()=>({})})).json()) ;
```

替换为

```js
  const snap = await api("/api/health").catch(() => ({}));
```

其余任何 `fetch(`（如图像上传、聊天流）逐个改为经 `api()` 或确认已带 `X-Auth-Token` 头。

- [ ] **Step 7: JS —— DOMContentLoaded 接入门（3116 行起改造）**

```js
window.addEventListener("DOMContentLoaded", async () => {
  // 1) 探活
  try {
    const h = await api("/api/health", "GET");
    if (h && h.ok) {
      toast("后端已就绪：" + h.name + " " + (h.version||""), "ok");
    } else throw new Error("health no ok");
  } catch (e) {
    toast("后端未启动，请双击「启动购物助手-网页版.bat」", "warn");
    $("#welcome-bubble").innerHTML = renderMarkdown(
      WELCOME_TEXT +
      "\n\n**当前未连接后端服务**，请先双击 `启动购物助手-网页版.bat` 启动本地服务（默认端口 8765），再刷新本页面。"
    );
    return;
  }
  applyThemeAssets();   // 按当前皮肤校正 favicon 与欢迎气泡 AI 头像
  // 2) 登录门：通过（或在门内完成登录）后才初始化业务
  if (!(await ensureAuth())) return;
  bootAfterAuth();
});
```

（原第 2 步五行加载调用移入 `bootAfterAuth`，勿重复。）

- [ ] **Step 8: 浏览器实测（chrome-devtools MCP + 临时数据根起服务）**

起测试服务（不碰真实数据）：
```
python -c "import sys,os,tempfile; sys.path.insert(0,'.'); sys.path.insert(0,'core'); import account_manager; t=tempfile.mkdtemp(prefix='_t9_ui_'); account_manager.ACCOUNTS=account_manager.AccountStore(os.path.join(t,'accounts.json')); account_manager.DATA_ROOT=os.path.join(t,'accounts'); account_manager.LEGACY_ROOT=os.path.join(t,'legacy'); import web_server; web_server.run(port=8901, open_browser=False)"
```
（后台运行；`LEGACY_ROOT` 目录需先 `os.makedirs`。）

浏览器走查（golden path + 边界）：
1. 打开 `http://127.0.0.1:8901` → 出现登录/注册门；直接调 `/api/profile` 应 401。
2. 注册 tab：缺邮箱/短密码有就地报错；合法注册后进入主界面，顶栏显示用户名。
3. 对话页发「查看档案」有回包；购物清单页可添加一条。
4. 退出登录 → 回门；注册第二个账户 → 购物清单为空（隔离）。
5. 深色模式切到深色 → 门与菜单配色正常；缩窄窗口门卡片不溢出。
6. 刷新页面 → 免登录直接进入（token 持久）。
7. 控制台无报错；全程无 emoji 图标。

---

### Task 8: 集成自测脚本 _t9_accounts_test.py + 全量回归

**Files:**
- Create: `_t9_accounts_test.py`

- [ ] **Step 1: 写脚本**

```python
# -*- coding: utf-8 -*-
"""多账户机制集成自测：内存起服务于 8901（不动 8765），临时数据根（不碰真实
accounts.json / accounts/ / 根目录数据文件）；不触发抓取与 LLM 连通探测。"""
import json
import os
import sys
import tempfile
import threading
import time
import urllib.request
import urllib.error

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, BASE_DIR)
sys.path.insert(0, os.path.join(BASE_DIR, "core"))

import account_manager

TMP = tempfile.mkdtemp(prefix="_t9_acct_")
account_manager.ACCOUNTS = account_manager.AccountStore(os.path.join(TMP, "accounts.json"))
account_manager.DATA_ROOT = os.path.join(TMP, "accounts")
account_manager.LEGACY_ROOT = os.path.join(TMP, "legacy")
os.makedirs(account_manager.LEGACY_ROOT, exist_ok=True)
# 预置旧数据：验证「首个注册账户继承存量数据」
with open(os.path.join(account_manager.LEGACY_ROOT, "virtual_cart.json"), "w", encoding="utf-8") as f:
    json.dump([{"id": "t1", "name": "旧购物车商品", "platform": "演示", "url": "",
                "current_price": 9.9, "added_at": "2026-09-15 10:00:00"}], f, ensure_ascii=False)

import web_server  # noqa: E402

PORT = 8901

def api(path, method="GET", body=None, token=None):
    req = urllib.request.Request(f"http://127.0.0.1:{PORT}" + path, method=method)
    req.add_header("Content-Type", "application/json")
    if token:
        req.add_header("X-Auth-Token", token)
    data = json.dumps(body).encode("utf-8") if body is not None else None
    try:
        with urllib.request.urlopen(req, data=data, timeout=15) as r:
            return r.status, json.loads(r.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        return e.code, json.loads(e.read().decode("utf-8"))

threading.Thread(target=lambda: web_server.run(port=PORT, open_browser=False), daemon=True).start()
for _ in range(80):
    try:
        api("/api/health"); break
    except Exception:
        time.sleep(0.1)
else:
    raise SystemExit("服务未就绪")

# 1) 未登录一律 401
assert api("/api/profile")[0] == 401, "未登录应 401"
assert api("/api/cart")[0] == 401
assert api("/api/health")[0] == 200 and "cart_count" not in api("/api/health")[1], "匿名 health 只给基础信息"

# 2) 注册校验
st, r = api("/api/auth/register", "POST", {"username": "a b", "email": "x@y.com", "password": "1234"})
assert st == 200 and not r["ok"], "非法用户名应被拒绝"
st, r = api("/api/auth/register", "POST", {"username": "小明", "email": "bad", "password": "1234"})
assert not r["ok"], "非法邮箱应被拒绝"

# 3) 首个注册账户继承存量数据
st, ra = api("/api/auth/register", "POST", {"username": "alice", "email": "a@x.com", "password": "1234"})
assert ra["ok"], ra
ta = ra["token"]
ad = os.path.join(account_manager.DATA_ROOT, "alice")
assert os.path.isfile(os.path.join(ad, "virtual_cart.json")), "旧购物车应迁入 alice"
assert not os.path.isfile(os.path.join(account_manager.LEGACY_ROOT, "virtual_cart.json")), "旧文件应已移走"

# 4) A 的数据只在 A
st, _ = api("/api/cart", "POST", {"action": "add",
           "product": {"name": "A的测试商品", "platform": "演示", "url": "", "current_price": 1.0}}, token=ta)
assert st == 200, "加购应成功"
st, cart_a = api("/api/cart", token=ta)
assert len(cart_a["cart"]) == 2, f"alice 应见 2 项（迁移1+新增1），实际 {len(cart_a['cart'])}"
st, me = api("/api/auth/me", token=ta)
assert me["user"]["username"] == "alice"

# 5) B 注册后从空白开始，看不到 A 的数据
st, rb = api("/api/auth/register", "POST", {"username": "bob", "email": "b@x.com", "password": "5678"})
assert rb["ok"], rb
tb = rb["token"]
st, cart_b = api("/api/cart", token=tb)
assert len(cart_b["cart"]) == 0, "bob 应为空购物车"
st, hb = api("/api/health", token=tb)
assert hb["cart_count"] == 0, "bob health 计数应为 0"
assert len(api("/api/cart", token=ta)[1]["cart"]) == 2, "A 数据不受 B 影响"

# 6) 登出 / 登录 / token 互不影响
api("/api/auth/logout", "POST", {}, token=ta)
assert api("/api/cart", token=ta)[0] == 401, "登出后旧 token 失效"
assert api("/api/cart", token=tb)[0] == 200, "B 的 token 不受影响"
st, rl = api("/api/auth/login", "POST", {"username": "alice", "password": "wrong"})
assert not rl["ok"] and rl["error"] == "用户名或密码错误。"
st, rl = api("/api/auth/login", "POST", {"username": "alice", "password": "1234"})
assert rl["ok"]
assert len(api("/api/cart", token=rl["token"])[1]["cart"]) == 2, "重登后数据还在（持久化隔离）"

# 7) API Key 路径按账户隔离（进程级断言，不触发网络）
account_manager.set_current("alice")
kf_a = ai_client._key_file()
account_manager.set_current("bob")
kf_b = ai_client._key_file()
assert kf_a != kf_b and "alice" in kf_a and "bob" in kf_b, "API Key 文件应按账户分路径"

import ai_client  # noqa: E402  上面引用前置声明：置顶导入亦可

print("_t9 多账户集成自测通过：401/注册校验/首账户迁移/双账户隔离/登出重登/Key路径隔离")
```

（实现时把 `import ai_client` 提到文件顶部 import 区，删掉文末补注。）

- [ ] **Step 2: 运行集成自测**

Run: `python _t9_accounts_test.py`
Expected: `_t9 多账户集成自测通过：401/注册校验/首账户迁移/双账户隔离/登出重登/Key路径隔离`

- [ ] **Step 3: 全量回归**

Run:
```
python core/account_manager.py
python core/shopping_list.py
python core/user_center.py
python -c "import sys; sys.path.insert(0,'.'); sys.path.insert(0,'core'); import web_server, shopping_agent, recommender, ai_client; print('all imports ok')"
```
Expected: 各自通过 + `all imports ok`

---

### Task 9: 配套文档同步

**Files:**
- Modify: `CLAUDE.md`（技术约束·目录约定一句）
- Create: `docs/log/260915.md`（若无则新建，有则追加小节；小节标题下加 `> 创建于 YYYY-MM-DD HH:MM`，用 date 取实际时间）
- Modify: `docs/project/新功能开发区.md`（待完成区勾移至归档区顶部「第2轮」）
- Modify: `docs/project/演示汇报.md`（新增多账户演示点）
- Move: `docs/project/功能模块/多账户/2026-09-15-多账户机制-design.md`、`-plan.md` → `docs/project/功能模块/多账户/archive/`

- [ ] **Step 1: CLAUDE.md 目录约定句修订**

「存储用本地 JSON 文件，无数据库；服务只绑定 127.0.0.1。目录约定：……数据 JSON 仍存项目根」改为「数据 JSON 存项目根或 `accounts/<用户名>/`（多账户隔离）」。

- [ ] **Step 2: 开发日志**

`docs/log/260915.md` 新增小节「多账户机制（注册/登录/数据隔离）」，含：改动文件清单、8 数据文件隔离表、鉴权方式（X-Auth-Token + 持久 token）、首账户继承存量、迁移说明、测试结果（account_manager 冒烟 + _t9 集成 + 浏览器走查）。

- [ ] **Step 3: 新功能开发区归档**

待完成区条目勾掉并整组移入归档区顶部：

```markdown
### 第2轮（260915）：多账户机制

- [X]  建立多账户机制（各个账号直接隔离）；创建账号时填写用户名+邮箱+密码即当作创建成功（当前只是在本地使用，并没有部署上线，所以就先这样简单实现）
  - **完成说明**：已落地。新增 core/account_manager.py（accounts.json 注册/登录/加盐哈希/token 持久化 + 当前账户上下文 + 首账户继承存量迁移）；8 类用户数据（档案/订单/购物车/清单/会话/用户中心/API Key/搜索历史）按 `accounts/<用户名>/` 隔离，login_state 等保持全局；web_server 全端点 X-Auth-Token 鉴权（401 统一拦截），会话缓存与共享宿主按账户分键；前端登录/注册门 + 顶栏账户菜单（退出登录）。设计/实施：`docs/project/功能模块/多账户/archive/`；详见 260915 开发日志。
```

- [ ] **Step 4: 演示汇报.md 增补（沿用现有文档小节格式）**

新增演示点：**多账户隔离**——现场注册两个账号，各自加购物车/记清单，互不可见；重启服务后免登录；强调「API Key 也按账户隔离，平台登录态共享」。

- [ ] **Step 5: spec/plan 归档移动**

```
mkdir docs/project/功能模块/多账户/archive
mv docs/project/功能模块/多账户/2026-09-15-多账户机制-design.md docs/project/功能模块/多账户/archive/
mv docs/project/功能模块/多账户/2026-09-15-多账户机制-plan.md docs/project/功能模块/多账户/archive/
```

- [ ] **Step 6: 汇报开发者**

文字汇总改动清单与验证结果；提醒：git 提交由开发者自持；`docs/project/project.md` 的架构描述如需对齐多账户，建议开发者审核后由 AI 协助修订（不擅自改）。

---

## Self-Review 记录

1. **Spec 覆盖**：账户模型（T1）、8 文件隔离+全局保留（T2/T3/T4）、鉴权 API 与 401（T6）、迁移（T1/T6）、前端门+菜单（T7）、错误边界（T1/T6/T8）、测试计划（T8）、配套文档（T9）——无缺口。
2. **占位符扫描**：无 TBD/TODO；所有代码步均给出完整代码或精确的逐行替换说明。
3. **类型/命名一致性**：`data_file`/`data_dir`/`set_current`/`current`/`bound`/`migrate_legacy_into`/`AccountStore`/`ACCOUNTS` 在 T1 定义、T2-T8 引用一致；`_cur()`/`_key_file()`（T3）与 T8 步骤 7 引用一致；前端 `saToken`/`showAuthGate`/`bootAfterAuth`（T7）内部互引一致。
4. **已知取舍**：`_t9` 中 `ai_client._key_file()` 属进程级断言（测试脚本与 server 同进程），可行；浏览器走查在 T7 步骤 8 独立起临时服务完成。
