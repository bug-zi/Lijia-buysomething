# 优化建议区

存放开发者对项目已有功能或者排版样式的优化建议。

---

## 待完成

- [ ]  请你把对话购物，对话输入框上方的“连衣裙，运动鞋，京东降噪耳机”这三个组件给删去；
- [ ]  用户中心 · 账户信息中的用户头像颜色可以删去了

## 归档区（按轮分组，从新到旧）

> **归档规则**：AI 每实施完一轮优化建议，把该轮完成的全部条目作为一组，在归档区**顶部**
> 新增「### 第N轮（YYMMDD）：本轮主题」小节，条目列于小节下（保留原始描述与完成说明）；
> 轮次从第 1 轮起、只增不减，新轮次永远插在最上方。

### 第 6 轮（260914）：二次确认弹窗主题化

- [X]  请你优化二次删除时候等二次弹窗消息提示的样式：改用和主题适配的，出现在项目中心位置的弹窗提示，不要用当前这种类似于系统消息的弹窗提示
  - **完成说明（2026-09-14）**：新增主题化居中弹窗组件（`.confirm-mask/.confirm-box`，卡片/描边/圆角/危险色全走 CSS 主题变量，四皮肤×深浅模式自动适配，带弹出动画），Promise 化 `askConfirm()`（确认）与 `askText()`（输入）两个助手替换全部系统弹窗——7 处 `confirm()`（删除会话/清空购物车/清空已完成条目/删除 API Key/清除登录态/清空档案/取消订单）与 2 处 `prompt()`（购物车备注、取消原因）清零，浏览器原生 confirm/prompt 不再出现；取消订单的两步（确认+填原因）合并为一个居中输入弹窗，危险操作确认按钮/图标呈危险红；标题正文走 textContent 防注入，支持 Enter 确认 / Esc 与点遮罩取消。浅色、深色、樱花粉换肤联动 Chrome 实测通过，九个入口逐一触发验证、取消路径零副作用（详见 `docs/log/260914.md`）。

### 第 5 轮（260914）：头像体系 + 项目 icon + 属性标签下线

- [X]  需要在用户中心中补充用户头像信息（用户自己上传，和对话购物中的图片上传方式一样，并且要长期保存有效）
  - **完成说明（2026-09-14）**：用户中心账户卡头像改为可点击上传（label 包裹 + hover 主题色描边），交互与对话购物图片上传一致（FileReader 转 data URI 提交，≤2MB）；后端落盘 `resource/user_avatar.<ext>`（覆盖上传自动清理旧残件），路径存 `user_center.json` 长期有效，带 `?v=` 时间戳做缓存穿透；配「恢复默认头像」按钮一键回退。
- [X]  在对话购物中完善AI助手的头像（回头连接gpt-image2.5生成）和用户头像（如果用户在对话购物中上传了图像则使用用户自己的，没有则用系统默认的，到时候也连接gpt-image2.5生成，男生女生版本各一张。在用户第一次使用项目，填写个人信息的时候，根据用户提交的性别信息决定默认用户图像用哪张）
  - **完成说明（2026-09-14）**：生成 `avatar_ai.png`（机器人）与男/女默认头像各一张（风格统一）；对话消息头像统一走 `avatarHtml()`——AI 恒用机器人头像，用户按 自定义头像 → 性别默认（male/female）→ person 图标 三级回退，改性别/头像后历史消息即时刷新；新手引导第 2 步扩为 昵称+性别+可选上传+头像色，选性别即实时预览对应默认头像。
- [X]  用gpt-image2.5生成项目的icon图标，替代左上角的默认图标。
  - **完成说明（2026-09-14）**：生成 `icon.png`（蓝底购物袋+AI 星，与经典蓝主题同系）替换侧栏左上角 Material 图标，并注册为浏览器标签页 favicon。
  - **同日补调（开发者反馈 icon/AI 头像蓝色与四皮肤不匹配）**：补齐四皮肤色系素材（每皮肤一对 icon+AI 头像，gpt-image-2 兜底生成 6 张新图）；侧栏 logo 改纯 CSS 按 `data-skin` 切换（四图叠放显隐，零闪变），favicon 由首屏脚本按皮肤设置、换肤即更新，AI 消息头像经 `aiAvatarSrc()` 按皮肤取图、换肤时已渲染消息整体换装。四皮肤循环实测全部联动正确。
- [X]  关于连接gpt-image2.5：我把中转站的baseURL和APIkey写进D:\Code\举一反三\李佳Agent\buy something\.env.请你自行获取然后调用完成生图
  - **完成说明（2026-09-14）**：读取 `.env` 配置并调通中转站；`gpt-image-2.5-flare/-sunburst` 当前均 503（无可用账号），4 张素材由 `gpt-image-2` 兜底生成（成图已验合格）；生成脚本 `_gen_images_tmp.py` 留根目录，2.5 恢复后可重跑换装（详见 `docs/log/260914.md`）。
- [X]  AI助手输出的商品信息中“**属性标签** ”这一项和链接信息重复，所以请你删去“**属性标签** ”这一信息
  - **完成说明（2026-09-14）**：`core/recommender.py` 输出行删「属性标签」改为独立「商品ID」行（前端 pid 解析不受影响）；前端 TOP3 卡片删 tags 解析/字段/`<dt>` 渲染；`Product.tags` 内部字段保留（筛选/匹配/LLM 点评输入仍需要，仅展示层下线）。实测 TOP3 输出与卡片均无「属性标签」、商品 ID 正常。
- [X]  关于连接gpt-image2.5：中转站当前无 gpt-image-2.5 可用账号（503 No available compatible accounts），第 5 轮 4 张素材暂由 gpt-image-2 兜底生成（已验合格）；2.5 恢复后可删旧图重跑根目录 `_gen_images_tmp.py` 换装，或确认 gpt-image-2 版本可用即删除该脚本。（原存放于待完成区，随第 6 轮归档整理移入本节）

### 第 4 轮（260913）：主题色入口 + 会话列表布局

- [X]  主题色调节不要直接放在导航栏，放在用户中心里面~~~~
  - **完成说明（2026-09-13）**：顶栏肤色点已移除（深色/浅色模式切换按钮按原意保留顶栏）；用户中心「界面与行为偏好」卡片顶部新增「主题色（皮肤）」行——四色点与 data-skin 一一对应（经典蓝/樱花粉/森野绿/暖阳橙），点选立即生效，持久化链路不变（localStorage `sa_skin` + 首屏防闪白）。顺带把 `applySkin` 的选中态选择器收窄为 `.skin-dot[data-skin]`，修复切肤时头像色选中点被误清的小 bug。
  - **同日补调**：顶栏保留的「深色模式」文字钮改为 Material 图标圆钮（`dark_mode`/`light_mode` 随模式切换）并移到状态胶囊之后（顶栏最右端），与开发者选定方案一致。
- [X]  对话购物模块中，因为添加了会话列表，导致对话聊天区域的空间变小了，看着不舒服。请你优化一下设计，改成可展开/收起的样式。并且放在右侧吧这样看着舒服些。
  - **完成说明（2026-09-13）**：会话面板移到聊天区**右侧**，且**默认收起**（聊天区恢复全宽）；聊天区右缘中部新增竖排「会话列表」侧缘把手（Material Symbols 图标 + 竖排文字），点击展开/收起、带宽度过渡动画，展开时把手随面板左缘移动、箭头翻转；展开状态存 localStorage（`sa_side_open`），刷新保持。窄屏（≤900px）浮层同步改为右侧滑出、由同一把手控制；旧 `show-side` 机制与快捷按钮里的「会话列表」chip 已删除。聊天区其余功能（发送/图片上传/快捷按钮）不受影响。
  - **同日补调**：侧缘竖排把手按开发者选定方案改为**顶栏右端图标钮**（`format_list_bulleted`，仅对话购物 tab 显示，展开时高亮 active），面板从顶栏下方滑出/收回；把手 HTML/CSS 已删除，展开/收起与持久化机制不变。

### 第 3 轮（260913）：人群交互风格 + 新手引导补全

- [X]  AI prompt针对不同人群设置不同交互风格；
  - **完成说明（2026-09-13 核实归档）**：已于 `2f565ae` 实现落地，本轮核实全链路后补记归档——用户中心「交互风格（人群）」选择器（默认/学生党/上班族/长辈）保存 persona 偏好；`core/ai_client.py` `_PERSONA_STYLES` 按人群注入导购润色系统提示词（仅语气侧重，不改变事实判断），`polish_recommendation_with_llm` 自动读偏好、缺省回落 default；`core/recommender.py` 推荐链路实际调用生效。
- [X]  刚开始使用的时候的新手引导（在新手引导环节就让用户配置一个网站账户信息进行体验）；
  - **完成说明（2026-09-13）**：引导此前已有（三步：欢迎→昵称头像→就绪），但缺「配置网站账户」环节，本轮补齐——引导扩为四步，新增第 3 步「配置一个购物网站账户（可选，推荐）」，展示京东/淘宝登录态卡片与扫码登录入口（弹出浏览器窗口人工扫码，程序绝不过手账号密码），可跳过、后可在配置库补办；登录态组件容器参数化以复用轮询逻辑；Chrome 实测四步流转、卡片只读渲染、配置库面板回归均正常（详见 `docs/log/260913.md`）。

### 第 2 轮（260912）：emoji 清零 + 主题壁纸美化

- [X]  发现当前AI发的消息还有使用emoji图标的——严禁项目中任何地方出现emoji图标！请你优化修复
  - **完成说明（2026-09-12）**：三层清零——① 后端 13 个源文件约 245 处 emoji 全部改纯文字标签（数据源徽标改 `[真实数据]`/`[演示数据]` 文字标记，CLAUDE.md 红线条款同步修订）；② LLM 出口新增 `ai_client.strip_emoji()` 净化器接入全部返回路径 + 5 处提示词禁 emoji 规则；③ 前端 `appendMsg` 渲染前净化（历史会话里的旧 emoji 显示时一并清除）。TOP3 卡片解析器同步去掉对 ✅/⚠️ 正则的依赖。实测产品代码 emoji 扫描归零，端到端消息干净。
- [X]  美化系统的深色/浅色模式搭配，以及系统的皮肤。给项目补充好看合适的背景图（例如resource中的背景图）
  - **完成说明（2026-09-12）**：`resource/bg-light.png`（樱花粉）/`bg-dark.png`（夜樱蓝）经新增的 `/resource/` 静态路由接入，按深浅模式自动切换；`body::before` 壁纸层 + `body::after` 皮肤主题色低饱和渐变层让四套皮肤与壁纸自动协调；topbar/卡片/聊天区/输入框玻璃化（半透明 + 磨砂，旧浏览器回退纯色）。8 组「模式×皮肤」组合实测截图复核，另修复 TOP-N 卡片触发回归与三处卡片解析错位（详见 `docs/log/260912.md`）。

### 第 1 轮（260912）：配置库

- [X]  配置网站cookie缓存信息这些能不能直接在配置库（把APIkey配置模块升级成配置库，常用网站登录这些操作也在这里完成）然后这个登录信息一般过多久会过期，有没有不过期的方法（比如说配置cookie）
  - **完成说明（2026-09-12）**：配置库已上线——Tab 5 升级为「配置库」，AI Key 区原样保留；新增京东/淘宝登录态**查看 / 立即检测 / 扫码登录（人工扫码，程序绝不过手凭据）/ 按域清除**；Cookie 本体继续由 `.browser_profile` 持久化目录管理（即「cookie 缓存进配置库」的落点：该能力本已存在，本轮补齐的是可见性与管理入口），`login_state.json` 只存状态元数据、绝不含 Cookie。过期问题解答已写入配置库界面「登录有效期说明」：各平台不一（一般几天到几个月），**没有真正永不过期的方法**，本地持久化 + 低频正常使用最接近，失效后一键重新扫码。实施细节见 `docs/log/260912.md`、`全局/archive/2026-09-12-配置库-design.md` 与同名 plan。
