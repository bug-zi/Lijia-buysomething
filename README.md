# 全自动个人购物 AI 助手（ShoppingAgent）

一个运行在个人电脑本地的**对话式购物决策 Agent**。用自然语言说出购物需求（如「买一条夏天的连衣裙，预算200以内」），系统完成 **建档 → 需求解析 → 选品 → 评价分析 → TOP3 推荐 → 购物车/比价 → 下单模拟 → 物流跟踪 → 售后** 全流程辅助。

> ⚠️ **支付红线**：本系统只做下单流程**模拟**（生成订单号），**绝不代用户支付**，也不连接任何真实支付渠道。

## 特性亮点

- **零第三方依赖启动**：后端基于 Python 标准库 `http.server`，前端为单文件 `index.html`，双击 `.bat` 即可运行；
- **AI 增强可选、全链路兜底**：LLM 仅做增强（解析/摘要/润色），不做事实决策；任何 AI 调用失败都静默回退到规则引擎，功能永不因 AI 失效而中断；
- **真实数据优先、绝不编造**：数据源三层回退（真实抓取缓存 → Playwright 真实浏览器抓取 → 演示 Mock），每条商品带 `data_source` 标注（📦 真实 / ⚠️ 演示）；
- **人机协作抓取**：受电商反爬限制，采用「用户粘贴 1-5 个商品详情链接 → Agent 逐个抓取打分」的协作模式；遇验证码暂停交还人工，回复「继续抓取」续跑，绝不批量遍历或绕过平台验证；
- **个人持久档案驱动推荐**：约 27 个字段的用户偏好档案（尺码/色系/风格/材质/预算等）作为打分的永久参考；
- **多服务商 LLM**：OpenAI 兼容协议，自动探测智谱 / DeepSeek / 通义千问 / 硅基流动 / 豆包 / Kimi，视觉任务自动切换视觉模型；
- **API Key 安全**：Key 仅本地存储且脱敏展示（前后 4 位），绝不回传前端明文。

## 快速开始

### 环境要求

- Windows 10/11
- Python 3.9+（安装时勾选 *Add Python to PATH*）

### 启动

| 方式 | 操作 | 说明 |
|---|---|---|
| **网页版（推荐）** | 双击 `启动购物助手-网页版.bat` | 启动本地服务并自动打开浏览器，地址 `http://127.0.0.1:8765/`，仅本机可访问；关闭窗口即停止服务 |
| **命令行版** | 双击 `启动购物助手.bat` | 终端对话，支持 `python shopping_agent.py demo` 跑 18 个演示用例全流程回归 |

两个脚本均内置 4 层 Python 解释器探测兜底，错误日志写入 `%TEMP%\shopping_agent_web_error.log`（网页版）/ `%TEMP%\shopping_agent_error.log`（CLI）。

### 可选：开启真实抓取（安装浏览器驱动）

不安装驱动也能完整体验（走演示 Mock 数据，带 ⚠️ 标注）。要启用**真实搜索与抓取**：

**方式一（推荐）**：双击项目根目录的 `安装浏览器驱动.bat`——自动安装 patchright（反拦截浏览器驱动，默认源失败自动切换清华镜像）、检测本机 Chrome/Edge、按需下载 Chromium 内核，一步到位。

**方式二（手动）**：

```bash
pip install patchright
# 本机没有 Chrome/Edge 时，再下载浏览器内核（约 150MB）：
python -m patchright install chromium
```

> 说明：驱动缺失时应用自动回退原版 Playwright，两者都装不装不影响启动。首次真实搜索若弹出登录页，用手机 APP 扫码一次即可——会话保存在本地 `.browser_profile/`，短期之内无需重复登录；程序绝不索要你的账号密码。

### 可选：配置 LLM（AI 增强）

在网页版「AI 设置」Tab 中填入任一服务商的 API Key 即可；也可以用环境变量：

```bash
set SHOPPING_LLM_API_KEY=你的key
set SHOPPING_LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
set SHOPPING_LLM_MODEL=glm-4-flash
```

不配置 Key 不影响任何功能——所有 AI 环节自动回退规则引擎。默认推荐免费档模型（如智谱 `glm-4-flash`）。

## 使用示例

```text
你：买一个降噪耳机，1500以下，京东
AI：（自动打开京东搜索页，限量抓取真实商品卡片 → 五维打分 → TOP3 推荐：
     图片/价格/销量/链接，标注 📦 真实）
你：买第2款
AI：（订单草稿预览 + 支付风险提醒）确认下单吗？
你：确认
AI：（模拟下单，生成订单号与运单号 → 物流随时间推进 → 已签收后可发起售后）
```

补充说明：搜索结果不到 3 条或被平台拦截时，会提示你**粘贴 1-5 个商品详情链接**走细抓对比（价格/评价/规格更全）；也可以主动发链接深挖某款，细抓后重新打分。

支持增量调整（「第二款太宽松，换更修身的，预算升到300」直接叠加修改）、快捷档案命令（「查看档案 / 修改身高=175 / 录入档案 / 继续建档」）、购物车与降价监控、图片找同款与多图对比等。

## 五维评分模型

加权总分 0-100（`recommender.py`）：

| 维度 | 权重 | 说明 |
|---|---|---|
| 档案匹配度 | 30% | 色系/风格/材质/版型/尺码/品牌/讨厌元素/发货地区 |
| 性价比 | 25% | 折扣率 + 销量 + 预算贴合 |
| 真实口碑 | 20% | 好评率 + 带图追评占比 + 评价量 |
| 差评风险 | 15% | 差评点数量与严重词命中 |
| 发货时效 | 10% | 发货天数 + 自营/次日达加成 |

## 项目结构

```text
├── 启动购物助手-网页版.bat   # 网页版一键启动
├── 启动购物助手.bat          # CLI 一键启动
├── 安装浏览器驱动.bat        # 可选：安装 patchright/playwright 浏览器驱动
├── shopping_agent.py         # 入口 1：主 Agent 总控 ChatSession（意图分流、工作流串联、CLI demo）
├── web_server.py             # 入口 2：HTTP 后端（20 个 API 端点、静态托管，绑定 127.0.0.1）
├── index.html                # 前端单页：6 个 Tab（对话/用户中心/订单/购物车/推荐/配置库）+ 主题/皮肤/新手引导
├── core/                     # 业务模块目录（入口通过 sys.path 自动挂载）
│   ├── request_parser.py     # 需求解析规则引擎 + LLM JSON 增强（含澄清问答泛词守卫）
│   ├── product_searcher.py   # 商品模型、三层回退搜索、演示 Mock、抓取缓存
│   ├── web_scraper.py        # Playwright/Patchright 真实搜索与详情抓取（反拦截、登录墙交还人工）
│   ├── recommender.py        # 五维评分、TOP-N 格式化、AI 点评接入
│   ├── order_manager.py      # 下单模拟、物流虚拟推进、售后/取消
│   ├── profile_module.py     # 偏好档案：19 批次分步引导录入
│   ├── virtual_cart.py       # 购物车 CRUD、降价监控
│   ├── ai_client.py          # LLM/视觉调用、Key 探测与脱敏、人群风格注入、失败回退
│   ├── session_store.py      # 多会话持久化（chat_sessions.json）
│   ├── config_store.py       # 配置库：网站登录态元数据（绝不存 Cookie 内容）
│   └── user_center.py        # 用户中心：账户信息与界面/行为偏好
└── docs/                     # 项目文档库（见下）
```

### 本地数据文件（JSON 即数据库，均在项目根目录）

| 文件 | 内容 |
|---|---|
| `user_profile.json` | 用户偏好档案（敏感，勿外传） |
| `user_center.json` | 用户中心账户与偏好 |
| `chat_sessions.json` | 多会话消息与状态 |
| `scrape_cache.json` / `search_history.json` | 真实抓取缓存（30 分钟 TTL）/ 搜索历史 |
| `virtual_cart.json` | 虚拟购物车 |
| `orders.json` | 模拟订单与物流轨迹 |
| `login_state.json` | 各平台登录状态元数据（不含 Cookie） |
| `api_key.json` | LLM Key（已在 `.gitignore` 中排除，脱敏存储） |

## 安全与合规设计

- **绝不代用户支付**：仅模拟下单流程，确认前后均有支付风险提醒；
- **数据真实**：绝不编造商品数据，真实/演示双标注，抓取失败如实告知；
- **抓取克制**：按用户主动发起的购物需求限量搜索（每平台每次 ≤10 条，不翻页、不遍历、不轮询），详情页支持粘贴 1-5 个链接细抓，真实有头浏览器，遇验证码/登录墙交还人工，不绕过平台风控；
- **隐私本地化**：全部数据存本机 JSON 文件，服务只绑定 `127.0.0.1`，Key 脱敏展示。

## 已知限制（如实告知）

- **历史价格曲线不支持**：仅做当前价横向对比；
- **图片分析仅为视觉推测**：不保证准确，以商品网页为准；
- **真实抓取成功率**受电商平台风控波动影响，抓不到时会明确告知并回退演示数据。

## 开发与测试

- 全流程回归：`python shopping_agent.py demo`（18 个演示用例）；
- 各模块自带 `__main__` 自测入口，如 `python core/recommender.py`；
- 部署形态：单机单进程，升级 = 替换文件重启 `.bat`。

## 文档体系

项目文档库位于 `docs/`：`docs/project/project.md` 是项目全景介绍；各模块子目录下 `design.md`（开发者主导）与 `designs-specs.md`（AI 基于前者生成，开发的直接依据）配套维护；`docs/log/` 按天记录开发日志。权责划分详见 `CLAUDE.md` 与 `docs/project/README.md`。
